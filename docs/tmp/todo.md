# 当前总目标

同步 `windows/tauri-client` 当前主页启动链、会话状态入口和启动期磁盘展示覆盖到正式文档，形成后续继续扩展页面与功能时必须遵守的固定约束。

## 当前未完成子步骤

1. 重建正式架构文档中的主页启动链说明
   - 明确“恢复配置 -> 查询 runtime 磁盘 -> 打开会话 -> 启动重扫 -> 退出启动覆盖 -> 自动连接”的唯一主页启动顺序。
   - 明确“恢复配置”和“打开会话”已经拆成两个独立 Tauri command，不再允许重新并回单一初始化命令。
   - 明确会话重试只重试“打开会话 -> 启动重扫 -> 自动连接”，不重复恢复配置。

2. 重建正式 UI 文档中的会话状态入口说明
   - 明确顶部会话状态块已经重建为可点击按钮，而不是静态标签。
   - 明确初始化态、正常态、失败态三种展示规则，以及对话框对应的状态视觉和行为。
   - 明确会话状态对话框是查看当前状态与执行失败重试的唯一正式入口。

3. 重建正式文档中的磁盘展示覆盖边界
   - 明确“正在初始化 / 会话失败”属于主页展示覆盖态，而不是 Backend 真实磁盘状态。
   - 明确会话成功后磁盘仍可暂时显示 `invalid`，直到启动重扫成功后才恢复真实状态。
   - 明确启动期覆盖映射只能在主页单一映射口完成，不允许散到卡片组件和 API 层补判断。

4. 重建正式文档中的边界与非目标
   - 明确当前轮不处理运行期 `session broken` 自动自愈，不建立后台自动重连体系。
   - 明确托盘重新显示主窗口不重复执行启动自连接。
   - 明确会话状态入口、启动覆盖和手动重扫是三套不同职责，不允许后续混用。

5. 阶段验收与归档
   - 将本轮实现同步到正式 UI / 架构文档后，再更新 `docs/progress/2026-05-14.md` 验收说明。
   - 当前阶段验收口径继续保持：
     - 程序启动直接进入主页；
     - 启动期磁盘统一显示无效覆盖；
     - 会话成功后自动重扫并恢复真实状态；
     - 顶部会话状态按钮正确反映初始化 / 正常 / 失败；
     - 失败后点击状态按钮可打开对话框并执行重试。

## 当前轮边界

- 当前轮只同步正式文档，不继续扩展新的运行期会话恢复机制。
- 当前轮不修改 Backend 真实磁盘状态模型。
- 当前轮不把会话状态对话框再扩展成完整控制面板。
- 当前轮继续坚持 `Element Plus` 为基础，只记录已经落地的必要接管与边界。

## 当前唯一下一步

先同步正式架构文档，明确主页启动链、Tauri command 边界和会话失败重试链。

参考：

- `docs/development/workflow.md`
- `docs/development/development-principles.md`
- `docs/progress/README.md`
