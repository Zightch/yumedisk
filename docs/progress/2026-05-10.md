# 2026-05-10

## 本次新增完成

- 已完成 `BackendCore` 抽离工作的 `Step 2`：重建对外类型与完整配置面。
- 已把 `windows/BackendCore/types/types.h` 的 core 对外结构收口为纯运行时语义：
  - `SessionConfig`
  - `DiskConfig`
  - `CreateDiskRequest`
  - `ManagedDiskSnapshot`
  - `DiskMetadata`
  - `DiskQueueConfig`
- 已把 `BackendCore` 的 session / disk 配置拆开，不再继续混成单个 `AppConfig`：
  - session 级：
    - `heartbeatIntervalMs`
    - `initialEventQueueCapacity`
  - disk 级：
    - `targetId`
    - `sectorSize`
    - `diskSizeBytes`
    - `queueDepth`
    - `writeSlotBytes`
    - `readWorkerCount`
    - `writeWorkerCount`
    - `ackBatchMaxRanges`
    - `readOnly`
- 已把 `CreateDiskRequest` 中宿主输入态字段彻底拿掉：
  - 不再出现 `requestedMode`
  - 不再出现 `rawFilePath`
  - 不再夹带 UI 表单概念
- 已把介质标识收口为 core 运行时枚举 `MediaKind`：
  - `denseMem`
  - `sparseMem`
  - `rawFile`
- 已新增 `BackendCore` 配置构造与校验入口：
  - `validateSessionConfig`
  - `validateDiskConfig`
  - `validateCreateDiskRequest`
  - `buildAkOpenParams`
  - `buildAkDiskParams`
- 已把 `BackendCore/runtime/runtime.cpp` 的打开 session / 建盘路径改为直接消费上述显式配置：
  - `AkOpen` 不再吃硬编码的 `heartbeatIntervalMs / initialEventQueueCapacity`
  - `AkCreateDisk` 不再由 core 内部隐式推导 `queueDepth / worker / ack` 组合
  - 当前 runtime 只负责校验、落地、记录日志和驱动 `AppKernel`
- 已把 `BackendCore/media/media.cpp` 的介质初始化边界同步切到纯 core 语义：
  - 只接 `MediaKind`
  - 不再解析宿主输入态字段
  - 仍保持“介质实例必须由宿主侧先创建再移交”这条边界不变

## 本次新增验收

- `cmake --build windows\\BackendCore\\cmake-build-debug --clean-first` 已通过。
- 当前 `BackendCore.dll` 已在类型与配置面重建后继续独立编译通过。

## 本次确认边界

- 这一步只完成 `BackendCore` 对外类型与完整配置面重建，不继续在本轮切换 `client -> BackendCore` 调用链。
- 当前 `BackendCore` 仍只保留 `Media` 抽象接口，不承接具体 `Media` 子类实现。
- 当前 `BackendCore` 的建盘路径已经改成吃显式 disk 配置，但宿主侧 `media` 创建与 UI 参数整形仍待后续 `backendHost` 重建时接入。
- 下一步直接进入“重建 `BackendCore` 运行时主线”，继续把 `BackendContext / DiskRuntime / event thread / stats / debug snapshot` 从当前过渡形态收口成 DLL 内唯一 runtime 真状态。

## 本次新增完成（`BackendCore` Step 3）

- 已完成 `BackendCore` 抽离工作的 `Step 3`：重建 `BackendCore` 运行时主线。
- 已把 `windows/BackendCore/types/types.h` 继续收口回纯对外类型头，只保留：
  - 默认常量
  - `MediaKind`
  - `SessionConfig`
  - `DiskConfig`
  - `CreateDiskRequest`
  - `ManagedDiskSnapshot`
  - `BackendStatsSnapshot`
  - `DebugSnapshot`
- 已把以下运行时内部结构从 public types 中移出，改为 DLL 内部私有实现：
  - `DiskMetadata`
  - `DiskQueueConfig`
  - `DiskLifecycleState`
  - `DiskMediaState`
  - `DiskRuntime`
- 已新增 `windows/BackendCore/runtime/runtimeDisk.h` 作为 `BackendCore` 内部私有运行时头，专门承接：
  - 单盘 metadata
  - 单盘 queue 配置
  - 单盘 lifecycle
  - 单盘 media 持有
  - 单盘 staging 持有
- 已把 `BackendContext` 从 `types/types.h` 中抽离出来，重建为 `windows/BackendCore/runtime/runtime.h` 中的正式 runtime API 类。
- 已把 `BackendContext` 的真实运行时状态全部收进 `BackendContext::Impl`，由 DLL 内唯一持有：
  - `SessionConfig`
  - `AK_SESSION*`
  - `stopEvent`
  - `stop`
  - `eventThread`
  - `map<targetId, DiskRuntime>`
  - `logLines`
  - `openStatus / openWin32Error / openSucceeded`
- 已确认当前 session 真状态、日志缓冲、事件线程、单盘 runtime map 不再作为 public struct 字段暴露给宿主解释。
- 已保留并收口 `BackendContext` 对外最小运行时操作面：
  - `setSessionConfig`
  - `open / close`
  - `querySessionStateText`
  - `snapshotLogLines`
  - `snapshotManagedDisks`
  - `queryBackendStats`
  - `queryDebugSnapshot`
  - `createManagedDisk`
  - `removeManagedDisk`
  - `removeAllManagedDisks`
- 已把 `queryBackendStats().diskCount` 的统计来源收回 core 内部 `map<targetId, DiskRuntime>`，不再通过外部快照二次解释。
- 已把 `media/media.cpp` 与 `StagingStore/StagingStore.cpp` 对 `DiskRuntime` 的依赖改为只包含内部私有头，不再依赖 public types 暴露内部运行时结构。
- 已把 `config/config.h`、`media/media.h` 的底层 `AppKernel` 类型依赖改为显式包含，不再依赖 `types/types.h` 间接带出。

## 本次新增验收（`BackendCore` Step 3）

- `cmake --build windows\\BackendCore\\cmake-build-debug --clean-first` 已通过。
- 当前 `BackendCore.dll` 在 runtime 主线内收后继续独立编译通过。

## 本次确认边界（`BackendCore` Step 3）

- 这一步只完成 `BackendCore` 运行时主线内收，不继续在本轮重建宿主到 core 的 `Media` 移交接口。
- 当前 `BackendCore` 仍不承接具体 `Media` 子类实现；宿主创建 `Media` 后移交给 core 的正式入口仍留到下一步完成。
- 当前 `createManagedDisk` 路径的剩余过渡点已经收窄为“宿主如何把 `Media` 实例正式交给 core”，不再是 runtime 真状态暴露问题。
- 下一步直接进入“重建 `BackendCore` 建盘与删盘接口”，把宿主移交 `Media`、参数校验、建盘删盘收口成正式 core 接口。

## 本次新增完成（`BackendCore` Step 4）

- 已完成 `BackendCore` 抽离工作的 `Step 4`：重建 `BackendCore` 建盘与删盘接口。
- 已把 `windows/BackendCore/types/types.h` 中只剩过渡意义的 `CreateDiskRequest` 删掉，避免 core 对外类型继续承接“宿主输入打包对象”。
- 已把 `windows/BackendCore/runtime/runtime.h` 的建盘入口收口为显式参数面：
  - `DiskConfig`
  - `MediaKind`
  - `std::unique_ptr<Media>`
- 已把 `windows/BackendCore/runtime/runtime.cpp` 的建盘主线改为正式消费宿主移交的 `Media` 实例，不再停留在“core 里只知道 `MediaKind`，但 `Media` 实例尚未进入正式接口”的过渡状态。
- 已把 `windows/BackendCore/config/config.h` 与 `windows/BackendCore/config/config.cpp` 的建盘校验入口改为显式校验：
  - `DiskConfig`
  - `MediaKind`
  - `Media*`
- 已新增 core 侧建盘一致性校验：
  - `mediaKind` 不能为空
  - `media` 实例不能为空
  - `media->sizeBytes()` 必须与 `diskConfig.diskSizeBytes` 一致
- 已明确收掉一个隐藏推导点：
  - core 不再在建盘时根据 `rawFile` 介质偷偷改写 `diskSizeBytes`
  - `diskSizeBytes` 必须由宿主显式给出并与移交的 `Media` 实例一致
- 已把 `windows/BackendCore/media/media.cpp` 的介质接入动作从“按 `MediaKind` 初始化”收口为“接收宿主已经创建好的 `Media` 并正式接管持有权”。
- 已保留并确认删盘、全删盘、关闭路径仍由 `BackendCore` 统一负责：
  - `removeManagedDisk`
  - `removeAllManagedDisks`
  - `close`
  - 清理时继续统一释放 `Media` 与 `StagingStore`

## 本次新增验收（`BackendCore` Step 4）

- `cmake --build windows\\BackendCore\\cmake-build-debug --clean-first` 已通过。
- 当前 `BackendCore.dll` 在正式接入宿主 `Media` 移交接口后继续独立编译通过。

## 本次确认边界（`BackendCore` Step 4）

- 这一步只完成 `BackendCore` 的正式建删盘接口收口，不继续在本轮重建 `client` 宿主适配层。
- 当前 `BackendCore` 仍只暴露 `Media` 抽象接口，不承接任何具体 `Media` 子类实现。
- 当前 `BackendCore` 已完成“core 接收宿主创建好的 `Media`”这一条正式边界，下一步不再需要继续为建盘接口补过渡包装。
- 下一步直接进入“重建 `client` 宿主适配层 `backendHost`”，把 `QString/QFileInfo/UI DTO` 与 `BackendCore` 运行时接口彻底隔开。

## 本次新增完成（`client` Step 1）

- 已完成 `BackendCore` 最小抽离闭环的 `client Step 1`：重建 `client` 宿主适配层 `backendHost`。
- 已新增 `windows/client/backendHost/BackendHost/`，把宿主适配面正式收口为 `BackendHost`：
  - 负责 `QString` / 表单输入解析
  - 负责 `QFileInfo` / 路径存在性判断
  - 负责 `rawFile` 大小与 `4096` 对齐校验
  - 负责 `denseMem / sparseMem / rawFile` 选择
  - 负责创建具体 `Media`
  - 负责调用 `BackendCore`
  - 负责把 core 快照转换成 UI DTO
- 已把 `BackendHostCreateDiskRequest` 扩成宿主输入态对象，当前除原有容量、目标号、只读、介质模式、`rawFilePath` 外，也预留了承接 core disk 配置的文本字段：
  - `sectorSizeText`
  - `queueDepthText`
  - `writeSlotBytesText`
  - `readWorkerCountText`
  - `writeWorkerCountText`
  - `ackBatchMaxRangesText`
- 已把 `Widget/CreateDiskDialog -> Backend` 的旧调用链切到：
  - `Widget/CreateDiskDialog`
  - `-> BackendHost`
  - `-> BackendCore`
- 已把 `client` 中原先混着 runtime 真状态的旧实现从编译链路中摘掉，并删除以下过渡文件：
  - `windows/client/backend/Backend.*`
  - `windows/client/backend/config/`
  - `windows/client/backend/runtime/`
  - `windows/client/backend/StagingStore/`
  - `windows/client/backend/types/`
  - `windows/client/backend/media/media.*`
  - `windows/client/backend/media/Media/Media.h`
- 已确认当前 `client` 不再自行编译和持有：
  - session 真状态
  - `DiskRuntime` map
  - staging 生命周期
  - `AppKernel` 事件线程
- 已把 `client` 的构建链最小切到 `BackendCore`：
  - `windows/client/CMakeLists.txt` 改为引入并链接 `BackendCore`
  - `client` 产物目录在构建后复制：
    - `BackendCore.dll`
    - `AppKernel.dll`
- 已把 `windows/client/AGENTS.md` 中的推荐结构示例同步到 `backendHost` 新目录形态。

## 本次新增验收（`client` Step 1）

- `cmake --build windows\\client\\cmake-build-debug --clean-first` 已通过。
- 当前 `client` 已可以在链接 `BackendCore` 的前提下继续完成 Qt 工程构建。

## 本次确认边界（`client` Step 1）

- 这一步只完成宿主适配层重建，不继续在本轮搬运具体 `Media` 实现目录归属。
- 当前具体 `Media` 实现仍暂留在 `windows/client/backend/media/`，只是已不再承担 runtime 主线职责。
- 当前 `BackendHost` 只负责输入整形、介质创建、DTO 转换与 core 调用，不重新持有 runtime 真状态。
- 下一步直接进入“重建 `client` 介质实现归属”，把 `FileMedia/MemoryMedia/RawFileMedia` 从 `client/backend/media/` 收到正式宿主侧目录。

## 本次新增完成（`client` Step 2）

- 已完成 `BackendCore` 最小抽离闭环的 `client Step 2`：重建 `client` 介质实现归属。
- 已把宿主侧具体介质实现从过渡目录 `windows/client/backend/media/` 收到正式目录：
  - `windows/client/media/FileMedia/`
  - `windows/client/media/MemoryMedia/`
  - `windows/client/media/RawFileMedia/`
- 已把 `client` 构建链中的具体介质源文件切到正式宿主侧目录：
  - `windows/client/CMakeLists.txt`
- 已把宿主适配层与介质内部引用同步改到正式宿主侧路径：
  - `windows/client/backendHost/BackendHost/BackendHost.cpp`
  - `windows/client/media/RawFileMedia/RawFileMedia.h`
  - `windows/client/media/FileMedia/FileMedia.cpp`
  - `windows/client/media/MemoryMedia/MemoryMedia.cpp`
  - `windows/client/media/RawFileMedia/RawFileMedia.cpp`
- 已移除 `windows/client/backend/media/` 这组过渡目录残留，避免继续误导 `backend` 仍持有具体介质实现。
- 已确认当前边界进一步稳定为：
  - `BackendCore` 只保留 `Media` 抽象接口
  - `client` 保留 `BackendHost + FileMedia/MemoryMedia/RawFileMedia`
  - 具体文件介质家族不进入 `BackendCore`

## 本次新增验收（`client` Step 2）

- `cmake --build windows\\client\\cmake-build-debug --clean-first` 已通过。
- 已确认 `windows/client/` 下不再存在 `backend/media` 旧路径引用。

## 本次确认边界（`client` Step 2）

- 这一步只完成宿主侧具体介质实现归属收口，不继续在本轮推进 UI 验收与脚本联调。
- 当前 `BackendHost` 继续负责具体介质实例创建，再把 `std::unique_ptr<Media>` 移交给 `BackendCore`。
- 当前 `BackendCore` 继续只依赖 `Media` 抽象接口，不反向依赖任何宿主侧文件介质实现。
- 下一步直接进入“重建最小运行验收口”，验证当前 `client.exe + BackendCore.dll + AppKernel.dll` 的最小闭环运行链。
