# 2026-05-28

## 已完成

### 1. tauri-client 文件盘尾块处理已改为逻辑补齐，不再丢弃不足盘块的数据

- `windows/tauri-client/src-tauri/src/backend/raw_file_media.rs` 已把 RAW 文件容量模型从“向下截断到盘块边界”改为“按盘块大小向上补齐为逻辑容量”。
- 文件实际大小不足最后一个盘块时：
  - 真实文件内已有字节继续参与磁盘数据读取。
  - 超过真实 EOF 但仍落在逻辑盘容量内的部分，读取固定返回 `0`。
  - 写入跨过真实 EOF 时，只写入真实文件覆盖范围内的前缀；超出真实 EOF 的尾部字节直接丢弃，不写回文件。
- `0` 字节 RAW 文件仍保持无效，不允许作为文件盘打开。

### 2. 文件盘对齐粒度已收口到 BackendRust 的 sector_size，不再写死 512

- `windows/tauri-client/src-tauri/src/backend/config_service.rs` 已新增统一入口，当前从 `backend_rust::DiskConfig::default().sector_size` 获取盘块大小。
- `windows/tauri-client/src-tauri/src/backend/persistence_service.rs` 中 RAW 文件探测与打开已改为显式使用该 sector size。
- `windows/tauri-client/src-tauri/src/backend/disk_service.rs` 与 `windows/tauri-client/src-tauri/src/network/runtime_flow.rs` 中磁盘挂载配置已显式使用同一 sector size，避免探测容量与挂载配置不一致。

### 3. file media 读写链路边界已收束

- 当前修改只落在 `tauri-client` 的 file media 实现层，没有改 `BackendRust` 的 staging / overlay / commit 行为。
- 因此最终落盘结果已满足：
  - 尾块真实文件内前缀可读写。
  - 逻辑补齐部分读恒为 `0`。
  - 逻辑补齐部分不持久化。

## 验收

- `windows/tauri-client/src-tauri` 执行 `cargo test raw_file_media --lib` 通过，当前 `7` 个相关测试通过。
- `windows/tauri-client/src-tauri` 执行 `cargo test --lib` 通过，当前 `31` 个库测试通过。
- 已新增覆盖：
  - 逻辑容量按对齐粒度向上补齐
  - 小于一个盘块的非空 RAW 文件可打开
  - 跨真实 EOF 读取尾部自动补零
  - 跨真实 EOF 写入只保留真实文件范围内前缀
  - `4096` sector size 场景下按配置粒度对齐

## 当前边界

- 本轮没有修改 `BackendRust` staging 层；未提交写入在 staging 覆盖读期间，仍按现有 staging 语义工作。
- 当前 sector size 来源仍是 `backend_rust::DiskConfig::default().sector_size`，还没有做成用户态可变配置。
- 本轮只修改了 file media；memory media 与 network media 的读写语义未改。

### 4. tauri-client 创建网络盘草稿卡片已收口展示细节

- `windows/tauri-client/src/features/createNetworkDisk/NetworkDraftList.vue` 已将草稿项删除入口改为垃圾桶图标按钮。
- 草稿项容量区已改为在实际容量值后内联追加状态后缀，根据元数据 `readOnly` 展示为 `10.0 GB · 读写` 或 `20.0 GB · 只读` 这类形式。
- 本次仅收紧删除按钮圆角为 `8px`，草稿卡片自身圆角保持原样不变。

### 5. Unit Attention / DataChanged Phase A 已开始落地到 YumeDiskSCSI

- `windows/shared/yumedisk_proto.h` 已将共享版本口径从 `0.1.0.0` 升级到 `0.1.0.1`，并预留 `YumeDiskCommandNotifyDataChanged` 命令号给后续阶段继续接通。
- `windows/YumeDiskSCSI/YumeDiskSCSI/core/defs.h` 已为每个 target 补上 `PendingDataChangedUa` 真状态，保持 `pending UA` 只由 `YumeDiskSCSI` 持有。
- `windows/YumeDiskSCSI/YumeDiskSCSI/control/control.c` 已新增 miniport 内部 `NotifyDataChanged` 入口：
  - 只接受 `Header.TargetId`
  - `PayloadLength` 必须为 `0`
  - target 首次从 `pending=false` 变为 `true` 时，best-effort 触发 `StorPortAsyncNotificationDetected(..., RAID_ASYNC_NOTIFY_FLAG_MEDIA_STATUS)`
- `windows/YumeDiskSCSI/YumeDiskSCSI/scsi/scsi.c` 已补正式 `Unit Attention` 设备语义：
  - `REQUEST SENSE` 在存在 pending 时返回一次 `UNIT ATTENTION / ASC 28h / ASCQ 00h`
  - `TEST UNIT READY`、`READ`、`WRITE`、`READ CAPACITY`、`MODE SENSE`、`VERIFY` 在存在 pending 时先返回一次 `CHECK CONDITION + UNIT ATTENTION`
  - `INQUIRY` / `REPORT LUNS` 不消费 pending UA
- `windows/YumeDiskSCSI/YumeDiskSCSI/core/protocol.c` 已补：
  - `DiskTryMarkPendingDataChangedUa`
  - `DiskTryConsumePendingDataChangedUa`
  - `StorPortSetUnitAttributes` 注册 `DeviceAttentionSupported + AsyncNotificationSupported`
  - `StorPortAsyncNotificationDetected(...MEDIA_STATUS)` 的目标地址构造与统一调用

### 6. Unit Attention / DataChanged Phase B 已接入 YumeDiskKMDF 同步代理

- `windows/YumeDiskKMDF/YumeDiskKMDF/control/ioctl.c` 已为 `YumeDiskCommandNotifyDataChanged` 补上正式白名单入口。
- `NotifyDataChanged` 当前固定走现有 `ControlProxyMessage` 同步控制面，不引入新的异步 transport 或额外事件总线。
- `KMDF` 侧唯一校验已收在 `control/ioctl.c`：
  - `PayloadLength` 必须为 `0`
  - `Header.TargetId` 必须不超过 `YUMEDISK_MAX_USABLE_TARGET_ID`
- 当前 `KMDF` 不维护任何 target 级 `pending UA` 镜像状态，只做会话约束、参数校验和向 miniport 的同步代理。

### 7. Unit Attention / DataChanged Phase C 已补齐 AppKernel 下行 API

- `windows/AppKernel/include/appkernel.h` 已新增正式公开接口 `AkNotifyDiskDataChanged(AK_DISK* disk)`，当前只承载单盘 `data_changed` 下行通知。
- `windows/AppKernel/src/protocol/ak_protocol.c` 已新增 dedicated sender `AkProtocolNotifyDataChanged(...)`：
  - 固定发送 `YumeDiskCommandNotifyDataChanged`
  - `PayloadLength` 固定为 `0`
  - target 编号在 AppKernel 层先做一次合法性校验
- `windows/AppKernel/src/disk/ak_disk.c` 已把入口语义收在 `AkDiskNotifyDataChanged(...)`：
  - `AK_DISK*` 不能为空
  - 只允许 `AkStateRunning` 生命周期发起
  - 盘对象必须仍注册在所属 `AK_SESSION` 的 disk list 中，否则返回 `AK_STATUS_NOT_FOUND`
  - transport 或命令发送失败时，沿用现有 `CommandFailures/LastError` 记录路径
- `windows/AppKernel/src/session/ak_session.c` 已补 `AkSessionIsDiskRegistered(...)`，把“target 是否仍存在”留在 AppKernel 本层收口，不把这层职责下推给 KMDF/SCSI。

### 8. Unit Attention / DataChanged Phase D 已补齐 BackendRust 单盘下行接口

- `windows/BackendRust/src/appkernel.rs` 已同步 FFI 声明 `AkNotifyDiskDataChanged(...)`，使 Rust 宿主可以直接调用当前正式 AppKernel 下行 API。
- `windows/BackendRust/src/runtime.rs` 已为宿主补上正式入口 `notify_managed_disk_data_changed(target_id, out_error_text)`：
  - 固定只接单盘 `target_id`
  - 先校验 session 已打开
  - 再校验 target 仍存在于当前 `BackendRust` 管理盘集合内
  - 再校验目标盘已有有效 `AK_DISK*` 句柄
  - 再查询当前生命周期，固定只允许 `running` 盘下发
  - 最后同步调用 `AkNotifyDiskDataChanged(...)`
- 当前失败文本已收成最小集合：
  - `session-not-open`
  - `target-not-found`
  - `disk-handle-not-ready`
  - `disk-not-running(<lifecycle>)`
  - `query-disk-state-failed(0xXXXXXXXX)`
  - `notify-data-changed-failed(0xXXXXXXXX)`
- 当前 `BackendRust` 继续明确不做：
  - `smid` 注册表
  - sibling 查找
  - 共享组状态缓存
  - `WriteFinalCommitted -> sibling notify` 隐式 fanout
  - `NotifyDataChanged` 专用后台线程、队列或 retry 状态机
- `AkEventWriteFinalCommitted` 事件线程语义保持不变，仍只负责当前盘 staged write commit，不顺手触发任何其他盘通知。

### 9. BackendRust SDK 已同步到当前公开面

- `windows/BackendRust/BackendRust-SDK文档.md` 已同步补齐：
  - `SECTOR_ALIGNMENT_BYTES` 常量导出
  - `query_component_version_snapshot()` 公开方法
  - `notify_managed_disk_data_changed()` 的固定语义、入口约束、失败文本与边界
- 同时修正 `DiskConfig.ack_batch_max_ranges` 默认值为当前真实实现值 `96`，避免 SDK 文档继续滞后代码。

### 10. Unit Attention / DataChanged Phase E 已收口到 rust-cli 本地共享内存正式命令面

- `windows/rust-cli/src/cli/local/memory/` 已成为本地共享内存唯一目录入口：
  - `media.rs` 承载共享 `DenseMem`
  - `registry.rs` 承载 `smid` 分配、`smid -> media`、`smid -> bound targets`、`target -> smid` 绑定关系
- `windows/rust-cli/src/cli/shell.rs` 与 `windows/rust-cli/src/cli/host.rs` 已把命令面正式收口为：
  - `sm <size-mib>`
  - `ct size=<mib> [ro=<true|false>] [target=<id>]`
  - `ct smid=<id> [ro=<true|false>] [target=<id>]`
  - `rm target=<id>|target=all`
  - `rm smid=<id>|smid=all`
- 旧口径已删除，不再保留兼容桥：
  - `ct <disk-size-mib> [dense|auto] [true|false] [target]`
  - 旧 `rm all`
- `rust-cli` 当前承担 sibling fanout 唯一职责：
  - 轮询 `BackendRust::poll_managed_disk_event()`
  - 当收到 `WriteFinalCommitted` 时，仅对同一 `smid` 下“除自己外”的 sibling target 调 `notify_managed_disk_data_changed(...)`
  - `BackendRust` 仍不缓存共享组状态，也不在事件线程里直接做 sibling notify
- 共享组生命周期已收口：
  - target 删除时同步解绑 `smid`
  - `rm smid=<id>` 在仍有 bound targets 时固定拒绝 `smid-in-use`
  - `rm smid=all` 在任意共享区仍被占用时整条拒绝

### 11. Unit Attention / DataChanged Phase F 已完成本地最小闭环验收

- `windows/rust-cli/src/cli/shell.rs` 已补最小调试命令：
  - `dbg-read target=<id> offset=<bytes> length=<bytes>`
  - `dbg-write target=<id> offset=<bytes> hex=<...>`
- 本轮 runtime 验收固定按共享本地 `MemoryMedia` 跑通：
  - `sm 64`
  - `ct smid=1 target=3 ro=false`
  - `ct smid=1 target=4 ro=false`
- 真实写入阶段使用管理员 raw disk 句柄对 `\\\\.\\PhysicalDrive1` 执行 512B 扇区写：
  - 偏移 `4096` 写入 `UA-PHASE-F-20260528...`
  - 偏移 `4608` 再写入第二段标记数据
- sibling 可见性已黑盒确认：
  - 写入前 `\\\\.\\PhysicalDrive2` 对应偏移读取全 `0`
  - 写入后 `\\\\.\\PhysicalDrive2` 在相同偏移直接读到新字节
  - 写入前后 `PhysicalDrive1/2` 均保持 `RAW / Online / IsOffline=False / IsReadOnly=False`
- sibling 生命周期已确认：
  - 删除 target `3` 后，target `4` 仍保持 `running, online=true`
  - 此后系统侧继续可通过 `\\\\.\\PhysicalDrive2` 读到已写入数据
- 共享组删除语义已实测：
  - `rm smid=1` 在 target 仍绑定时返回 `smid-in-use`
  - `rm target=all` 后 `smid` 仍保留且 `bound_targets=none`
  - 随后 `rm smid=all` 成功清空共享区
- 本机 runtime 限制已确认：
  - 直接对 `\\\\.\\PhysicalDrive2` 发 `IOCTL_SCSI_PASS_THROUGH_DIRECT(TEST UNIT READY)` 仍返回 Win32 `1306`
  - 因此当前黑盒验收未直接抓到一次 `REQUEST SENSE 28/00`
  - 该 UA 正式语义继续由阶段 A-D 的驱动链测试覆盖

## 本次验收

- 使用 `C:\\opt\\MicrosoftVisualStudio\\2022\\Community\\MSBuild\\Current\\Bin\\MSBuild.exe` 执行：
  - `msbuild windows\\YumeDiskSCSI\\YumeDiskSCSI.sln /t:Build /p:Configuration=Release /p:Platform=x64 /m` 通过
  - `msbuild windows\\YumeDiskKMDF\\YumeDiskKMDF.sln /t:Build /p:Configuration=Release /p:Platform=x64 /m` 通过

- 使用 `cmake` + `MSVC 2022` 执行：
  - `cmake -S windows\\AppKernel -B build\\appkernel-vs2022 -G "Visual Studio 17 2022" -A x64` 通过
  - `cmake --build build\\appkernel-vs2022 --config Release` 通过
- 使用 `Qt MinGW 13.1.0` 执行：
  - `cmake -S windows\\AppKernel -B build\\appkernel-mingw-qt1310 -G "MinGW Makefiles" -DCMAKE_C_COMPILER=C:/Qt/Tools/mingw1310_64/bin/gcc.exe` 通过
  - `cmake --build build\\appkernel-mingw-qt1310` 通过
- 使用 `cargo` 执行：
  - `cargo test --verbose` 已在 `windows/BackendRust/` 下通过
  - 当前 `BackendRust` 单测共 `11` 项通过，已覆盖 `notify_managed_disk_data_changed()` 的未开 session、未知 target、无句柄、非 running 生命周期、成功下发，以及失败不清 staging 等路径
- 使用 `cargo` 继续执行：
  - `cargo test` 已在 `windows/BackendRust/` 下再次通过，当前共 `12` 项测试通过
  - `cargo test` 已在 `windows/rust-cli/` 下通过，当前共 `24` 项测试通过
- 使用管理员 raw disk 句柄执行本地共享盘闭环：
  - `\\\\.\\PhysicalDrive1` 的真实 512B 写入可在 `\\\\.\\PhysicalDrive2` 上直接读回
  - `rust-cli debug_stats events_queued` 从建盘后的 `2` 增至首次写入后的 `3`，对应一次额外 `WriteFinalCommitted` 宿主事件入队
- 本机 `VS 2026 / MSBuild 18` 与当前 `WDK 17` 任务 DLL 不匹配，会在进入源码编译前失败；本轮已切回 `VS 2022 / MSBuild 17` 完成正式验证。
