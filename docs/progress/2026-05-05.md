# 2026-05-05

## 已完成

- 完成“Strip the current data path down to a minimal skeleton and delete contamination points”子步骤。
- `windows/shared/yumedisk_proto.h` 收缩为控制面骨架，只保留 `QueryInfo / CreateDisk / RemoveDisk / RemoveAllDisks / Heartbeat` 基础命令；旧 `WaitEvent / ReadReply / WriteAck` 及其事件结构已从当前编译路径移除。
- `windows/YumeDiskSCSI` 旧数据面已退出当前实现：
  - `queue` 层改成最小空壳，只保留清理入口。
  - `control` 层不再分发 `WaitEvent / ReadReply / WriteAck`，也不再向当前编译路径保留旧 synthetic event 逻辑。
  - `scsi` 读写 CDB 不再走旧 pending/event 模型，当前骨架阶段直接拒绝 READ/WRITE，等待下一步新协议入口重建。
- `windows/tests/RWTestApp` 已改成控制面骨架工具；旧 wait worker、read reply、write ack、内存介质模拟和压测后端全部移除。
- `windows/tests/MyApp5` 已移除 `wait` 命令和旧 `WaitEvent` 调试路径。
- 完成“Rebuild the protocol entrypoints on top of the skeleton”子步骤。
- `windows/shared/yumedisk_proto.h` 已切换到新协议命令空间，加入 `PostReadSlot / PostWriteSlot / ReadAck / WriteAckBatch / SubmitSlot / CancelSlot` 以及对应 payload 结构，不再保留旧 `WaitEvent / ReadReply / WriteAck` 协议体。
- `windows/YumeDiskKMDF/YumeDiskKMDF/control/ioctl.c` 已改成两类入口：
  - 控制消息继续走 `QueryInfo / CreateDisk / RemoveDisk / RemoveAllDisks` 透传。
  - 新数据面入口已切到 `PostReadSlot / PostWriteSlot / ReadAck / WriteAckBatch / CancelSlot` 语义，并在 KMDF 侧把 slot / ack 转成 `SubmitSlot / ReadAck / WriteAckBatch / CancelSlot` 小描述符转发给 SCSI。
- `windows/YumeDiskSCSI/YumeDiskSCSI/control/control.c` 已识别并校验新协议 payload；当前阶段对 `SubmitSlot / ReadAck / WriteAckBatch / CancelSlot` 在校验通过后统一返回 `STATUS_NOT_SUPPORTED`，明确下一步应由真实队列实现接手。
- `windows/tests/RWTestApp/main.cpp` 与 `windows/tests/MyApp5/main.cpp` 已改成新的 `DeviceIoControl(input=message, output=buffer)` 收包方式，匹配新的 KMDF 边界解释。
- 完成“Rework KMDF session handling around file-bound lifecycle and locked timeout state”子步骤。
- `windows/YumeDiskKMDF/YumeDiskKMDF` 的 session 生命周期已从 device-global 下沉到 file-bound：
  - `CTRL_FILE_CONTEXT` 现在持有 `SessionLock / WatchdogTimer / MiniportHandle / SessionId / LastHeartbeatTick / State`。
  - `CTRL_DEVICE_CONTEXT` 只保留独占打开控制，不再缓存数据面默认 miniport handle。
- `ControlSessionTryOpen` 现在在 App 打开 file object 时立即建立 session、打开 miniport handle、启动 watchdog；`ControlSessionCleanup` 在 file cleanup 时固定执行 `RemoveAllDisks | SessionClose -> close miniport handle -> release open slot`。
- heartbeat 机制已收敛为仅在 KMDF 内维护：
  - `Heartbeat` IOCTL 只更新 KMDF session 的 `LastHeartbeatTick`。
  - `YumeDiskSCSI` 不再承担 heartbeat 机制。
- watchdog 超时后，KMDF 会把该 file session 切到 locked 状态，发送 `RemoveAllDisks | SessionClose` 清盘并取消后端，再关闭自己持有的 miniport handle；在 file object 真正关闭前，该 session 的后续 IOCTL 直接失败。
- `transport` 层已改成基于 file session 工作：
  - `ControlOpenMiniportHandle(...)` 显式打开 file-bound miniport handle。
  - `ControlProxyCommand(...)`、`ControlCloseMiniportHandle(...)`、`ControlSendSessionCleanup(...)` 都改为围绕 `CTRL_FILE_CONTEXT` 工作。
- `control/ioctl.c` 已改为从 `WdfRequestGetFileObject(Request)` 定位 session；除 heartbeat 外，其余命令都先获取 file session 再透传，避免 handle/state 与 watchdog cleanup 竞态。
- `windows/tests/RWTestApp/main.cpp` 已增加自动 heartbeat 后台线程；`windows/tests/MyApp5/main.cpp` 已增加手工 `heartbeat` 命令，便于 session 生命周期调试。
- 完成“Rewrite SCSI queue processing to drain while work is available and split lock scope”子步骤。
- `windows/YumeDiskSCSI/YumeDiskSCSI/core/defs.h` 已把 miniport 队列状态从单一控制锁扩成三段：
  - `SessionLock` 只管 session claim / close。
  - `ReadQueueLock` 只管 posted read slots 与 pending reads。
  - `WriteQueueLock` 只管 posted write slots、pending writes 与 ACK 落账。
  - `NextEventId` 改成 interlocked 自增，不再依赖单大锁。
- `windows/YumeDiskSCSI/YumeDiskSCSI/queue/queue.c` 已从空壳改成真实数据面队列：
  - `SubmitSlot` 现在会把 App 预投 read/write slot 变成长期 pending miniport control SRB。
  - READ SRB 会排入 pending read 表，命中同 target 的 posted read slot 后立刻回填 `YUMEDISK_READ_SLOT_EVENT` 并完成 slot。
  - WRITE SRB 会排入 pending write 表，消费 posted write slot 时在锁外复制片段数据并回填 `YUMEDISK_WRITE_SLOT_HEADER`。
  - `ReadAck` 会按 `eventId` 找回原始 READ SRB，把 App buffer 拷回系统 buffer 后完成请求。
  - `WriteAckBatch` 会按 bitmap 落账 `eventId + seq`，全部确认后完成原始 WRITE SRB；单个 fragment 失败只打死对应大 WRITE，不影响后续 WRITE。
  - `CancelSlot`、`RemoveDisk`、`RemoveAllDisks`、`SessionClose` 都会把对应 target 或整 session 的 pending slots / pending SRBs 清掉，而不是把 stale 项留在队列里。
- 请求推进已经改成 `drain while available`：
  - READ/WRITE/slot submit/ACK 进入后，都会持续循环消费当前已经可匹配的 slot 与 pending request，而不是只推进一个就等下一次异步唤醒。
  - 大数据 copy、App slot completion、系统 SRB completion 都被移到锁外，避免高队列深度下把 spin lock 变成 CPU 热点。
- `windows/YumeDiskSCSI/YumeDiskSCSI/scsi/scsi.c` 已不再直接拒绝 READ/WRITE：
  - 现在会解析 READ/WRITE(6/10/12/16) CDB 的 LBA 与 block count。
  - 合法 READ/WRITE 会进入新的 pending queue 路径；非法长度或非法 CDB 仍在边界返回错误。
- `windows/YumeDiskSCSI/YumeDiskSCSI/control/control.c` 的新协议命令已经真正接到队列层：
  - `SubmitSlot` 可挂起并在稍后由真实 I/O 完成。
  - `ReadAck` / `WriteAckBatch` / `CancelSlot` 不再只是 `STATUS_NOT_SUPPORTED` 空壳。
  - `PostWriteSlot` 自带的 ACK trailer 现在会在 SCSI 侧先消费，再续租 write slot credit。
- `windows/shared/yumedisk_proto.h` 已把 `YUMEDISK_MINIPORT_TIMEOUT_SEC` 从 `30s` 提高到 `3600s`，避免长期 pending slot 在当前实现下天然踩到 miniport control timeout。
- 完成“Rework App backend scheduling to remove whole-backend serialization and piggyback write ACK”子步骤。
- `windows/tests/RWTestApp/main.cpp` 已从控制面骨架工具重建为真实的 App-owned queue backend：
  - `BackendContext` 只保留 session、配置、统计和磁盘表；磁盘表由 map 级 mutex 管理，不再用一个全局 `diskLock` 串行整个后端。
  - 每个 `ManagedDisk` 都独立持有内存介质、`256 KiB` 条带 `shared_mutex`、pending write ACK 队列、read worker 池、write worker 池和 idle ACK flush 线程。
  - `ct` 创建磁盘后立即启动该 target 的 worker；`rm` / `rm all` / session close 会先停 worker 再移除磁盘，worker 生命周期和磁盘生命周期绑定。
- 读路径的 App 后端并发模型已经落下：
  - read worker 持续预投 `PostReadSlot`，拿到 `YUMEDISK_READ_SLOT_EVENT` 后直接从 App 介质复制到 read buffer，再发送 `ReadAck`。
  - 读复制只按目标 LBA 命中的条带加共享锁，不再把整个后端读路径串起来。
- 写路径的 App 后端并发模型已经落下：
  - write worker 持续预投 `PostWriteSlot`，稳态下会把上一批已完成 fragment 的 `YUMEDISK_WRITE_ACK_RANGE` 作为 trailer piggyback 到下一次 `PostWriteSlot` 输入里。
  - 收到 write slot 后，App 只对命中的介质条带加独占锁，完成写入后把对应 ACK range 记入 per-disk pending 队列。
  - 单独的 `WriteAckBatch` 现在只作为 idle flush / 收尾 / 异常回收兜底，由每盘独立 flush 线程负责发送。
- `RWTestApp` 已补齐新的最小运行面：
  - 保留后台 heartbeat 线程维持 KMDF file session。
  - CLI 支持 `ct / rm / rm all / ls / stats / exit`，并支持 `--queue-depth`、`--slot-bytes`、`--io-size`、`--disk-size-mb`、`--sector-size`、`--target` 参数，便于下一步直接回连压测。
- 完成“Reconnect the benchmark loop”代码侧子步骤。
- `windows/tests/RWTestApp/main.cpp` 现在会在 `ct` 之后主动等待新盘枚举出来，并把可见磁盘信息绑定回对应 target：
  - 创建磁盘后会轮询可见 `GUID_DEVINTERFACE_DISK`，优先把新出现的 YumeDisk 识别为当前 target，并记录 `device path + PhysicalDriveN`。
  - `ls` 现在会刷新并打印每个 managed target 对应的 `path` 和 `physical_drive`，不再只给 target 编号。
- `RWTestApp` 已补上压测入口输出：
  - 新增 `bench [target]` 命令，可按当前 target 重新打印原始磁盘路径和建议的 `diskspd` 命令。
  - `ct` 在成功识别 `PhysicalDrive` 后会直接输出 `Q1T1 / Q8 / Q32` 的 `1M` 顺序读写 `diskspd` 命令，便于马上回连压测。
- 本轮按用户要求未实际运行 benchmark；本轮只保证 benchmark 入口已回连且工程可编译，实际吞吐量/CPU/稳定性验证留给下一步人工执行。

## 验收

- `cmake --build out/rwtestapp-skeleton --config Debug` 通过。
- `cmake --build out/myapp5-skeleton --config Debug` 通过。
- `MSBuild.exe windows/YumeDiskSCSI/YumeDiskSCSI/YumeDiskSCSI.vcxproj /t:Build /p:Configuration=Debug /p:Platform=x64` 完成编译和链接；最终失败点为环境侧 `InfVerif.dll` 缺失，不是源码编译错误。
- `MSBuild.exe windows/YumeDiskKMDF/YumeDiskKMDF/YumeDiskKMDF.vcxproj /t:Build /p:Configuration=Debug /p:Platform=x64` 结论同上：源码编译和链接通过，最终卡在环境侧 `InfVerif.dll` 缺失。
- 第二轮验证中，`RWTestApp`、`MyApp5`、`YumeDiskSCSI`、`YumeDiskKMDF` 的源码编译均通过；驱动项目仍只剩同一个环境侧 `InfVerif.dll` 缺失问题。
- 第三轮验证中，`RWTestApp`、`MyApp5`、`YumeDiskSCSI`、`YumeDiskKMDF` 在本轮 session/watchdog 改动后仍保持源码编译和链接通过；驱动项目最终失败点依旧只有环境侧 `InfVerif.dll` 缺失。
- 第四轮验证中，`RWTestApp`、`MyApp5`、`YumeDiskSCSI`、`YumeDiskKMDF` 在本轮 SCSI queue 重写后仍保持源码编译和链接通过；驱动项目最终失败点依旧只有环境侧 `InfVerif.dll` 缺失。
- 第五轮验证中，`RWTestApp` 在 App backend 调度重写后重新通过 `cmake --build out/rwtestapp-skeleton --config Debug`。
- 第五轮验证中，`MyApp5` 继续通过 `cmake --build out/myapp5-skeleton --config Debug`。
- 第五轮验证中，`YumeDiskSCSI`、`YumeDiskKMDF` 在本轮 app 侧并发后端重建后仍保持源码编译、链接、测试签名与打包通过；最终失败点依旧只有环境侧 `InfVerif.dll` 缺失。
- 第六轮验证中，`RWTestApp` 在 benchmark 回连工具面补齐后继续通过 `cmake --build out/rwtestapp-skeleton --config Debug`。
- 第六轮验证中，`MyApp5` 继续通过 `cmake --build out/myapp5-skeleton --config Debug`。
- 第六轮验证中，`YumeDiskSCSI`、`YumeDiskKMDF` 在本轮 benchmark 回连操作面改动后仍保持源码编译、链接、测试签名与打包通过；最终失败点依旧只有环境侧 `InfVerif.dll` 缺失。

## 已收束边界

- 当前仓库已经没有旧 `WaitEvent` 数据面主路径。
- 当前骨架阶段只保留控制面闭环，读写压测能力暂时故意断开。
- 下一步必须从新协议入口开始重建，不能回退到旧 event inline data 模型。
- 当前新协议入口、KMDF session/watchdog、SCSI real queue 都已经落下；剩余缺口已经收缩到 App backend worker 与 benchmark 闭环，不再是驱动侧协议骨架问题。
- heartbeat/watchdog 的唯一真实来源已经收敛到 KMDF file session；SCSI 不再承担 App 存活探测职责。
- miniport handle 生命周期已经与 App file object 绑定，不再允许回退到 device-global lazy cached handle 模型。
- session 一旦 watchdog 超时进入 locked 状态，在 file object 关闭前不允许通过新的 heartbeat 或新的 slot 请求复活。
- 当前 SCSI 已经具备真实 pending queue 和 while-drain 行为，下一步不应再回退到“每次只处理一个、靠下一次唤醒继续推进”的单步模型。
- 当前实现仍然使用长期 pending `IOCTL_SCSI_MINIPORT` slot SRB，只是先把 timeout 拉长到 `3600s`；真正摆脱这类长期 pending transport 风险仍留给后续更深一层的收束。
- App backend 已经切到每盘独立 worker + 条带锁模型；当前不应再回退到“整个后端一把锁”或“收到一个 slot 只推进一步”的串行路径。
- write ACK 的稳态路径已经收敛为 piggyback 到下一次 `PostWriteSlot`；独立 `WriteAckBatch` 现在只保留给空窗 flush / 收尾 / 异常恢复。
- Benchmark 回连入口现在已经在 `RWTestApp` 中恢复到可直接操作的状态；剩余缺口只剩人工执行 `Q1T1 / Q8 / Q32`、记录吞吐量/内核 CPU，并确认不再出现挂死、磁盘 100% 和 session 泄漏。
