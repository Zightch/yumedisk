# 2026-05-14

- 重建 windows/tauri-client/src/widgets/DiskListPanel/DiskListPanel.vue 外壳结构，去掉 l-card 头体分层，改为单一圆角裁剪面，收紧磁盘列表面板上下圆角露底问题。
- 已继续收创建内存盘 / 文件盘对话框结构：
  - 创建类对话框只保留右上 / 右下圆角，左上 / 左下改为直角；
  - 内存盘对话框改为固定标题 + 正文独立纵向滚动；
  - 文件盘对话框改为固定标题 + 固定 tabs 头 + 正文独立纵向滚动；
  - 文件盘对话框不再依赖 `el-tab-pane` 承担正文布局，而是由对话框自己根据当前 tab 渲染内容区；
  - `取消 / 创建` 按钮并入正文滚动链末尾，不再依赖默认 dialog footer。
- 当前这轮对话框微调的目标不是补样式细节，而是把头部、页签、正文和滚动责任重新划清，避免小高度窗口下出现纵向滚动缺失、正文居中悬空和横向滚动残留。 
- 已完成启动链第一步重建：
  - 删除 `windows/tauri-client/src/pages/init/InitPage.vue` 独立初始化页入口；
  - `windows/tauri-client/src/app/App.vue` 改为启动后直接进入主页；
  - 新增 `windows/tauri-client/src/features/homeBootstrap/useHomeBootstrap.ts`，将当前主页启动时序收口为单一编排模块；
  - `windows/tauri-client/src/pages/home/HomePage.vue` 已切换到编排模块提供的启动入口，不再在页面组件内直接维护整段初始化时序。
- 当前这一步只重建了前端入口与主页编排边界，没有开始拆分 Rust 侧“恢复配置 / 打开会话”命令，也还没有接入新的会话状态按钮和启动期展示覆盖。
- 已完成启动链第二步重建：
  - `windows/tauri-client/src-tauri/src/backend/session_service.rs` 已拆为 `restore_client_state` 与 `open_session` 两段明确语义；
  - `windows/tauri-client/src-tauri/src/commands/session.rs` 不再保留 `initialize_client` 聚合命令，改为两个单职责 Tauri command；
  - `windows/tauri-client/src/shared/api/sessionClient.ts` 已切换为 `restoreClientState()` 与 `openSession()` 两个前端桥接入口；
  - `windows/tauri-client/src/features/homeBootstrap/useHomeBootstrap.ts` 已改为“先恢复 runtime / 配置，再查询主页磁盘，再单独打开 session”的主页启动链；
  - `windows/tauri-client/src/entities/session/model.ts` 已去掉不再需要的 `InitializeClientResponse` 包装结构，前端直接消费稳定的 `SessionSnapshot`。
- 当前这一步只收紧了“恢复配置 / 打开会话”的后端与前端命令边界，还没有开始重建主页的启动期展示覆盖、会话状态按钮与失败重试入口。
- 已完成启动链第三步重建：
  - `windows/tauri-client/src/entities/session/model.ts` 新增前端唯一 `sessionPhase` 基础类型，明确 `initializing / ready / failed` 三态；
  - `windows/tauri-client/src/features/homeBootstrap/useHomeBootstrap.ts` 新增 `sessionPhase` 与 `diskDisplayPhase` 两个唯一状态源，不再把启动期展示判断散在 UI 组件内；
  - `windows/tauri-client/src/features/homeBootstrap/homeDisplayMapper.ts` 新增主页唯一展示映射口，统一把启动期磁盘覆盖为 `invalid + 正在初始化`，会话失败覆盖为 `invalid + 会话失败`；
  - `windows/tauri-client/src/pages/home/HomePage.vue` 已改为持有真实 `runtimeDisks`，并只在主页通过映射器生成 `displayDisks` 下发给列表；
  - `windows/tauri-client/src/widgets/AppHeader/AppHeader.vue` 已改为消费 `sessionPhase`，顶部状态块现可区分“正在初始化 / 会话正常 / 会话失败”。
- 当前这一步已经把“真实磁盘状态”和“主页展示覆盖状态”分开，但还没有完成启动顺序重排：当前仍是“打开会话后直接自连接，再退出启动覆盖”，还没把启动重扫插到自连接前。
- 已完成启动链第四步重建：
  - `windows/tauri-client/src/features/homeBootstrap/useHomeBootstrap.ts` 已重建为唯一主页启动主链，启动顺序收为“恢复配置 -> 查询 runtime 磁盘 -> 打开会话 -> 启动重扫 -> 退出启动覆盖 -> 自连接”；
  - 启动重扫已并回 `useHomeBootstrap` 内部状态源，不再由 `HomePage.vue` 自己直接操作 `rescan_runtime_disks` 返回结果；
  - 启动重扫失败时，当前 runtime 磁盘列表会保留，并继续通过主页统一展示覆盖显示为 `invalid`，不再因为重扫失败直接把列表清空；
  - 自连接已改为严格使用“启动重扫后的快照”作为输入，只在磁盘恢复到真实展示态后再触发；
  - `diskDisplayPhase` 已改为只在启动重扫成功后切到 `normal`，不再在 `finally` 中以“会话 ready”这一条件粗放退出启动覆盖。
- 当前这一步已经完成启动顺序重排；下一步开始重建顶部会话状态按钮与会话状态对话框，收紧失败重试入口。
- 已完成会话状态入口重建：
  - `windows/tauri-client/src/features/homeBootstrap/useHomeBootstrap.ts` 已进一步拆出“恢复配置 / 加载磁盘列表”和“打开会话 / 启动重扫 / 自动连接”两段链路，并新增 `retryOpenSessionFlow()` 作为唯一失败重试入口；
  - 顶部会话状态块已重建为可点击按钮，文件位于 `windows/tauri-client/src/widgets/AppHeader/AppHeader.vue`，初始化态显示旋转指示，正常态显示“会话正常”，失败态显示“会话失败”；
  - 新增 `windows/tauri-client/src/features/sessionStatus/SessionStatusDialog.vue`，会话状态查看与失败重试不再散落在主页其他组件中；
  - 会话对话框说明文案已通过 `windows/tauri-client/src/entities/session/presenter.ts` 统一映射，避免按钮文本、对话框标题和说明各写一套；
  - 会话失败说明已优先读取 Tauri `ApiError.detail`，例如 Backend 返回的底层状态文本，不再只显示笼统失败消息；
  - `open_session` 成功后，顶部会话状态会立即切到正常态；磁盘列表仍保持启动期 `invalid` 覆盖，直到启动重扫成功后才退出覆盖并进入自动连接。
- 当前这一步已经完成会话状态按钮、会话状态对话框和失败重试入口；下一步转入正式 UI / 架构文档同步，收口这些规则的长期约束。
- 验证：
  - 已执行 `pnpm build`，构建通过；
  - 已执行 `cargo check`，通过；
  - `cargo check` 过程中遇到过一次 `tauri-client.exe` 占用构建产物，清理旧进程后已恢复正常；
  - 仍存在既有 `@vueuse/core` Rollup 注释警告与 chunk size 警告，与本步无关。
