# 2026-05-08

## 已完成

- 已按当前 `workflow` 将 `docs/tmp/todo.md` 从 `AppKernel` 重建任务切换为 `windows/client` 最小闭环任务：
  - 当前总目标已收紧为“完成客户端最小闭环”
  - 当前路线固定为“先收功能目标，再整理 `AppKernel` 接入调用面，最后补 `UI`”
  - 当前边界已明确不引入 `YumeAgent`、`IPC`、独立后台服务进程和额外 runtime/store/viewmodel 层级树
  - 已进一步收束可见盘枚举边界：
    - 不下沉到 `AppKernel`
    - 继续留在宿主控制层
    - 为避免 `TestApp` 和 `client` 重复维护，后续收敛为宿主侧共享扫描静态库
  - 当前最小闭环能力已固定为：
    - 查看 `AppKernel session` 状态
    - 创建磁盘
    - 删除磁盘
    - 查看受管磁盘列表
    - 查看关键日志
    - 显式退出并完成删盘、关 session 收口
  - 当前唯一下一步已收紧为：
    - 先把 `windows/client` 从 Qt 示例 `Widget` 壳收紧为可承接真实客户端实现的最小工程骨架，并为后续接入宿主侧共享扫描静态库留出清晰结构位置
- 已将宿主侧可见盘扫描能力抽成共享静态库 `scan`，并完成命名与结构收束：
  - 新增 [windows/scan/CMakeLists.txt](../../windows/scan/CMakeLists.txt)
  - 新增 [windows/scan/scan.h](../../windows/scan/scan.h)
  - 新增 [windows/scan/scan.cpp](../../windows/scan/scan.cpp)
  - 头文件已上移到项目根，删除 `include/` 目录
  - 实现文件保持扁平结构，不再保留额外 `src/` 目录
  - 项目名已从中间态 `HostScan` 收束为最终名 `scan`
  - 宿主侧扫描命名已去掉 `yumedisk_host_` 前缀
- `TestApp` 已切到共享扫描静态库：
  - 删除当时 `windows/TestApp/src/scan.cpp`
  - 删除当时 `windows/TestApp/src/scan.h`
  - 当时 `windows/TestApp/CMakeLists.txt` 改为链接 `scan`
  - 当时 `windows/TestApp/src/types.h` 改为直接复用 [windows/scan/scan.h](../../windows/scan/scan.h) 的 `DiskIdentity`
  - 当时 `windows/TestApp/src/runtime.cpp` 改为调用共享扫描接口
  - 当时 `windows/TestApp/src/media.cpp` 补齐 `WinIoCtl.h` 显式依赖，消除原先依赖本地扫描头的传递包含
- `client` 工程已完成共享扫描静态库接线：
  - 当时 `windows/client/CMakeLists.txt` 已同时接入 `AppKernel` 和 `scan`
  - 后续客户端宿主代码可直接复用同一套可见盘扫描实现，不再复制 `SetupAPI` 枚举逻辑
- `AppKernel SDK` 文档已同步当前唯一边界：
  - [windows/AppKernel/AppKernel-SDK文档.md](../../windows/AppKernel/AppKernel-SDK文档.md) 已明确 `AppKernel` 不承接系统可见盘枚举
  - `scan` 已进入 SDK 交付物与最小接入路径
  - 已补充 `scan` 的职责边界与推荐调用面
- 已新增只读盘建盘参数：
  - [windows/shared/yumedisk_proto.h](../../windows/shared/yumedisk_proto.h) 的 `YUMEDISK_CREATE_DISK` 新增 `ReadOnly`
  - [windows/AppKernel/include/appkernel.h](../../windows/AppKernel/include/appkernel.h) 的 `AK_DISK_PARAMS` 新增 `ReadOnly`
  - `AppKernel` 仅做参数透传，不新增第二套只读数据面分支
- `YumeDiskSCSI` 已收束为只读唯一行为闸口：
  - 建盘时保存 target 的 `ReadOnly` 属性
  - `MODE_SENSE(6/10)` 对只读盘带出 write-protect 位
  - 系统 `WRITE/WRITE6/WRITE12/WRITE16` 请求统一返回写保护错误与 `DATA PROTECT / 0x27/0x00`
- `TestApp` 的 `ct` 命令已扩展为：
  - `ct <disk-size-mb> [auto|dense|sparse] [true|false] [target]`
  - 其中 `true` 表示系统只读盘，`false` 表示读写盘
  - `help` 与命令行 usage 已同步标明这两个布尔值的语义，避免命令口径分叉
- 已把 `SCSI Features` 这一套并行兼容闸口从当前实现中删除，正式收束为“运行时兼容性只看 `VersionBe` 相等”。
- 共享协议头 [windows/shared/yumedisk_proto.h](../../windows/shared/yumedisk_proto.h) 已删除：
  - `YUMEDISK_FEATURE_FLAGS`
  - `YUMEDISK_SCSI_INFO.Features`
- `YumeDiskSCSI` 的 [control/control.c](../../windows/YumeDiskSCSI/YumeDiskSCSI/control/control.c) 已不再填充 `Features` 字段；`QueryScsiInfo` 只返回：
  - `VersionBe`
  - `AdapterSignature`
- 共享协议头进一步继续收束，已删除：
  - `YUMEDISK_KMDF_INFO.ServiceName`
  - `YUMEDISK_SCSI_INFO.ServiceName`
  - `YUMEDISK_KMDF_INFO.Reserved`
  - `YUMEDISK_SCSI_INFO.MaxTargets`
  - `YUMEDISK_SCSI_INFO.Reserved`
- `AppKernel` 的 [ak_session.c](../../windows/AppKernel/src/session/ak_session.c) 已删除：
  - `SCSI app-owned queue` 特性位检查
  - `AkOpen ready` 日志中的 `features=...` 输出
- 正式文档已经同步到当前唯一口径：
  - [windows/AppKernel/AppKernel设计文档.md](../../windows/AppKernel/AppKernel设计文档.md)
  - [windows/AppKernel/AppKernel-SDK文档.md](../../windows/AppKernel/AppKernel-SDK文档.md)
  - [docs/architecture/技术文档.md](../architecture/技术文档.md)
  - [docs/architecture/功能文档.md](../architecture/功能文档.md)
  - [docs/architecture/项目概述.md](../architecture/项目概述.md)
- 同时回看了现有兼容闸口：
  - `AppKernel` 侧当前已只保留 `KMDF.VersionBe == AppKernel.VersionBe` 与 `SCSI.VersionBe == AppKernel.VersionBe` 两个运行时兼容判断。
  - `KMDF` 侧仍保留 `QueryScsiInfo` 的 `AdapterSignature + VersionBe` 探测。
  - 其中 `VersionBe` 属于兼容判断；`AdapterSignature` 只用于确认“当前打开到的是不是我们的 miniport”，不是兼容分叉。
  - 当前没有发现额外的 `Features`、`ProtocolVersion`、双轨协议或 capability gate 继续参与运行时兼容判定。
  - `QueryKmdfInfo / QueryScsiInfo` 现在也只保留真实消费字段，不再挂无业务读取的占位信息。
- 版本规则文档也已与当前真实口径对齐：
  - 对外发行后，协议/语义不兼容变化必须升级版本。
  - 首次正式发行前继续固定使用 `0.1.0.0`，不为未发行阶段的协议收束单独滚动版本号。

## 验收

- `cmake -S windows\TestApp -B build\TestApp-mingw -G Ninja -DCMAKE_BUILD_TYPE=Debug -DCMAKE_C_COMPILER=C:/Qt/Tools/mingw1310_64/bin/gcc.exe -DCMAKE_CXX_COMPILER=C:/Qt/Tools/mingw1310_64/bin/g++.exe` 已通过。
- `cmake --build build\TestApp-mingw` 已通过。
- `cmake -S windows\client -B windows\client\cmake-build-debug -G Ninja -DCMAKE_BUILD_TYPE=Debug -DCMAKE_C_COMPILER=C:/Qt/Tools/mingw1310_64/bin/gcc.exe -DCMAKE_CXX_COMPILER=C:/Qt/Tools/mingw1310_64/bin/g++.exe` 已通过。
- `cmake --build windows\client\cmake-build-debug` 已通过。
- `cmake --build build/appkernel-mingw` 已通过。
- `cmake --build build/appkernel-msvc-vs --config Debug` 已通过。
- `msbuild windows\\YumeDiskSCSI\\YumeDiskSCSI.sln /t:Build /p:Configuration=Release /p:Platform=x64 /m` 已通过源码编译与链接；最终仍失败在环境侧：
  - `InfVerif.dll` 缺失
  - `DriverVer` 被判定为未来日期
- `msbuild windows\\YumeDiskKMDF\\YumeDiskKMDF.sln /t:Build /p:Configuration=Release /p:Platform=x64 /m` 已通过源码编译与链接；最终仍失败在环境侧：
  - `InfVerif.dll` 缺失
  - `DriverVer` 被判定为未来日期
