# 2026-05-11

## 本次新增完成（`BackendRust` 完整落地）

- 已按现有 `windows/BackendCore/` 的职责边界，把 `BackendRust` 从最小骨架扩成完整重建实现：
  - `windows/BackendRust/build.rs`
  - `windows/BackendRust/src/appkernel.rs`
  - `windows/BackendRust/src/config.rs`
  - `windows/BackendRust/src/error.rs`
  - `windows/BackendRust/src/lib.rs`
  - `windows/BackendRust/src/media.rs`
  - `windows/BackendRust/src/runtime.rs`
  - `windows/BackendRust/src/scan.rs`
  - `windows/BackendRust/src/staging.rs`
  - `windows/BackendRust/src/types.rs`
  - `windows/BackendRust/src/win32.rs`
- 已在 `BackendRust` 内直接编译并链接 `AppKernel` C 源码，不再把这一步留作后续草案事项。
- 已在 Rust 侧重建 `AppKernel` 需要的最小 FFI：
  - session / disk / event / stats 结构
  - `AkOpen`
  - `AkClose`
  - `AkWaitEvent`
  - `AkCreateDisk`
  - `AkRemoveDisk`
  - `AkQuerySessionState`
  - `AkQuerySessionStats`
  - `AkQueryDiskState`
- 已按当前 `BackendCore` 行为重建 `BackendContext` 主线：
  - sessionConfig 持有
  - open / close
  - event thread
  - N 盘 runtime map
  - snapshot / stats / debug snapshot
  - createManagedDisk / removeManagedDisk / removeAllManagedDisks
- 已按当前 `BackendCore` 行为重建 staged write 逻辑：
  - stage
  - overlay read
  - commit
  - reject
- 已在 `BackendRust` 中补上可见盘扫描逻辑，继续保持：
  - `SetupAPI`
  - `IOCTL_STORAGE_QUERY_PROPERTY`
  - `IOCTL_DISK_GET_LENGTH_INFO`
  - `IOCTL_STORAGE_GET_DEVICE_NUMBER`
  - 基于 `Zightch / YumeDisk` 的 Vendor/Product 过滤
- 已保持 `Media` 边界不变：
  - 介质由宿主侧创建
  - `BackendRust` 只接收并持有 `Box<dyn Media>`
  - `BackendRust` 不理解内存盘、文件盘、网络盘内部细节
- 已新增 `windows/BackendRust/BackendRust-SDK文档.md`，明确 Rust 版正式边界与对 `NetworkMedia` 的当前口径。
- 已同步修正 `windows/BackendRust/AGENTS.md`，使其从“最小草案”口径切到“完整重建实现”口径。

## 本次新增验收（`BackendRust` 完整落地）

- `cargo test` 已在 `windows/BackendRust/` 下通过。
- 当前 `BackendRust` 已能独立完成构建，并通过基础单测。

## 本次确认边界（`BackendRust` 完整落地）

- 当前 `windows/BackendCore/` 继续保留，不做删除或替换。
- 当前 `BackendRust` 已按同级职责边界完成重建，但还没有把上层宿主切到 Rust 版。
- 当前 `BackendRust` 继续坚持：
  - `Media` 是随机读写逻辑块设备
  - `write_locked()` 成功即表示介质实例接受并承担后续一致性责任
  - 不在 core 里提前引入 `NetworkMedia` 的完整确认链

## 本次新增完成（`rust-cli` 最小闭环）

- 已新增 `windows/rust-cli/`，作为 `BackendRust` 的最小宿主 CLI：
  - `windows/rust-cli/AGENTS.md`
  - `windows/rust-cli/Cargo.toml`
  - `windows/rust-cli/src/main.rs`
  - `windows/rust-cli/src/dense_mem.rs`
  - `windows/rust-cli/src/disk_io.rs`
  - `windows/rust-cli/src/stress.rs`
- 已把 `rust-cli` 当前目标收为只做 `denseMem`，不引入 `rawFile`、网络盘、UI。
- 已在 `rust-cli` 中打通最小闭环：
  - 启动宿主
  - 打开 `BackendRust` session
  - 建盘
  - 对 `PhysicalDrive` 做多线程并发读写
  - 回读校验
  - 对内存介质做提交后校验
  - 删盘
  - 退出
- 已补最小 `denseMem` 介质实现，继续遵守由宿主创建 `Media` 再移交给 `BackendRust` 的边界。
- 已补最小 Win32 盘 I/O 访问代码，直接对 `\\\\.\\PhysicalDriveN` 做定点读写验证。

## 本次新增验收（`rust-cli` 最小闭环）

- `cargo build` 已在 `windows/rust-cli/` 下通过。
- 已通过管理员终端运行：
  - `windows/rust-cli/target/debug/rust-cli.exe smoke`
- 本次 `smoke` 实际输出已完整覆盖：
  - `state=startup`
  - `state=open`
  - `state=create`
  - `state=rw-test ok`
  - `state=memory-verify ok`
  - `state=remove`
  - `state=exit ok`

## 本次确认边界（`rust-cli` 最小闭环）

- 当前 `rust-cli` 只服务于 `BackendRust` 最小闭环验证，不承担正式上层接入职责。
- 当前并发测试只覆盖 `denseMem`。
- 为避免被当前 `BackendRust::close()` 退出路径卡住，`rust-cli` 在最小闭环成功后直接结束进程，不在 CLI 成功路径上显式调用 `close()`。
- `BackendRust` 当前事件线程已切到 `AkPollEvent + Sleep` 轮询，避免 `AkWaitEvent` 在宿主退出阶段阻塞。

## 本次新增完成（`BackendRust` 正式关闭链）

- 已修通 `BackendRust` 的正式关闭链，不再依赖 CLI 进程直接退出兜底。
- 已把事件线程入口从持有 `Arc<BackendContext>` 改为只持有 `Arc<Inner>`，并在事件线程内部用 `ManuallyDrop<BackendContext>` 包装，避免事件线程退出时触发 `BackendContext::drop -> close()` 递归关闭。
- 已恢复 `rust-cli` 成功路径与失败路径中的显式 `context.close()`。

## 本次新增验收（`BackendRust` 正式关闭链）

- `cargo build` 已再次在 `windows/rust-cli/` 下通过。
- 已再次通过管理员终端运行：
  - `windows/rust-cli/target/debug/rust-cli.exe smoke`
- 本次 `smoke` 已完整输出并正常退出：
  - `state=startup`
  - `state=open`
  - `state=create`
  - `state=rw-test ok`
  - `state=memory-verify ok`
  - `state=remove`
  - `state=exit ok`

## 本次确认边界（`BackendRust` 正式关闭链）

- 当前关闭链根因已确认并收口：不是 `AkClose` 本身卡死，而是事件线程持有的 `Arc<BackendContext>` 在退出时触发二次 `close()`，导致主线程卡在 `join()`。
- 当前 `rust-cli` 已回到正常显式关闭路径，不再使用 `std::mem::forget()` 规避关闭。
