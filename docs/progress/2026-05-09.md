# 2026-05-09

## 已完成

- 已把 `tests/` 下复制来的旧 UIA 脚本收束为当前仓库可直接复用的 `YumeDisk client` 调试工具：
  - [tests/uia_test.ps1](../../tests/uia_test.ps1) 已从旧项目脚本改成通用单步调试脚本
  - [tests/uia_scenario.ps1](../../tests/uia_scenario.ps1) 已改成面向 `windows/client` 的场景脚本
  - [tests/uia_test_gbk.ps1](../../tests/uia_test_gbk.ps1) 已重写为 UTF-8 主脚本的 GBK 包装入口
- `uia_test.ps1` 当前已支持：
  - 顶层窗口枚举 `windows`
  - 主窗口等待 `waitwindow`
  - 主窗口信息读取 `windowinfo`
  - 主窗口关闭 `closewindow`
  - 控件树导出 `list`
  - 控件存在性检查 `exists`
  - 控件读取 `read`
  - 控件写入 `write`
  - 控件点击 `click`
  - 控件切换 `toggle`
  - 控件可用性读取 `enabled`
  - 条件等待 `wait`
- `uia_scenario.ps1` 当前已收口为壳体阶段可直接执行的场景：
  - `smoke`
  - `inspect`
  - `close_to_tray`
  - `stop_process`
  - `full_shell`
- 已补一份当前仓库专用测试调试文档：
  - [tests/README.md](../../tests/README.md)
  - 文档已明确：
    - 何时使用管理员终端 / MCP
    - 当前脚本能力和推荐用法
    - 当前 `client` 仍处壳体阶段时的验收口径
    - 后续 `client` UI 必须遵守的 `accessibleName` 测试契约
- 已明确管理员边界：
  - 只有在启动 `client.exe` 做 UIA 调试时才使用管理员 MCP
  - 平时改代码、改文档、构建仍保持普通终端，避免文件权限被管理员操作污染
- 已完成 `windows/client` 工程骨架第一步收紧，并按当前口径保持 `QWidget` 主窗口，不切到 `QMainWindow`：
  - 主窗口代码已收口到 `windows/client/widget/`
  - 目录结构已调整为：
    - `windows/client/main.cpp`
    - `windows/client/widget/widget.h`
    - `windows/client/widget/widget.cpp`
    - `windows/client/widget/widget.ui`
    - `windows/client/client_backend.h`
    - `windows/client/client_backend.cpp`
  - `client_backend` 当前只作为极薄占位层存在：
    - 已链接 `AppKernel`
    - 已可读取 `AK_VERSION_BE`
    - 当前不伪造 session / disk 真状态
  - 主窗口已不再是默认空白示例壳，已补最小可承接布局：
    - session 状态占位
    - 受管磁盘表占位
    - 建盘 / 删盘按钮占位
    - 日志面板占位
  - 已补稳定 UIA 测试契约承接位：
    - 主窗口 `yumedisk.client.main_window`
    - session 状态值 `yumedisk.session.state_value`
    - 磁盘表 `yumedisk.disk.table`
    - 建盘按钮 `yumedisk.disk.create_button`
    - 删盘按钮 `yumedisk.disk.remove_button`
    - 日志面板 `yumedisk.log.text`
  - 现有托盘常驻壳语义保持不变：
    - 关闭主窗口只隐藏
    - 托盘菜单仍保留打开与退出动作

## 验收

- `pwsh -NoLogo -NoProfile -File tests\uia_test.ps1 -Help` 已通过。
- `pwsh -NoLogo -NoProfile -File tests\uia_scenario.ps1 -List` 已通过。
- 通过管理员 MCP 执行：
  - `pwsh -NoLogo -NoProfile -File .\tests\uia_scenario.ps1 -Scenario full_shell -Launch -StopAfter`
- 壳体验证结果：
  - `client.exe` 成功启动
  - 主窗口 `Client` 成功被 UIA 定位
  - `windowinfo` 成功
  - `children` 控件枚举成功
  - `descendants` 控件枚举成功
  - 关闭主窗口后进程仍存活
  - 最后已成功停止目标进程
- `cmake -S windows\client -B windows\client\cmake-build-debug -G Ninja -DCMAKE_BUILD_TYPE=Debug -DCMAKE_C_COMPILER=C:/Qt/Tools/mingw1310_64/bin/gcc.exe -DCMAKE_CXX_COMPILER=C:/Qt/Tools/mingw1310_64/bin/g++.exe` 已通过重配。
- `cmake --build windows\client\cmake-build-debug --clean-first` 已通过。
- 通过管理员 MCP 执行：
  - `pwsh -NoLogo -NoProfile -File .\tests\uia_scenario.ps1 -Scenario full_shell -Launch -StopAfter -ExePath windows/client/cmake-build-debug/client.exe`
- 当前壳体验证结果已更新为：
  - 主窗口可被 UIA 以 `accessibleName = yumedisk.client.main_window` 直接定位
  - descendants 控件树中已能稳定看到 session / disk / log 关键占位控件
  - 关闭主窗口后进程仍存活
  - 场景脚本清理进程成功

## 已确认边界

- 当前 `windows/client` 仍是示例壳体，因此 UIA 脚本当前只验证窗口壳和关闭语义，不伪造不存在的业务控件测试。
- 真正的 session / 建盘 / 删盘 / 日志 UIA 场景，等 `client` UI 和宿主后端落地后，继续在这套脚本上扩展，不再另起一套测试工具。
- 当前这一步只完成客户端工程骨架收紧，不继续提前搬入 `TestApp` 宿主运行时代码。
- 当前 `client_backend` 只是下一步接入最小宿主后端的落脚点，不单独发展新架构层。
- `windows/client` 作用域内代码规范已正式收口：
  - 新增当时 `windows/client/AGENTS.md`
  - 已明确 Qt / C++ 采用类 Java 风格命名，类名大驼峰，方法名 / 成员变量 / 局部变量 / 参数统一小驼峰
  - 已进一步澄清：当前不是额外发明一套“区别于 Qt”的命名规则，而是直接与 Qt 框架既有风格对齐
  - 已明确目录优先按类拆分为“类名目录 + cpp/h/ui”
  - 没有明显类归属的代码再按功能模块目录拆分，例如 `utils/`、`media/`、`runtime/`
- 已完成 `windows/client` 最小宿主后端接入第一步，直接把 `TestApp` 当前需要的宿主主线收进 `client/backend/`：
  - 新增：
    - `windows/client/backend/types.h`
    - `windows/client/backend/config.h`
    - `windows/client/backend/config.cpp`
    - `windows/client/backend/media.h`
    - `windows/client/backend/media.cpp`
    - `windows/client/backend/runtime.h`
    - `windows/client/backend/runtime.cpp`
  - 已把 `Backend` 从纯占位层升级为真实宿主入口：
    - 构造时打开 `AppKernel session`
    - 启动独立事件线程承接 `AkWaitEvent`
    - 析构时执行 `RemoveAllManagedDisks -> AkClose`
  - 已把 `TestApp` 当前真正需要的宿主能力收口到 `client/backend/`：
    - `AppConfig`
    - `BackendContext`
    - `ManagedDisk`
    - dense / sparse 介质承接
    - staged write 缓存与 commit / reject 应用
    - 可见盘扫描接入共享静态库 `scan`
  - `RunCommandLoop` 未迁入，当前客户端后端不再依赖 `CLI` 命令循环才能运行
  - `windows/client/CMakeLists.txt` 已补 `AppKernel` 运行时 DLL 复制，避免 `client.exe` 启动时缺少 `libAppKernel.dll`

## 本次新增验收

- `cmake --build windows\\client\\cmake-build-debug` 已通过。
- 通过管理员 MCP 执行：
  - `pwsh -NoLogo -NoProfile -File .\\tests\\uia_scenario.ps1 -Scenario smoke -Launch -StopAfter -ExePath windows/client/cmake-build-debug/client.exe`
- 通过管理员 MCP 执行：
  - `pwsh -NoLogo -NoProfile -File .\\tests\\uia_test.ps1 -ProcessId <pid> -Action list -Scope descendants`
  - `pwsh -NoLogo -NoProfile -File .\\tests\\uia_test.ps1 -ProcessId <pid> -Name yumedisk.log.text -Action read`
- 本次验证结果：
  - `client.exe` 已能正常启动，不再弹出 `libAppKernel.dll` 缺失错误
  - 主窗口仍可被 UIA 正常定位
  - descendants 控件树中仍可稳定看到 `yumedisk.session.state_value` 与 `yumedisk.log.text`
  - `yumedisk.log.text` 已可直接读到真实后端启动日志
  - 日志已出现真实 `AkOpen` / session ready / version / config 信息

## 本次确认边界

- 当前这一步只完成“宿主后端主线接入”，不继续提前做 GUI 可调用 API 整理。
- 当前 UI 仍只是壳体展示面：
  - 建盘 / 删盘按钮仍未接真实交互
  - 受管磁盘列表仍未绑定真实快照
  - 下一步再把后端能力整理成 GUI 直接调用的方法面
- 已按“最小组件拆分目录”把 `windows/client/backend/` 再收口一轮：
  - `windows/client/backend/backend.*` 继续作为宿主总入口保留在 `backend/` 根下
  - `config` 收到 `windows/client/backend/config/`
  - `media` 收到 `windows/client/backend/media/`
  - `runtime` 收到 `windows/client/backend/runtime/`
  - `types` 收到 `windows/client/backend/types/`
  - 当前没有明显类边界、但有明确功能边界的后端代码，统一按组件子目录承接

## 本次目录验收

- `windows/client/backend/` 当前目录结构已更新为：
  - `windows/client/backend/backend.cpp`
  - `windows/client/backend/backend.h`
  - `windows/client/backend/config/config.cpp`
  - `windows/client/backend/config/config.h`
  - `windows/client/backend/media/media.cpp`
  - `windows/client/backend/media/media.h`
  - `windows/client/backend/runtime/runtime.cpp`
  - `windows/client/backend/runtime/runtime.h`
  - `windows/client/backend/types/types.h`
- `cmake --build windows\\client\\cmake-build-debug` 在目录重排后已再次通过。

## 本次新增完成

- 已在 `docs/development/development-principles.md` 的“结构重构与层次依赖原则”补充 `C / C++ / Qt` 目录组织固定口径：
  - 默认按最小组件目录组织
  - 明确类优先使用“目录 + c/cpp/h/ui”
  - 没有明显类归属但有明确功能边界的代码，按组件目录承接
- 已把 `windows/client` 当前宿主后端整理成 `UI` 可直接调用的方法面：
  - 当时 `windows/client/backend/backend.h`
  - 当时 `windows/client/backend/backend.cpp`
  - 当时 `windows/client/backend/runtime/runtime.h`
  - 当时 `windows/client/backend/runtime/runtime.cpp`
- 当前已具备的 GUI 可调用后端能力包括：
  - `querySessionState`
  - `snapshotManagedDisks`
  - `createManagedDisk`
  - `removeManagedDisk`
  - `removeAllManagedDisks`
  - `queryBackendStats`
  - `queryDebugSnapshot`
  - `logLines`
- 已补宿主后端快照结构，避免 UI 自己解释底层状态：
  - `ManagedDiskSnapshot`
  - `BackendStatsSnapshot`
  - `DebugSnapshot`

## 本次新增验收

- `cmake --build windows\\client\\cmake-build-debug` 在接口整理后已再次通过。
- 通过管理员 MCP 执行：
  - `pwsh -NoLogo -NoProfile -File .\\tests\\uia_scenario.ps1 -Scenario smoke -Launch -StopAfter -ExePath windows/client/cmake-build-debug/client.exe`
- 本次验证结果：
  - 主窗口仍可正常启动
  - UIA 仍可定位主窗口
  - 当前接口整理未破坏现有托盘壳与后端启动路径

## 本次确认边界

- 当前这一步只完成“GUI 可直接调用的后端接口整理”，还没有把主窗口控件改成真实交互面。
- 现阶段 `Widget` 仍只读取 session 文本和日志文本，没有接建盘、删盘、磁盘列表刷新。
- 下一步直接进入最小 UI 闭环，不再继续扩额外后端层级。

## 本次新增完成

- 已在临时草案中收束 `client` 后端抽 DLL 的边界：
  - 当时的 `backend dll boundary` 临时草案
- 当前临时结论已固定为：
  - DLL 项目名统一使用 `BackendCore`
  - `Media` 由宿主侧先创建，再移交给 `BackendCore`
  - `FileMedia` 不进入 `BackendCore`
  - `FileMedia` 与具体 `Media` 子类实现继续留在 `client` 宿主侧
  - `BackendCore` 不依赖 `Qt`
  - `BackendCore` 不负责 UI 展示 DTO
- 已补一条新的硬边界：
  - `BackendCore` 应完整暴露当前 `AppKernel` 已有配置项
  - 上层必须可控 session 级和 disk 级配置
  - 当前 UI 可以暂不全部开放，但 `BackendCore` 不得私自藏配置或把配置硬编码成不可控
- 当前草案中已明确纳入的 `AppKernel` 配置面至少包括：
  - session 级：
    - `HeartbeatIntervalMs`
    - `InitialEventQueueCapacity`
  - disk 级：
    - `TargetId`
    - `SectorSize`
    - `DiskSizeBytes`
    - `QueueDepth`
    - `WriteSlotBytes`
    - `ReadWorkerCount`
    - `WriteWorkerCount`
    - `AckBatchMaxRanges`
    - `ReadOnly`

## 本次确认边界

- 当前这一步只收口 `BackendCore` 临时草案边界，不修改正式文档。
- 当前这一步不修改代码，不开始拆项目。
- 下一步如果继续推进，应先把 `BackendCore` 的最小导出接口收成正式重构 todo，再进入实现。

## 本次新增完成

- 已把下一轮 `BackendCore` 抽离工作的详细执行清单正式写入：
  - `docs/tmp/todo.md`
- 新 `todo` 当前已按工作流收口为：
  - 一个明确总目标
  - 分阶段的详细子步骤
  - 当前轮边界
  - 唯一下一步
- 当前详细 `todo` 已覆盖的重建范围包括：
  - `BackendCore` 工程骨架与 DLL 目标
  - `BackendCore` 对外类型与完整配置面
  - `BackendCore` 运行时主线
  - `BackendCore` 建盘 / 删盘接口
  - `client/backendHost` 宿主适配层
  - `client` 介质实现归属重建
  - `client -> backendHost -> BackendCore` 调用链重建
  - 构建与部署链路重建
  - 最小闭环验收口径重建

## 本次确认边界

- 当前这一步只完成下一轮详细 `todo` 的编排，不修改代码，不推进第一子步骤实现。
- 当前唯一下一步已经固定为：
  - 先重建 `BackendCore` 工程骨架与编译目标
- 后续执行时仍需严格遵守单步停止规则：
  - 每轮只做一个子步骤
  - 完成即归档
  - 完成即提交
  - 更新 `todo` 后立即停下

## 本次新增完成

- 已把 `windows/client/widget/` 从展示壳推进为最小可用交互面：
  - `windows/client/widget/widget.h`
  - `windows/client/widget/widget.cpp`
  - `windows/client/widget/widget.ui`
- 主窗口当前已直接接入现有 `Backend` 方法面，形成最小闭环操作面：
  - session 状态定时刷新
  - 受管磁盘列表定时刷新
  - 日志面板定时刷新
  - 基于当前选中行启用删盘按钮
  - 建盘成功 / 删盘成功后即时刷新视图
- 已新增最小建盘对话框，并按当前 `client` 目录规则独立成最小类目录：
  - `windows/client/createDiskDialog/createDiskDialog.h`
  - `windows/client/createDiskDialog/createDiskDialog.cpp`
  - `windows/client/createDiskDialog/createDiskDialog.ui`
- 当前建盘对话框已承接最小必需参数：
  - 容量（MiB）
  - 介质模式（`auto` / `dense` / `sparse`）
  - 只读开关
  - `target id`（留空自动分配）
- 已补建盘参数的最小入口校验，并继续把业务闸口收在对话框 / `Backend` 唯一入口，不把参数拼装散到控件回调中。
- 已把新增建盘对话框接入 `windows/client/CMakeLists.txt`，保持 Qt 工程构建路径一致。
- 当前 UIA 测试契约已被真实控件承接：
  - `yumedisk.create.dialog`
  - `yumedisk.create.capacity_input`
  - `yumedisk.create.media_mode_combo`
  - `yumedisk.create.read_only_check`
  - `yumedisk.create.target_id_input`
  - `yumedisk.create.submit_button`
  - `yumedisk.create.cancel_button`
- 托盘常驻行为保持不变：
  - 关闭主窗口只隐藏
  - 托盘打开主窗口
  - 托盘退出客户端

## 本次新增验收

- `cmake --build windows\\client\\cmake-build-debug` 在接入最小交互面后已再次通过。
- 通过管理员 MCP 执行：
  - `pwsh -NoLogo -NoProfile -File .\\tests\\uia_scenario.ps1 -Scenario smoke -Launch -StopAfter -ExePath windows/client/cmake-build-debug/client.exe`
- 通过管理员 MCP 执行：
  - 启动 `windows/client/cmake-build-debug/client.exe`
  - `pwsh -NoLogo -NoProfile -File .\\tests\\uia_test.ps1 -ProcessId <pid> -Name yumedisk.disk.create_button -Action click`
  - `pwsh -NoLogo -NoProfile -File .\\tests\\uia_test.ps1 -Window yumedisk.create.dialog -ProcessName client -Action waitwindow -Timeout 5000`
  - `pwsh -NoLogo -NoProfile -File .\\tests\\uia_test.ps1 -Window yumedisk.create.dialog -ProcessName client -Name yumedisk.create.capacity_input -Action read`
  - `pwsh -NoLogo -NoProfile -File .\\tests\\uia_test.ps1 -Window yumedisk.create.dialog -ProcessName client -Name yumedisk.create.cancel_button -Action click`
- 本次验证结果：
  - 主窗口仍可正常启动并被 UIA 定位
  - `yumedisk.disk.create_button` 已可点击
  - `yumedisk.create.dialog` 已可被 UIA 定位
  - `yumedisk.create.capacity_input` 已可读取默认值 `64`
  - `yumedisk.create.cancel_button` 已可关闭建盘对话框
- 通过管理员 MCP 执行一次默认参数建盘提交路径探测：
  - `yumedisk.create.submit_button` 可点击
  - 当前环境下日志返回 `session-not-open`
  - 日志同时记录 `AkOpen` 失败，说明错误已沿 `Backend -> UI` 正常暴露

## 本次确认边界

- 当前这一步只完成“最小 UI 交互面落地”，不继续提前做 UI 美化、额外状态层或扩展配置项。
- 当前主窗口已具备最小建盘 / 删盘 / 列表 / 日志交互路径，但是否能走通真实闭环仍取决于运行环境中的 `AppKernel` / 控制设备是否可用。
- 默认建盘提交流程在当前机器上已验证到真实错误面，而不是停留在纯前端假交互；当前失败原因是运行环境中的 `AppKernel session` 未打开，不是 UI 接线缺失。

## 本次新增完成

- 已进一步收紧 `windows/client` 的命名规则：
  - 类目录名统一与类名保持一致，使用大驼峰；
  - 类文件名统一与类名保持一致，覆盖 `ui / cpp / h`；
  - 没有明显类归属的功能组件目录与文件，继续统一使用小驼峰；
  - 规则已同步写入当时 `windows/client/AGENTS.md`
- 已把当前客户端类目录与类文件统一改名到新规则：
  - `windows/client/Widget/Widget.h`
  - `windows/client/Widget/Widget.cpp`
  - `windows/client/Widget/Widget.ui`
  - `windows/client/CreateDiskDialog/CreateDiskDialog.h`
  - `windows/client/CreateDiskDialog/CreateDiskDialog.cpp`
  - `windows/client/CreateDiskDialog/CreateDiskDialog.ui`
  - `windows/client/backend/Backend.h`
  - `windows/client/backend/Backend.cpp`
- 已补一个明确的后端退出闸口：
  - `Backend` 新增 `shutdown()`
  - `Widget` 新增 `quitClient()`
  - `main.cpp` 已把 `aboutToQuit` 收口到 `backend.shutdown()`
- 已补一个稳定的主窗口显式退出入口：
  - 主窗口新增 `yumedisk.client.quit_button`
  - 退出路径不再只依赖托盘菜单或对象析构链间接触发
- 已补当前测试契约文档中的显式退出按钮约定：
  - [tests/README.md](../../tests/README.md)

## 本次新增验收

- `cmake --build windows\\client\\cmake-build-debug` 在目录与退出路径调整后已再次通过。
- 通过管理员 MCP 执行：
  - `pwsh -NoLogo -NoProfile -File .\\tests\\uia_scenario.ps1 -Scenario full_shell -Launch -StopAfter -ExePath windows/client/cmake-build-debug/client.exe`
- 壳体验证结果：
  - `smoke / inspect / close_to_tray` 全部通过
  - 主窗口关闭仍只隐藏到托盘，不直接退出
- 通过管理员 MCP 执行真实主线探测：
  - 启动 `windows/client/cmake-build-debug/client.exe`
  - 通过 `yumedisk.disk.create_button` 打开建盘对话框
  - 通过 `yumedisk.create.submit_button` 走默认参数建盘
  - 通过 descendants 控件树确认当前已经看到：
    - `target = 0`
    - `state = running`
    - `media = dense`
    - 可见盘路径
  - 通过 `yumedisk.log.text` 确认真实日志已记录：
    - `AkOpen: ready`
    - `created target=0`
    - `visiblePath=...`
- 通过管理员 MCP 执行显式退出探测：
  - 点击 `yumedisk.client.quit_button`
  - 进程成功退出

## 本次确认边界

- 当前这一步完成的是“命名规则收口 + 显式退出路径收口 + 构建与分段 UIA 验证”，不继续在本轮顺手扩额外 UI 层级。
- 当前真实主线里，`session -> create -> list -> log -> explicit quit` 已验证通过。
- 当前剩余缺口集中在“删盘验收”：
  - 通过 UIA 点击表格 `DataItem` 后，`yumedisk.disk.remove_button` 仍保持禁用；
  - 这说明下一步需要专门收束“表格选择 / 删盘触发”这一条，判断是 `UI` 选择实现问题还是当前 UIA 选择契约问题。

## 本次新增完成

- 已确认当前删盘按钮无法启用的直接原因不在 `Widget` 业务逻辑，而在 UIA 脚本对 Qt 表格 `DataItem` 的点击语义没有优先走选择模式。
- 已把 [tests/uia_test.ps1](../../tests/uia_test.ps1) 的 `click` 行为收口为：
  - 先对目标控件执行 `SetFocus`
  - 对 `DataItem / ListItem / TreeItem / TabItem` 优先走 `SelectionItemPattern.Select()`
  - 其余控件继续回退到原有 `InvokePattern / Legacy` 路径
- 已把 [tests/uia_scenario.ps1](../../tests/uia_scenario.ps1) 扩展出当前客户端专用的最小闭环场景：
  - `minimal_loop`
  - 场景固定回归：`create -> list -> remove -> quit`
- 已同步更新 [tests/README.md](../../tests/README.md)：
  - 明确 `minimal_loop` 的用途
  - 明确当前全量回归口径为 `full_shell + minimal_loop`
  - 明确表格行选择继续统一走 `click`，由脚本内部处理 Qt 表格选择语义

## 本次新增验收

- `cmake --build windows\\client\\cmake-build-debug` 已再次通过。
- 通过管理员 MCP 执行：
  - `pwsh -NoLogo -NoProfile -File .\\tests\\uia_scenario.ps1 -Scenario minimal_loop -Launch -ExePath windows/client/cmake-build-debug/client.exe`
  - `pwsh -NoLogo -NoProfile -File .\\tests\\uia_scenario.ps1 -Scenario full_shell -Launch -StopAfter -ExePath windows/client/cmake-build-debug/client.exe`
- 本次验证结果：
  - `create -> list -> remove -> quit` 最小闭环已跑通
  - UIA 选中表格行后，`yumedisk.disk.remove_button` 已能稳定变为可用
  - 点击删盘后，日志已出现 `removed target=<id>`
  - 受管磁盘行已从表格中移除
  - 点击 `yumedisk.client.quit_button` 后进程已成功退出
  - `full_shell` 现有 `smoke / inspect / close_to_tray` 回归仍全部通过

## 本次确认边界

- 本次新增改动只收口在测试契约与回归场景，不额外引入新的客户端业务层或 UI 状态层。
- 当前“删盘按钮不启用”问题已判定为测试契约问题，已通过 UIA 脚本侧最小修复收口，不继续对 `Widget` 选择逻辑做无依据改动。

## 本次新增完成

- 已把显式退出闸口从“只删盘”收口为真实关闭后端上下文：
  - 当时 `windows/client/backend/Backend.cpp`
  - `Backend::shutdown()` 现在直接走 `closeBackendContext()`
  - 退出路径不再只依赖析构阶段补做 `AkClose`
- 已把 UIA 场景脚本继续扩成显式退出清理回归：
  - [tests/uia_scenario.ps1](../../tests/uia_scenario.ps1)
  - 新增 `quit_cleanup`
  - 固定验收：`create -> quit(without manual remove) -> visible disk back to baseline -> relaunch session reopen`
- 已更新测试文档：
  - [tests/README.md](../../tests/README.md)
  - 当前全量回归口径已更新为：
    - `full_shell`
    - `minimal_loop`
    - `quit_cleanup`

## 本次新增验收

- `cmake --build windows\\client\\cmake-build-debug` 已通过。
- 通过管理员 MCP 执行：
  - `pwsh -NoLogo -NoProfile -File .\\tests\\uia_scenario.ps1 -Scenario quit_cleanup -Launch -StopAfter -ExePath windows/client/cmake-build-debug/client.exe`
  - `pwsh -NoLogo -NoProfile -File .\\tests\\uia_scenario.ps1 -Scenario minimal_loop -Launch -ExePath windows/client/cmake-build-debug/client.exe`
  - `pwsh -NoLogo -NoProfile -File .\\tests\\uia_scenario.ps1 -Scenario full_shell -Launch -StopAfter -ExePath windows/client/cmake-build-debug/client.exe`
- 本次验证结果：
  - 创建磁盘后不手动删盘，点击 `yumedisk.client.quit_button`，进程成功退出
  - 系统侧 `YumeDisk` 可见盘在退出后回落到基线
  - 再次启动 `client.exe` 后，日志重新出现 `session opened`
  - 重启后未出现 `open session failed` / `open-failed(...)`
  - 现有 `minimal_loop` 和 `full_shell` 回归仍全部通过

## 本次确认边界

- 当前“显式退出是否真实执行 `RemoveAllManagedDisks -> AkClose`”这一条已经验收通过。
- 这一步仍只收口最小闭环，不提前开始做 `raw` 文件盘 UI、文件选择器或额外配置页。
## 本次新增完成

- 已把 `raw` 文件盘接入当前客户端唯一建盘主线，不再另起第二套流程：
  - `windows/client/backend/types/types.h`
  - `windows/client/backend/Backend.h`
  - `windows/client/backend/Backend.cpp`
  - `windows/client/backend/runtime/runtime.cpp`
  - `windows/client/backend/config/config.cpp`
  - `windows/client/backend/media/media.cpp`
- 当前 `MediaMode` 已扩成统一一套：
  - `auto / dense / sparse / raw`
- 已把 `raw` 文件盘的最小后端承接收口到现有 `BackendContext -> ManagedDisk -> media` 主线：
  - `CreateDiskRequest` 新增 `rawFilePath`
  - `ManagedDisk` 新增统一文件后备介质句柄与路径字段
  - `media` 层新增 `raw` 文件打开与大小校验
  - `raw` 文件大小以现有文件真实大小为准，不再额外走容量参数
- 已保持最小语义边界：
  - `dense` 继续走内存介质
  - `sparse` 继续走临时 sparse 文件
  - `raw` 直接绑定现有文件
  - 读写主线继续复用同一套 `hostReadBytes / hostStageWrite / applyCommittedWrite`
- 已把最小 UI 参数面补到当前建盘对话框，不提前做文件选择器：
  - `windows/client/CreateDiskDialog/CreateDiskDialog.h`
  - `windows/client/CreateDiskDialog/CreateDiskDialog.cpp`
  - `windows/client/CreateDiskDialog/CreateDiskDialog.ui`
- 当前建盘对话框在 `raw` 模式下的固定路径已经收口为：
  - 选择 `media = raw`
  - 填写现有 `raw` 文件路径
  - 容量输入自动退出当前路径，不再参与提交
- 已补 `raw` 模式最小前端校验：
  - 路径不能为空
  - 必须是已存在普通文件
  - 文件大小必须大于 0
  - 文件大小必须按 `4096` 对齐
- 已把主窗口列表展示同步扩到 `raw`：
  - `windows/client/Widget/Widget.cpp`
  - 表格 `Media` 列当前已可显示 `raw`
- 已把 UIA 工具做最小增强，避免后续继续手撕下拉框操作：
  - `tests/uia_test.ps1` 新增 `select`
  - 对 `QComboBox` 先尝试 `ValuePattern`，失败后回退展开列表 + 模式选择 + 坐标点击兜底
- 已更新 UIA 测试契约文档：
  - `tests/README.md`
  - 新增 `select` 动作说明
  - 新增 `yumedisk.create.raw_file_input` 控件契约

## 本次新增验收

- `cmake --build windows\client\cmake-build-debug` 已通过。
- 通过管理员 MCP 执行真实 `raw` 最小闭环：
  - 启动 `windows/client/cmake-build-debug/client.exe`
  - 打开建盘对话框
  - 选择 `media = raw`
  - 写入现有 `raw` 文件路径 `C:\Users\0\AppData\Local\Temp\YumeDiskClientRawTests\raw-64m.bin`
  - 提交建盘
  - 验证日志出现：
    - `created target=0`
    - `media=raw`
    - `rawFile=...raw-64m.bin`
  - 验证表格行出现：
    - `target = 0`
    - `state = running`
    - `media = raw`
  - 验证系统侧可见盘数从基线 `0` 增加到 `1`
  - 验证删盘后日志出现 `removed target=0`
  - 验证系统侧可见盘回落到基线
  - 验证显式退出后进程退出
- 通过管理员 MCP 顺序回归：
  - `pwsh -NoLogo -NoProfile -File .\tests\uia_scenario.ps1 -Scenario minimal_loop -Launch -ExePath windows/client/cmake-build-debug/client.exe`
- 回归结果：
  - 现有 `dense / sparse` 默认最小闭环未退化
  - `create -> list -> remove -> quit` 仍全部通过

## 本次确认边界

- 这一步只完成把 `raw` 文件盘纳入当前唯一最小闭环，不提前扩文件选择器、历史路径记忆或额外配置页。
- 当前 `raw` 路径仍固定要求用户提供一个已存在、按 `4096` 对齐的文件路径。
- 当前 UIA 回归已具备稳定操控 `QComboBox` 的最小能力，后续优先复用 `select`，不再回退到临时手写操作。

## 本次新增完成

- 已把 `raw` 文件盘正式纳入固定 UIA 回归入口，不再只靠手工调试探测：
  - [tests/uia_scenario.ps1](../../tests/uia_scenario.ps1)
  - 新增 `raw_loop`
  - 新增 `-RawFilePath`
- 已把原有最小闭环场景收口成可复用的公共步骤，避免 `dense` / `raw` 两条测试路径继续分叉：
  - 建盘对话框打开
  - 介质选择 / 输入
  - 列表收口判断
  - 删盘与显式退出收口
- 已把 UIA 单步工具补一层点击兜底，收口 `QTableWidget` 行选择抖动：
  - [tests/uia_test.ps1](../../tests/uia_test.ps1)
  - `click` 现在在 `Invoke / Selection / LegacyIAccessible` 失败后继续回退坐标点击
- 已把测试说明更新到当前统一回归口径：
  - [tests/README.md](../../tests/README.md)
  - 当前全量顺序固定为：
    - `full_shell`
    - `minimal_loop`
    - `quit_cleanup`
    - `raw_loop`
  - 已补 `raw_loop` 默认测试文件与 `-RawFilePath` 用法

## 本次新增验收

- `pwsh -NoLogo -NoProfile -File tests\\uia_scenario.ps1 -Help` 已通过。
- `pwsh -NoLogo -NoProfile -File tests\\uia_scenario.ps1 -List` 已通过。
- 通过管理员 MCP 顺序执行并分别通过：
  - `pwsh -NoLogo -NoProfile -File .\\tests\\uia_scenario.ps1 -Scenario full_shell -Launch -StopAfter -ExePath windows/client/cmake-build-debug/client.exe`
  - `pwsh -NoLogo -NoProfile -File .\\tests\\uia_scenario.ps1 -Scenario minimal_loop -Launch -ExePath windows/client/cmake-build-debug/client.exe`
  - `pwsh -NoLogo -NoProfile -File .\\tests\\uia_scenario.ps1 -Scenario quit_cleanup -Launch -StopAfter -ExePath windows/client/cmake-build-debug/client.exe`
  - `pwsh -NoLogo -NoProfile -File .\\tests\\uia_scenario.ps1 -Scenario raw_loop -Launch -ExePath windows/client/cmake-build-debug/client.exe`
- 本次验证结果：
  - `full_shell` 继续通过，壳体 `smoke / inspect / close_to_tray` 未退化
  - `minimal_loop` 继续通过，`dense` 主线仍保持 `create -> list -> remove -> quit`
  - `quit_cleanup` 继续通过，显式退出后的可见盘与 session reopen 语义未退化
  - `raw_loop` 已固定通过，`raw` 主线已收口为：
    - `create -> list -> remove -> quit`
    - 日志包含 `media=raw`
    - 日志包含 `rawFile=C:/Users/0/AppData/Local/Temp/YumeDiskClientRawTests/raw-64m.bin`

## 本次确认边界

- 当前 `windows/client` 最小闭环已正式收口完成：
  - 用户不再依赖 `CLI`
  - `dense / sparse / raw` 已进入同一客户端主线
  - 壳体、建盘、删盘、日志、显式退出与退出清理都已有固定回归入口
- 当前测试层仍保持极简：
  - `raw_loop` 默认只使用一个 `64 MiB`、`4096` 对齐的固定 `raw` 文件
  - 当前不扩文件选择器自动化、历史路径集或额外配置页测试
- 当前总目标已经完成；下一轮如继续推进，应重新写新的 `docs/tmp/todo.md`，而不是在当前最小闭环 `todo` 上继续叠加。

## 本次新增完成

- 已把 `windows/client` 当前 `sparse` 介质从临时 sparse 文件实现收口为纯内存实现：
  - 当时 `windows/client/backend/media/media.cpp`
  - `sparse` 当前不再创建临时文件
  - `sparse` 当前与 `dense` 一样都直接落到内存介质主线
- 已把当前客户端介质命名统一改为：
  - `denseMem`
  - `sparseMem`
  - `rawFile`
- 已同步更新以下层面的介质口径：
  - 后端枚举与日志文本
  - 建盘对话框下拉选项
  - 主窗口列表 `Media` 列展示
  - UIA 场景脚本与测试说明
- 当前 `rawFile` 语义保持不变：
  - 仍要求现有文件路径
  - 仍直接绑定现有文件句柄
  - 仍通过 `rawFile=` 日志暴露路径

## 本次新增验收

- 本次未执行构建或 UIA 回归。
- 原因：用户在本轮中明确要求“接下来你不用做测试了，告诉我测试方法即可”，因此本步只完成实现收口、测试方法说明、归档与提交。

## 本次确认边界

- 这一步只收口 `sparse` 纯内存化和当前介质命名，不继续扩展新模式或改动 `rawFile` 语义。
- 当前仓库里的历史归档记录仍保留旧名称 `dense / sparse / raw`，但从本次变更起，客户端当前口径已经切到 `denseMem / sparseMem / rawFile`。

## 本次新增完成

- 已把当前客户端新结构正式收口为草案：
  - 当时的 `yumedisk client formal proposal` 临时草案
  - 已固定：
    - `BackendContext` 作为全局控制面
    - `map<targetId, DiskRuntime>` 作为 `N` 盘运行时展开
    - `StagingStore` 与 `Media` 按盘各持一份
    - `Media -> MemoryMedia / FileMedia -> RawFileMedia` 作为当前介质继承主线
- 已把结构表达拆成独立关系图，不再混画：
  - 控制链
  - 数据链
  - 继承链
  - 持有链
- 已清理 `docs/tmp/` 下干扰当前推进的旧草稿，只保留当前还在使用的文档：
  - 当时的 `yumedisk client formal proposal` 临时草案
  - [docs/tmp/todo.md](../tmp/todo.md)
- 已按新正式结构和 [docs/development/development-principles.md](../development/development-principles.md) 重写当前轮 `todo`：
  - 当前目标已切到“重建 `windows/client` 第一阶段最小闭环结构”
  - 当前子步骤已统一改成“重建 xxx”口径

## 本次新增验收

- 本次未运行构建或 `UIA` 回归。
- 原因：本步只处理结构方案归档、临时文档清理和下一轮 `todo` 重写，不涉及代码或行为变化。

## 本次确认边界

- 当前只完成正式结构草案收口、`docs/tmp` 草稿清理和下一轮 `todo` 轮换。
- 当前还没有开始按新结构重建 `windows/client` 代码。

## 本次新增完成

- 已完成 `windows/client` 结构重建 `Step 1`：重建全局控制面骨架。
- 已把 `BackendContext` 收口为当前唯一全局控制面：
  - `session` 生命周期
  - 事件泵线程
  - 日志入口
  - `map<targetId, DiskRuntime>` 持有
  - 建盘 / 删盘 / 全部删盘 / 状态查询入口
- 已把 `Backend` 收成纯 `UI` 侧调用入口，不再自己拼装第二套控制逻辑：
  - `windows/client/backend/Backend.cpp`
  - `windows/client/backend/Backend.h`
- 已把按盘持有表从命名上收口到正式方案口径：
  - `disks` -> `diskRuntimes`
  - `ManagedDisk` 主命名开始切到 `DiskRuntime`
- 已保留一层 `using ManagedDisk = DiskRuntime` 过渡别名，仅用于避免本步把单盘骨架与控制面重建混做一轮。
- 已把运行时外部散函数大幅收回到 `BackendContext` 成员方法，控制职责不再散落：
  - `open`
  - `close`
  - `querySessionStateText`
  - `snapshotLogLines`
  - `snapshotManagedDisks`
  - `queryBackendStats`
  - `queryDebugSnapshot`
  - `findFirstFreeTarget`
  - `createManagedDisk`
  - `removeManagedDisk`
  - `removeAllManagedDisks`
- 已同步把 `windows/client/AGENTS.md` 的后端主线描述收口到 `BackendContext + DiskRuntime`。

## 本次新增验收

- `cmake --build windows\\client\\cmake-build-debug` 已通过。

## 本次确认边界

- 这一步只完成全局控制面骨架重建，不继续拆 `DiskRuntime / StagingStore / Media` 的单盘内部结构。
- 当前仍保留 `ManagedDisk` 过渡别名，下一步再正式重建按盘运行时骨架并去掉历史命名残留。
- 当前未新增测试或改 UI 行为，只验证构建闭合。

## 本次新增完成

- 已完成 `windows/client` 结构重建 `Step 2`：重建按盘运行时骨架。
- 已把单盘运行时从“平铺一组全字段”收紧为按职责分组的按盘子结构：
  - `DiskMetadata`
  - `DiskQueueConfig`
  - `DiskLifecycleState`
  - `DiskMediaState`
  - `DiskStagingState`
- `DiskRuntime` 当前只作为单盘边界对象持有上述子结构，不再继续把：
  - metadata
  - queue config
  - handle lifecycle
  - media state
  - staged write state
  混在同一层平铺字段里。
- 已把后端主线同步切到新的按盘结构口径：
  - `windows/client/backend/types/types.h`
  - `windows/client/backend/runtime/runtime.cpp`
  - `windows/client/backend/media/media.h`
  - `windows/client/backend/media/media.cpp`
- 已去掉 `ManagedDisk = DiskRuntime` 过渡别名，单盘主命名正式统一到 `DiskRuntime`。
- 当前 `BackendContext` 继续只持有：
  - `AK_SESSION`
  - 事件线程
  - 日志
  - `map<targetId, DiskRuntime>`
  不再额外承接单盘内部资源字段。
- 当前单盘资源归属已明确收口为：
  - `metadata` 归 `DiskRuntime::metadata`
  - `AK_DISK*` 归 `DiskRuntime::lifecycle`
  - backing file / memory / lock 归 `DiskRuntime::media`
  - staged writes / ordinal 归 `DiskRuntime::staging`

## 本次新增验收

- `cmake --build windows\\client\\cmake-build-debug` 已通过。

## 本次确认边界

- 这一步只完成按盘运行时骨架重建，不继续提前抽 `StagingStore` 独立组件，也不提前展开 `Media / MemoryMedia / FileMedia / RawFileMedia` 继承实现。
- 当前 `DiskRuntime` 仍是单盘聚合根，但其内部资源边界已经先按正式方案收紧。
- 下一步直接进入“重建按盘暂存层”，把当前 `DiskStagingState` 继续收口成正式的按盘暂存组件。

## 本次新增完成

- 已完成 `windows/client` 结构重建 `Step 3`：重建按盘暂存层。
- 已新增独立按盘暂存组件：
  - `windows/client/backend/StagingStore/StagingStore.h`
  - `windows/client/backend/StagingStore/StagingStore.cpp`
- 已把原先散落在 `media` 里的 staged write 状态与行为收口到 `StagingStore`：
  - `stageWriteLocked`
  - `overlayReadLocked`
  - `commitLocked`
  - `rejectLocked`
  - `clearLocked`
- 当前按盘暂存真实来源已收口为：
  - `DiskRuntime::staging`
  - 不再继续维护 `DiskStagingState` 这种“只有容器没有行为”的平铺状态块
- 已把按盘提交 / 拒绝入口收口到暂存组件主线：
  - `commitDiskRuntimeStaging`
  - `rejectDiskRuntimeStaging`
- 已把以下链路改为统一走单盘暂存层：
  - `hostStageWrite`
  - `hostReadBytes` 的 overlay 叠加
  - `AkEventWriteFinalCommitted`
  - `AkEventWriteFinalRejected`
- 已把 `cleanupManagedDiskMedia` 的暂存清理也统一收回 `StagingStore::clearLocked`，不再直接在别处擦写暂存容器。
- 已同步更新以下文件以对齐新主线：
  - `windows/client/backend/types/types.h`
  - `windows/client/backend/media/media.h`
  - `windows/client/backend/media/media.cpp`
  - `windows/client/backend/runtime/runtime.cpp`
  - `windows/client/CMakeLists.txt`

## 本次新增验收

- `cmake --build windows\\client\\cmake-build-debug` 已通过。

## 本次确认边界

- 这一步只完成按盘暂存层重建，不继续提前展开 `Media / MemoryMedia / FileMedia / RawFileMedia` 的继承实现。
- 当前仍保持 `UI -> Backend -> BackendContext -> map<targetId, DiskRuntime>` 主线不变，只把单盘暂存读写链收口到唯一按盘暂存组件。
- 下一步直接进入“重建介质抽象主线”，把当前 `denseMem / sparseMem / rawFile` 的介质差异继续从后端其他组件中抽离。

## 本次新增完成

- 已完成 `windows/client` 结构重建 `Step 4`：重建介质抽象主线。
- 已按正式方案把介质继承主线落成独立类目录：
  - `windows/client/backend/media/Media/Media.h`
  - `windows/client/backend/media/MemoryMedia/MemoryMedia.h`
  - `windows/client/backend/media/MemoryMedia/MemoryMedia.cpp`
  - `windows/client/backend/media/FileMedia/FileMedia.h`
  - `windows/client/backend/media/FileMedia/FileMedia.cpp`
  - `windows/client/backend/media/RawFileMedia/RawFileMedia.h`
  - `windows/client/backend/media/RawFileMedia/RawFileMedia.cpp`
- 已把按盘介质真实来源从平铺状态块收口到单盘唯一介质实例：
  - `DiskMediaState` 不再自己持有 `memory / backingFile / backingFileIoLock`
  - `DiskMediaState` 当前只持有：
    - `lock`
    - `std::unique_ptr<Media> instance`
- 已把 `denseMem / sparseMem / rawFile` 的承接关系收口为：
  - `denseMem` -> `MemoryMedia`
  - `sparseMem` -> `MemoryMedia`
  - `rawFile` -> `RawFileMedia`
- 已把文件型公共读写能力收口到 `FileMedia`，不再把文件句柄读写散落在 `media.cpp` 里。
- 已把 `media.cpp` 收回为当前唯一的介质装配与宿主回调入口：
  - 负责 `requestedMode -> resolvedMode` 装配
  - 负责初始化 `DiskRuntime::media.instance`
  - 负责 `hostReadBytes / hostStageWrite`
  - 负责通过 `Media` 抽象进入实际介质实现
- 已保持当前控制主线不变：
  - `UI -> Backend -> BackendContext -> map<targetId, DiskRuntime>`
  - 这一步只抽离介质差异，不新增第二套控制层或状态真相。
- 已同步更新：
  - `windows/client/backend/types/types.h`
  - `windows/client/backend/media/media.cpp`
  - `windows/client/CMakeLists.txt`
  - `windows/client/backend/StagingStore/StagingStore.cpp`

## 本次新增验收

- `cmake --build windows\\client\\cmake-build-debug` 已通过。

## 本次确认边界

- 这一步只完成介质抽象主线重建，不继续提前整改 `Widget / CreateDiskDialog` 的调用边界。
- 当前 `Media` 差异已经从后端其他组件中抽离，但 `UI` 与运行时分离边界仍待下一步继续收口。
- 下一步直接进入“重建 UI 与运行时分离边界”，把 `Widget / CreateDiskDialog` 对后端的消费固定到快照与命令接口。

## 本次新增完成

- 已完成 `windows/client` 结构重建 `Step 5`：重建 `UI` 与运行时分离边界。
- 已把 `Widget` 对后端的消费收口为单一快照入口：
  - `windows/client/backend/Backend.h`
  - `windows/client/backend/Backend.cpp`
  - `Backend` 当前新增统一 `snapshot()`，一次性返回：
    - `sessionStateText`
    - `logLines`
    - `disks`
- 已把 `Widget` 从“分别读取 session / disks / logs”收口为“只消费快照 + 只发命令”：
  - `windows/client/Widget/Widget.h`
  - `windows/client/Widget/Widget.cpp`
  - 当前主窗口不再直接感知后端内部 `ManagedDiskSnapshot / MediaMode / visiblePath` 细节拼装
  - 当前表格展示文本统一由 `Backend` 整形成 `BackendManagedDiskSnapshot`
- 已把建盘请求边界回收到后端唯一闸口：
  - `windows/client/CreateDiskDialog/CreateDiskDialog.h`
  - `windows/client/CreateDiskDialog/CreateDiskDialog.cpp`
  - `CreateDiskDialog` 当前只负责采集表单文本与选项
  - `Backend::createManagedDisk()` 当前统一负责：
    - 容量解析
    - `target id` 解析
    - `rawFile` 路径与大小校验
    - `BackendCreateDiskRequest -> CreateDiskRequest` 转换
- 当前 `UI -> Backend -> BackendContext -> map<targetId, DiskRuntime>` 主控制链保持不变，没有新增 `ViewModel / Store / Facade` 一类扩展壳。
- 当前 `UI` 已进一步收口为：
  - 读：`BackendSnapshot`
  - 写：`createManagedDisk / removeManagedDisk / shutdown`

## 本次新增验收

- `cmake --build windows\\client\\cmake-build-debug` 已通过。

## 本次确认边界

- 这一步只完成 `UI` 与运行时分离边界重建，不继续在本轮顺手推进下一轮重构项。
- 当前 `Widget / CreateDiskDialog` 已不再持有第二份业务真相，也不再复制后端校验逻辑。
- 当前这轮 `windows/client` 第一阶段最小闭环结构重建目标已完成；按 `docs/development/workflow.md`，本轮应在归档、提交、清空 `todo` 后停止，等待下一轮重新写入。

## 本次新增完成

- 已完成 `BackendCore` 抽离工作的 `Step 1`：重建独立工程骨架与编译目标。
- 已新建独立项目目录：
  - `windows/BackendCore/CMakeLists.txt`
  - `windows/BackendCore/BackendCore/BackendCore.h`
  - `windows/BackendCore/config/`
  - `windows/BackendCore/runtime/`
  - `windows/BackendCore/StagingStore/`
  - `windows/BackendCore/types/`
  - `windows/BackendCore/media/`
- 已把当前不依赖 `Qt` 的最小核心目录从 `windows/client/backend/` 独立出一份到 `windows/BackendCore/`：
  - `config`
  - `runtime`
  - `StagingStore`
  - `types`
  - `media/Media`
- 已补最小过渡版 `windows/BackendCore/media/media.cpp` 与 `windows/BackendCore/media/media.h`，使 `BackendCore` 在当前轮不依赖 `FileMedia / MemoryMedia / RawFileMedia` 实现的前提下先独立闭合编译。
- 已把独立工程依赖接通到：
  - `AppKernel`
  - `scan`
- 当前 `BackendCore` 已满足本步骤目标：
  - 可以单独配置
  - 可以单独编译出 `BackendCore.dll`
  - 仍未切换 `client` 调用链

## 本次新增验收

- `cmake -S windows\\BackendCore -B windows\\BackendCore\\cmake-build-debug -G Ninja -DCMAKE_BUILD_TYPE=Debug -DCMAKE_C_COMPILER=C:/Qt/Tools/mingw1310_64/bin/gcc.exe -DCMAKE_CXX_COMPILER=C:/Qt/Tools/mingw1310_64/bin/g++.exe` 已通过。
- `cmake --build windows\\BackendCore\\cmake-build-debug --clean-first` 已通过。
- 当前产物已成功链接：
  - `AppKernel`
  - `scan`
  - `libBackendCore.dll`

## 本次确认边界

- 这一步只完成 `BackendCore` 独立工程骨架和最小非 `Qt` 核心目录抽离，不继续在本轮切换 `client -> BackendCore` 调用链。
- 当前 `BackendCore/media/media.cpp` 仍是过渡形态：只负责让独立运行时骨架先编译闭合，不承接具体 `Media` 子类创建。
- 当前仍保持既定边界不变：
  - `FileMedia` 不进入 `BackendCore`
  - 具体 `Media` 子类实现不进入 `BackendCore`
  - `Qt` / UI DTO 不进入 `BackendCore`
- 下一步直接进入“重建 `BackendCore` 对外类型与完整配置面”，把 `client` 侧输入态 / Qt DTO 与 core 运行时类型彻底切开。
