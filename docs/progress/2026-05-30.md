# 2026-05-30

## 已完成

### 1. Unit Attention / DataChanged runtime `28/00` 证据已补齐

- 本轮继续沿用本地共享 `MemoryMedia` 验证链：
  - `sm 64`
  - `ct smid=1 target=3 ro=false`
  - `ct smid=1 target=4 ro=false`
- 为避免系统侧提前消费 `pending UA`，本轮固定先将 sibling `\\\\.\\PhysicalDrive2` 离线，再对 `\\\\.\\PhysicalDrive1` 发真实 512B 原始写入。
- 最稳定样本为 `R6` 重跑：
  - `PhysicalDrive1` 写入偏移 `12288`
  - marker=`UA-DBGP-R6-DBGPRINT-20260530T0223`
  - 写入后等待 `1200ms`
  - 随后对 `PhysicalDrive2` 执行 `TEST UNIT READY x2`
- 同轮用户态 probe 结果已稳定复现：
  - 第一次 `TUR = CHECK CONDITION`
  - 第二次 `TUR = GOOD`
- 内核态已抓到 `YumeDiskSCSI` 临时验证日志：
  - `ua pending marked target=4 generation=1 source=notify_data_changed pending=true`
  - `ua returned target=4 opcode=0x00(TEST_UNIT_READY) path=pre-check sense_key=0x06 asc=0x28 ascq=0x00 scsi_status=0x02(CHECK_CONDITION)`

### 2. `28/00` 验收结论已落稳

- 以上证据已经同时覆盖：
  - sibling notify 已真实到达 `YumeDiskSCSI`
  - target `4` 的 `PendingDataChangedUa` 已被置位
  - 下一次 `TEST UNIT READY` 确实消费了一次 pending UA
  - 本次返回的正式 sense 语义为 `UNIT ATTENTION / 06 28 00`
  - 该 UA 只返回一次，随后恢复 `GOOD`
- 至此，`write committed -> sibling notify -> pending UA -> TEST UNIT READY 返回一次 28/00 -> 下一次恢复 GOOD` 这条主链已具备可归档 runtime 证据。

### 3. 临时验证日志已从驱动源码移除

- `windows/YumeDiskSCSI/YumeDiskSCSI/control/control.c` 中用于验证 `pending marked` 的临时 print 已移除。
- `windows/YumeDiskSCSI/YumeDiskSCSI/scsi/scsi.c` 中用于验证 `ua returned` 的临时 print 及其 helper 已移除。
- 验证完成后，`YumeDiskSCSI` 源码已回到无这批临时 print 的状态。

## 本次验收

- 使用 `windows/tests/scsi_ua_probe/scsi_ua_probe.exe` 对 `\\\\.\\PhysicalDrive2` 执行：
  - buffered `TEST UNIT READY x2`
- 使用管理员 raw disk 句柄对 `\\\\.\\PhysicalDrive1` 执行 512B 原始写入。
- 同轮 runtime 实测结果：
  - `probe=1 ... scsi_status=0x02(CHECK_CONDITION)`
  - `probe=2 ... scsi_status=0x00(GOOD)`
- 验证结束后已恢复环境：
  - `PhysicalDrive2` 重新上线
  - test targets 已删除
  - `smid` 已删除
  - 当前系统重新回到只剩 `PhysicalDrive0`

## 当前边界

- 本轮明确补齐的是内核态 `28/00` runtime 证据，不是用户态 `REQUEST SENSE` 明文返回。
- `\\\\.\\PhysicalDriveN` 经过 Windows disk class path 时，显式 `REQUEST SENSE` 仍可能返回 `ERROR_INVALID_PARAMETER (87)`，因此现阶段正式黑盒验收口径仍以 `TUR: CHECK CONDITION -> GOOD` 配合内核日志为准。
- 本次仅完成 runtime 证据补齐与临时日志清理；临时日志移除后的驱动重编译与替换需走常规驱动发布流程另行执行。
