# 2026-05-06

- 调整 write ACK 协议，移除 `POST_WRITE_SLOT` 输入里的 piggyback ACK 语义。
  - `windows/shared/yumedisk_proto.h` 中的 `YUMEDISK_SUBMIT_SLOT` 已收缩为纯 slot 描述符，不再混入 ACK trailer。
  - `windows/YumeDiskKMDF/YumeDiskKMDF/control/ioctl.c` 已改为强制 `POST_WRITE_SLOT` 输入 payload 为空；独立 `WRITE_ACK_BATCH` 保留为唯一 ACK 入口。
  - `windows/YumeDiskSCSI/YumeDiskSCSI/control/control.c` / `queue/queue.c` 已去掉 write slot 上的 ACK trailer 校验和消费逻辑。
- `WRITE_ACK_BATCH` 增加逐项失败返回。
  - 协议新增 `YUMEDISK_WRITE_ACK_BATCH_RESULT` 与 `YUMEDISK_WRITE_ACK_FAILURE`。
  - SCSI 处理 batch 时，不再因为单个 range 失败直接让整条控制命令失败；而是把失败的 `RangeIndex + Status` 回给 App。
  - 当前找不到 `eventId` 的 ACK range 会返回 `STATUS_NOT_FOUND`，便于后续把 cancelled/stale ACK 与正常落账区分开。
- `windows/tests/RWTestApp/main.cpp` 已切到“仅独立 flush ACK”模型：
  - write worker 不再从 `POST_WRITE_SLOT` 偷带 ACK。
  - `WRITE_ACK_BATCH` 返回中的逐项失败会被打印并计数，便于观察取消模型下的 stale/cancelled ACK。
- 本轮验证：
  - `cmake --build windows/tests/RWTestApp/cmake-build-debug --config Debug` 通过。
  - `YumeDiskSCSI`、`YumeDiskKMDF` 源码编译和链接通过；驱动项目最终失败点仍只有环境侧 `InfVerif.dll` 缺失。
- 多盘并发结构已按新原则收束到单一路径：
  - `windows/tests/RWTestApp/main.cpp` 不再为每盘启动 `read worker pool + write worker pool + ack flush thread`；现在每个磁盘只保留一个 slot engine 线程，`queueDepth` 解释为该盘独立的 read slot 深度和 write slot 深度。
  - App 侧 slot engine 会持续预投该盘自己的 read/write slots，并在同一线程内完成 read copy、`READ_ACK`、write 介质落盘和 `WRITE_ACK_BATCH` flush；不再让多盘共用一个 worker 池语义。
  - `RWTestApp` 入口已收紧 `--queue-depth <= 32`，对应单线程 `WaitForMultipleObjects` 的 `32 read + 32 write` handle 上限。
- `windows/YumeDiskSCSI/YumeDiskSCSI` 的数据面队列已从 adapter 全局状态下沉到 per-target：
  - `ReadQueueLock / WriteQueueLock`、`PostedReadSlots / PendingReads`、`PostedWriteSlots / PendingWrites` 现在都归属于 `YUME_DISK.Queue`，多盘不再共享同一组自旋锁和链表。
  - `DiskDrainReadSlots` / `DiskDrainWriteSlots`、slot submit、ACK 落账、cancel、pending cleanup 和 debug 统计都已经改成按 target 队列推进，同时保留 `drain while available`。
  - `WRITE_ACK_BATCH` 现在固定先完成 ACK 控制请求回 App，再完成因此已经满足条件的系统 WRITE SRB，消除 ACK 回包与系统 write completion 的顺序倒挂。
- 本轮验证补充：
  - `msbuild build\\verify-rwtestapp-vs2022\\RWTestApp.vcxproj /t:Build /p:Configuration=Release /p:Platform=x64 /m` 通过。
  - `msbuild windows\\YumeDiskSCSI\\YumeDiskSCSI.sln /t:Build /p:Configuration=Release /p:Platform=x64 /m` 已完成源码编译、链接、签名和打包；仍会打印环境侧 `InfVerif.dll` 缺失，但本轮改动引入的源码编译错误已清零。
