# 2026-05-06

- 调整 write ACK 协议，移除 `POST_WRITE_SLOT` 输入里的 piggyback ACK 语义。
  - `windows/shared/yumedisk_proto.h` 中的 `YUMEDISK_SUBMIT_SLOT` 已收缩为纯 slot 描述符，不再混入 ACK trailer。
  - `windows/YumeDiskKMDF/YumeDiskKMDF/control/ioctl.c` 已改为强制 `POST_WRITE_SLOT` 输入 payload 为空；独立 `WRITE_ACK_BATCH` 保留为唯一 ACK 入口。
  - `windows/YumeDiskSCSI/YumeDiskSCSI/control/control.c` / `queue/queue.c` 已去掉 write slot 上的 ACK trailer 校验和消费逻辑。
- `WRITE_ACK_BATCH` 增加逐项失败返回。
  - 协议新增 `YUMEDISK_WRITE_ACK_BATCH_RESULT` 与 `YUMEDISK_WRITE_ACK_FAILURE`。
  - SCSI 处理 batch 时，不再因为单个 range 失败直接让整条控制命令失败；而是把失败的 `RangeIndex + Status` 回给 App。
  - 当前找不到 `eventId` 的 ACK range 会返回 `STATUS_NOT_FOUND`，便于后续把 cancelled/stale ACK 与正常落账区分开。
- 当时 `windows/tests/RWTestApp/main.cpp` 已切到“仅独立 flush ACK”模型：
  - write worker 不再从 `POST_WRITE_SLOT` 偷带 ACK。
  - `WRITE_ACK_BATCH` 返回中的逐项失败会被打印并计数，便于观察取消模型下的 stale/cancelled ACK。
- 本轮验证：
  - `cmake --build windows/tests/RWTestApp/cmake-build-debug --config Debug` 通过。
  - `YumeDiskSCSI`、`YumeDiskKMDF` 源码编译和链接通过；驱动项目最终失败点仍只有环境侧 `InfVerif.dll` 缺失。
- 多盘并发结构已按新原则收束到单一路径：
  - 当时 `windows/tests/RWTestApp/main.cpp` 不再为每盘启动 `read worker pool + write worker pool + ack flush thread`；现在每个磁盘只保留一个 slot engine 线程，`queueDepth` 解释为该盘独立的 read slot 深度和 write slot 深度。
  - App 侧 slot engine 会持续预投该盘自己的 read/write slots，并在同一线程内完成 read copy、`READ_ACK`、write 介质落盘和 `WRITE_ACK_BATCH` flush；不再让多盘共用一个 worker 池语义。
  - `RWTestApp` 入口已收紧 `--queue-depth <= 32`，对应单线程 `WaitForMultipleObjects` 的 `32 read + 32 write` handle 上限。
- `windows/YumeDiskSCSI/YumeDiskSCSI` 的数据面队列已从 adapter 全局状态下沉到 per-target：
  - `ReadQueueLock / WriteQueueLock`、`PostedReadSlots / PendingReads`、`PostedWriteSlots / PendingWrites` 现在都归属于 `YUME_DISK.Queue`，多盘不再共享同一组自旋锁和链表。
  - `DiskDrainReadSlots` / `DiskDrainWriteSlots`、slot submit、ACK 落账、cancel、pending cleanup 和 debug 统计都已经改成按 target 队列推进，同时保留 `drain while available`。
  - `WRITE_ACK_BATCH` 现在固定先完成 ACK 控制请求回 App，再完成因此已经满足条件的系统 WRITE SRB，消除 ACK 回包与系统 write completion 的顺序倒挂。
- 本轮验证补充：
  - `msbuild build\\verify-rwtestapp-vs2022\\RWTestApp.vcxproj /t:Build /p:Configuration=Release /p:Platform=x64 /m` 通过。
  - `msbuild windows\\YumeDiskSCSI\\YumeDiskSCSI.sln /t:Build /p:Configuration=Release /p:Platform=x64 /m` 已完成源码编译、链接、签名和打包；仍会打印环境侧 `InfVerif.dll` 缺失，但本轮改动引入的源码编译错误已清零。
- KMDF `POST_READ_SLOT` / `POST_WRITE_SLOT` 传输已从“同步代理 miniport”切到“异步长 pending slot”：
  - `windows/YumeDiskKMDF/YumeDiskKMDF/transport/transport.c` 新增 `ControlProxySubmitSlotAsync`，改为自行 `IoAllocateIrp` 向 miniport 发 `IOCTL_SCSI_MINIPORT`，并在完成例程里再完成原始 App `WDFREQUEST`。
  - `windows/YumeDiskKMDF/YumeDiskKMDF/control/ioctl.c` 的 `POST_*_SLOT` 不再 inline complete；只有异步启动失败才立即返回错误，正常路径直接返回 `STATUS_PENDING`。
  - `windows/YumeDiskKMDF/YumeDiskKMDF/session/session.c` / `core/defs.h` 现在把 miniport `HANDLE + FILE_OBJECT + DEVICE_OBJECT` 作为同一会话资源保存，确保异步 slot IRP 的目标对象在整个 pending 生命周期内保持有效。
  - `ControlSessionRelease` 已移动到 slot 完成例程，session cleanup/watchdog 继续通过 `RemoveAllDisks + drain in-flight IO + close miniport handle` 收束关闭路径，不再提前释放长 pending slot 请求。
- 本轮编译验证：
  - `msbuild windows\\YumeDiskKMDF\\YumeDiskKMDF.sln /t:Build /p:Configuration=Release /p:Platform=x64 /m` 通过源码编译、链接、签名、打包；最终仍只有环境侧 `InfVerif.dll` 缺失。
  - `msbuild windows\\YumeDiskSCSI\\YumeDiskSCSI.sln /t:Build /p:Configuration=Release /p:Platform=x64 /m` 仍通过源码编译、链接、签名、打包；最终仍只有环境侧 `InfVerif.dll` 缺失。
  - `msbuild build\\verify-rwtestapp-vs2022\\RWTestApp.vcxproj /t:Build /p:Configuration=Release /p:Platform=x64 /m` 通过。
- 新增 `docs/troubleshooting/Windows驱动问题排查笔记.md`，把近期已确认的问题、根因和修复规则归档为长期笔记。
  - 重点归档了 `open control device failed, error=50`、`有设备无盘`、`Q2/Q8/Q32 挂死`、`1117/995 transient failure`、热路径 print 导致的性能塌陷，以及 `Ctrl+C / rm all / session cleanup` 相关清理语义。
  - `docs/README.md` 和 `docs/architecture/技术文档.md` 已补入口，后续排查优先先查这份笔记，再回看协议设计或实现细节。
- 按重建路线完成了“删回控制面骨架”这一步。
  - 当时 `windows/tests/RWTestApp/main.cpp` 已整体收缩成控制台骨架，只保留 `query / ct / rm / ls / debug / exit`、会话心跳和可见磁盘枚举；原有 slot engine、介质内存、ACK flush、压测辅助和并发数据面线程全部删除。
  - `windows/YumeDiskKMDF/YumeDiskKMDF/control/ioctl.c` 已只保留 `QueryInfo / QueryDebugState / CreateDisk / RemoveDisk / RemoveAllDisks / Heartbeat` 的活跃入口；`POST_READ_SLOT / POST_WRITE_SLOT / READ_ACK / WRITE_ACK_BATCH / CANCEL_SLOT` 现在统一返回 `STATUS_NOT_SUPPORTED`。
  - `windows/YumeDiskKMDF/YumeDiskKMDF/transport/transport.c` / `transport.h` 已删掉异步 slot transport 活跃链路，只保留 miniport 探测、同步控制命令转发、session cleanup 和 handle 生命周期所需的最小传输骨架。
  - `windows/YumeDiskSCSI/YumeDiskSCSI/control/control.c` 已改成控制面唯一入口；当时的 `QueryInfo` 仍处于临时过渡阶段，所有 slot / ack 协议命令统一返回 `STATUS_NOT_SUPPORTED`。
  - `windows/YumeDiskSCSI/YumeDiskSCSI/queue/queue.c` / `queue.h` 已重写成最小 stub：只保留 queue/debug 状态初始化、pending cleanup 空实现、读写 SRB 的 `STATUS_NOT_SUPPORTED` 返回和 debug 查询。
- 本步编译验证：
  - `msbuild windows\\YumeDiskKMDF\\YumeDiskKMDF.sln /t:Build /p:Configuration=Release /p:Platform=x64 /m` 通过源码编译、链接、签名、打包；最终仍只有环境侧 `InfVerif.dll` 缺失。
  - `msbuild windows\\YumeDiskSCSI\\YumeDiskSCSI.sln /t:Build /p:Configuration=Release /p:Platform=x64 /m` 通过源码编译、链接、签名、打包；最终仍只有环境侧 `InfVerif.dll` 缺失。
  - `msbuild build\\verify-rwtestapp-vs2022\\RWTestApp.vcxproj /t:Build /p:Configuration=Release /p:Platform=x64 /m` 通过。
- 按重建路线完成了 `KMDF` 会话状态管理这一步。
  - `windows/YumeDiskKMDF/YumeDiskKMDF/core/defs.h` 中的会话状态机已显式拆成 `Active / Locked / Closing / Closed`，不再把“cleanup 进行中”和“真正 closed”混成一个状态。
  - `windows/YumeDiskKMDF/YumeDiskKMDF/session/session.c` 现在把普通控制请求引用和未来 long-pending slot 引用拆成两套计数与零事件：
    - `InFlightRequestCount + InFlightZeroEvent`
    - `PendingSlotCount + PendingSlotZeroEvent`
  - watchdog 和 file cleanup 的收尾顺序现在统一为：
    - 先切状态为 `Locked` 或 `Closing`
    - 再下发 `RemoveAllDisks | SessionClose`
    - 再分别 drain 控制请求引用和 pending slot 引用
    - 最后关闭 miniport handle
  - `session.h` 已新增 `ControlSessionRegisterPendingSlot / ControlSessionUnregisterPendingSlot`，为后续把 `POST_READ_SLOT / POST_WRITE_SLOT` 重新接回时提供唯一的 slot 生命周期归属点；本步仍未重新启用数据面。
- 本步编译验证：
  - `msbuild windows\\YumeDiskKMDF\\YumeDiskKMDF.sln /t:Build /p:Configuration=Release /p:Platform=x64 /m` 通过源码编译、链接、签名、打包；最终仍只有环境侧 `InfVerif.dll` 缺失。
  - `msbuild windows\\YumeDiskSCSI\\YumeDiskSCSI.sln /t:Build /p:Configuration=Release /p:Platform=x64 /m` 通过；本步未引入新的源码编译错误。
  - `msbuild build\\verify-rwtestapp-vs2022\\RWTestApp.vcxproj /t:Build /p:Configuration=Release /p:Platform=x64 /m` 通过。
- 按重建路线完成了 `SCSI` per-target 单盘队列这一步。
  - `windows/YumeDiskSCSI/YumeDiskSCSI/core/defs.h` 里的 `YUME_DISK.Queue` 已补齐 per-target 队列计数和写 slot 形状状态；posted read/write slots、pending read/write requests 的唯一持有者已经下沉到每个 target 自己的队列，而不是 adapter 级共享数据面。
  - `windows/YumeDiskSCSI/YumeDiskSCSI/queue/queue.c` 已从 stub 重写成新的 miniport 侧数据面骨架：
    - `DiskQueueReadSrb / DiskQueueWriteSrb` 现在会真正按 target 入队并返回 `STATUS_PENDING`。
    - `DiskDrainReadSlots / DiskDrainWriteSlots` 按 `drain while available` 循环推进，不再退回“当前只处理一个，下次再异步碰运气”的模型。
    - `SubmitSlot / ReadAck / WriteAckBatch / CancelSlot` 已接回 miniport 侧真实处理逻辑，slot、pending I/O、ACK 落账和 pending cleanup 都只在对应 target 的读写队列里发生。
    - `RemoveDisk / RemoveAllDisks / reset / abort` 触发的 cleanup 现在会同时收掉该 target 的 posted slots 和 pending read/write SRB，不再只清控制面状态。
  - `windows/YumeDiskSCSI/YumeDiskSCSI/control/control.c` 已把数据面控制命令从统一 `STATUS_NOT_SUPPORTED` 改成真实分发：
    - `SubmitSlot` 允许长 pending，直到 slot 被消费或被取消。
    - `ReadAck` 会按 `eventId` 回填原始 READ SRB。
    - `WriteAckBatch` 会按 range/bitmap 落账，并固定为“先完成 ACK 控制请求，再完成因此满足条件的系统 WRITE SRB”。
    - `CancelSlot` 只取消尚未被消费的 posted slot，不再碰 adapter 级共享列表。
  - 当前这一层的稳定边界已经收住：
    - SCSI 侧 `READ/WRITE` 单请求失败只影响对应请求，不会把盘判死。
    - `WRITE` 拆分后的失败按整笔大 WRITE 失败收束，但不影响后续独立 WRITE。
    - 同一 target 的 posted write slot 当前收敛到单一 payload 形状；如果中途切换 slot payload 大小，miniport 直接拒绝，后续由 `App per-disk engine` 负责固定每盘 slot bytes。
    - `WRITE_ACK_BATCH` 的失败结果会先暂存，避免直接覆盖输入 ranges；这一步同时修掉了 input/output 复用同一 buffer 的覆盖风险。
- 本步编译验证：
  - `msbuild windows\\YumeDiskSCSI\\YumeDiskSCSI.sln /t:Build /p:Configuration=Release /p:Platform=x64 /m` 已通过源码编译、链接、签名和打包；最终仍只剩环境侧 `InfVerif.dll` 缺失。
  - `msbuild windows\\YumeDiskKMDF\\YumeDiskKMDF.sln /t:Build /p:Configuration=Release /p:Platform=x64 /m` 仍通过；本步没有引入新的 `KMDF` 源码编译问题。
  - `msbuild build\\verify-rwtestapp-vs2022\\RWTestApp.vcxproj /t:Build /p:Configuration=Release /p:Platform=x64 /m` 仍通过。
- 按重建路线完成了 `KMDF + App` per-disk 全并发执行这一步。
  - `windows/YumeDiskKMDF/YumeDiskKMDF/transport/transport.c` / `transport.h` 已重新接回唯一的数据面 transport：`ControlProxySubmitSlotAsync` 负责为 `POST_READ_SLOT` / `POST_WRITE_SLOT` 分配独立 miniport IRP，并把 slot 生命周期统一挂到 `session` 的 pending-slot 引用计数上；控制面小命令仍保留同步 `ControlProxyCommand`。
  - `windows/YumeDiskKMDF/YumeDiskKMDF/control/ioctl.c` 已把 `POST_READ_SLOT`、`POST_WRITE_SLOT`、`READ_ACK`、`WRITE_ACK_BATCH`、`CANCEL_SLOT` 从骨架重新接回真实转发：
    - `POST_*_SLOT` 现在只负责边界校验、direct-IO buffer 取得和异步 submit，不再在 WDF 队列线程里同步等待 miniport。
    - `READ_ACK` 现在固定通过 `METHOD_OUT_DIRECT` output buffer 传递 read payload，并由 `KMDF` 在边界处把 direct buffer 地址写入 `KernelVa` 后转发给 miniport。
    - `WRITE_ACK_BATCH` / `CANCEL_SLOT` 的 payload 约束已经集中收口到 `KMDF` 入口校验，不再把非法 batch / stale cancel 放进数据面核心。
  - `windows/YumeDiskSCSI/YumeDiskSCSI/control/control.c` 的 `QueryInfo` 当时已重新接回当前数据面能力；后续正式口径已经继续收束为“兼容性只看 `VersionBe`，不再依赖 `Features` 字段”。
  - 当时 `windows/tests/RWTestApp/main.cpp` 已从控制台骨架重建为新的 per-disk slot engine：
    - 每个磁盘只保留一个 slot engine 线程，不再恢复全局共享 worker pool。
    - 每盘独立持有 `queueDepth` 对应的 read slot 深度和 write slot 深度，不再跨盘共享队列深度。
    - 每盘固定自己的 `writeSlotBytes` 和内存介质，slot engine 在同一线程内完成预投 slot、read copy、`READ_ACK`、write apply、`WRITE_ACK_BATCH` flush 和调试统计。
    - 交互入口已收束到当前唯一闭环：`query / ct / rm / rm all / ls / stats / debug / exit`。
  - 当前稳定边界：
    - 只保留一条数据面协议链路：`POST_READ_SLOT -> READ_ACK` 与 `POST_WRITE_SLOT -> WRITE_ACK_BATCH`。
    - 不恢复旧 `WAIT_EVENT` inline data、piggyback ACK、全局共享队列深度或旧兼容 payload。
    - App 退出和 `rm/rm all` 当前统一先发控制命令收口，再停 per-disk slot engine；多盘状态来源仍然只有 `session + per-target queue + per-disk engine` 三层。
- 本步编译验证：
  - `msbuild windows\\YumeDiskKMDF\\YumeDiskKMDF.sln /t:Build /p:Configuration=Release /p:Platform=x64 /m` 通过源码编译、链接、签名和打包；最终失败点仍只有环境侧 `InfVerif.dll` 缺失。
  - `msbuild windows\\YumeDiskSCSI\\YumeDiskSCSI.sln /t:Build /p:Configuration=Release /p:Platform=x64 /m` 通过源码编译、链接、签名和打包；最终失败点仍只有环境侧 `InfVerif.dll` 缺失。
  - `msbuild build\\verify-rwtestapp-vs2022\\RWTestApp.vcxproj /t:Build /p:Configuration=Release /p:Platform=x64 /m` 通过。
- 运行验证确认并提交了“有设备无盘”最终修复，提交为 `8d969bf 修复有设备无盘问题`。
  - 最终定位信号是：`ct` 后 SCSI 已经接收并完成首个 `LBA 0 / 4KB` probe read slot，KMDF 也能打印 slot completion，但 App 侧 `backend_read_slot_completions=0 / backend_read_ack_commands=0`，导致系统枚举读没有形成 `READ_ACK` 闭环。
  - 关键根因不是 SCSI 单盘队列，也不是 `completeInfo=0`，而是 `KMDF POST_*_SLOT` 下发 miniport 时仍可能阻塞在 WDF 请求处理线程内；App per-disk slot engine 因此卡在 `DeviceIoControl(POST_READ_SLOT)` 提交阶段，没机会进入 overlapped wait/consume completion。
  - 修复边界已收束为：原始 `POST_READ_SLOT / POST_WRITE_SLOT` WDFREQUEST 先进入 long-pending，实际 `IoCallDriver(IOCTL_SCSI_MINIPORT)` 放到 work item 中执行；miniport IRP 的 completion routine 和 work item 通过单一 completion state 竞争收尾，并由 completion routine 路径完成原始 App request、释放 IRP/work item、注销 pending slot 引用。
  - App 侧新增 `posting_read_slots / posting_write_slots` 调试计数，后续遇到 `visible_path=<pending-enumeration>` 时可以直接区分“slot 已投递但没有完成”和“线程仍卡在 POST_* 提交调用内部”。
  - `docs/troubleshooting/Windows驱动问题排查笔记.md` 已同步补充该问题的最终根因、判读方法和注意事项。
- 修复了“有设备无盘”场景里 probe read 的 slot completion 丢失问题。
  - 复现场景是：`ct` 后 `SCSI` 已经出现 `pending_reads=1 / pending_reads_issued=1`，但 App 侧 `backend_read_slot_completions=0 / backend_read_ack_commands=0`，说明系统首个枚举读请求已经发到 miniport，但 `POST_READ_SLOT` 的 completion 没有回到 App，枚举就卡死在 probe read。
  - 根因在 `windows/YumeDiskKMDF/YumeDiskKMDF/transport/transport.c`：上一版把 `SubmitSlot` 异步 IRP 改成了手工 `IoAllocateIrp` 路径，结果在当前 storport/miniport 场景下，slot IRP 无法稳定触发回 App 的 completion。
  - 这一步已把 `SubmitSlot` 重新收回到此前能真实跑通 completion 的 `IoBuildDeviceIoControlRequest` 路径，同时保留新的竞态收口规则：
    - completion routine 始终负责正常完成原始 `WDFREQUEST`
    - caller 只在 `IoCallDriver` 非 `STATUS_PENDING` 且 completion routine 明确没有被调用时兜底失败返回
    - 不再回到会吞掉提前 completion 的旧 `DispatchState/CompletedInline` pending 竞态
  - 预期运行时变化：
    - `ct` 后很快就应该看到 `backend_read_slot_completions` 和 `backend_read_ack_commands` 增长
    - `debug_scsi` 中 `read_acks_applied_total`、`read_completed` 会随之增长
    - 盘枚举不应再长期停在 `visible_path=<pending-enumeration>` 且 `visible_disk_count=0`
- 本步编译验证：
  - `msbuild windows\\YumeDiskKMDF\\YumeDiskKMDF.sln /t:Build /p:Configuration=Release /p:Platform=x64 /m` 通过源码编译、链接、签名和打包；最终失败点仍只有环境侧 `InfVerif.dll` 缺失。
  - `msbuild windows\\YumeDiskSCSI\\YumeDiskSCSI.sln /t:Build /p:Configuration=Release /p:Platform=x64 /m` 通过源码编译、链接、签名和打包；最终失败点仍只有环境侧 `InfVerif.dll` 缺失。
  - `msbuild build\\verify-rwtestapp-vs2022\\RWTestApp.vcxproj /t:Build /p:Configuration=Release /p:Platform=x64 /m` 通过。
- 按重建路线完成了“最终链路的退出/取消收口”这一步。
  - `windows/YumeDiskKMDF/YumeDiskKMDF/transport/transport.c` 已删除上一版 `IoBuildDeviceIoControlRequest + DispatchState/CompletedInline` 双状态收尾逻辑，改回“手工分配 IRP + completion routine 唯一收尾”模型。
  - 这一改动直接消除了 `SubmitSlot` 在 `IoCallDriver` 返回 `STATUS_PENDING` 之前就提前完成时，`KMDF` 把完成事件吞掉、原始 App 请求永远不完成的竞态窗口；这正是 `rm all / ctrl+c / app exit` 最容易卡住的链路。
  - `ControlSubmitSlotCompletionRoutine` 现在固定负责：
    - 解析 miniport 返回状态并完成原始 `WDFREQUEST`
    - `ControlSessionUnregisterPendingSlot`
    - `ControlSessionRelease`
    - `IoFreeIrp`
    - 不再把“谁负责最终完成请求”分散到 completion routine 和 `IoCallDriver` 返回路径两边。
  - 当时 `windows/tests/RWTestApp/main.cpp` 已补齐两个退出链路细节：
    - `BeginAsyncIoControl` 不再重复读取 `GetLastError()`，避免异步失败码被二次读取打乱。
    - `POST_READ_SLOT` / `POST_WRITE_SLOT` 在停机阶段如果只是撞上预期中的 `stop/session close`，不再先记一笔 `commandFailures`，避免 `rm all / exit / ctrl+c` 期间把正常停机误记成异常失败。
  - 当前这一步的收束边界：
    - 数据面仍然只有 `POST_READ_SLOT -> READ_ACK` 和 `POST_WRITE_SLOT -> WRITE_ACK_BATCH` 两条路径。
    - `rm all / ctrl+c / app exit` 的收尾不再依赖“`IoCallDriver` 返回后再补完成”的竞态状态机，只保留 miniport cleanup + completion routine 唯一收口。
    - 这一步我只做了编译验证，没有代替用户跑 VM 里的功能/压测；运行时验证需要下一轮按压测步骤人工确认。
- 本步编译验证：
  - `msbuild windows\\YumeDiskKMDF\\YumeDiskKMDF.sln /t:Build /p:Configuration=Release /p:Platform=x64 /m` 通过源码编译、链接、签名和打包；最终失败点仍只有环境侧 `InfVerif.dll` 缺失。
  - `msbuild windows\\YumeDiskSCSI\\YumeDiskSCSI.sln /t:Build /p:Configuration=Release /p:Platform=x64 /m` 通过源码编译、链接、签名和打包；最终失败点仍只有环境侧 `InfVerif.dll` 缺失。
  - `msbuild build\\verify-rwtestapp-vs2022\\RWTestApp.vcxproj /t:Build /p:Configuration=Release /p:Platform=x64 /m` 通过。
