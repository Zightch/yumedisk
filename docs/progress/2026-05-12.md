# 2026-05-12

## 本次新增完成（`tauri-client` Step 1）

- 已完成 `tauri-client` 当前轮 `Step 1`：重建顶层两态页面骨架与最小 `session` 初始化链。
- 已把前端模板页替换为正式两态结构：
  - `windows/tauri-client/src/app/App.vue`
  - `windows/tauri-client/src/pages/init/InitPage.vue`
  - `windows/tauri-client/src/pages/home/HomePage.vue`
- 已按 `windows/tauri-client/AGENTS.md` 收出前端标准目录骨架：
  - `src/app`
  - `src/pages`
  - `src/widgets`
  - `src/entities`
  - `src/shared`
- 已接入 `Element Plus`，当前阶段只使用其现成布局和基础组件，不额外建立自定义样式体系。
- 已把主页收成概念稿同层级的最小结构：
  - 顶部标题区
  - 设置入口占位
  - 会话状态展示
  - 添加按钮占位
  - 磁盘列表面板
- 已在 `src-tauri` 建立最小宿主桥接骨架：
  - `windows/tauri-client/src-tauri/src/state/client_state.rs`
  - `windows/tauri-client/src-tauri/src/backend/session_service.rs`
  - `windows/tauri-client/src-tauri/src/commands/session.rs`
  - `windows/tauri-client/src-tauri/src/api_error.rs`
- 已把 `BackendRust` 作为 `tauri-client` 宿主依赖接入，并打通：
  - 前端 `initializeClient()`
  - Tauri command `initialize_client`
  - Rust 宿主 `BackendContext::open()`
  - 初始化成功后自动切到主页
- 已补最小错误返回结构，初始化失败时前端会停留在初始化页并展示错误，不会退回模板页或静默失败。

## 本次新增验收（`tauri-client` Step 1）

- `pnpm build` 已在 `windows/tauri-client/` 下通过。
- `cargo build` 已在 `windows/tauri-client/src-tauri/` 下通过。
- `pnpm tauri build --debug --no-bundle` 已在 `windows/tauri-client/` 下通过。

## 本次确认边界（`tauri-client` Step 1）

- 当前只完成初始化页 → 主页的最小主路径，不继续展开创建盘、连接盘、配置持久化。
- 当前主页仍然是严格最小骨架：
  - 设置按钮仅保留入口占位
  - 添加按钮仅保留入口占位
  - 磁盘列表仍为静态空状态
- 当前没有引入任何自定义主题、动画、QSS 对应体系或概念稿视觉细节复刻。
- 当前 `src-tauri` 只开放 `session` 初始化命令，`disk` / `config` command 留到下一步。

## 本次新增完成（`tauri-client` Step 2）

- 已完成 `tauri-client` 当前轮 `Step 2`：重建严格按概念页收口的主页 `Element` 骨架。
- 已把主页骨架收束为概念页当前阶段允许的唯一结构：
  - 顶部品牌区：`YumeDisk`、设置入口占位
  - 顶部工具区：会话状态、添加按钮
  - 主体区：磁盘列表面板
  - 列表空态：空列表提示
  - 添加类型气泡：内存盘 / 文件盘 / 网络盘占位
- 已把添加类型入口收成 `Element Plus` 的 `Popover` 占位，不提前接入创建流程，也不额外扩展概念稿之外的入口。
- 已把磁盘列表面板补成概念稿同层级信息结构：
  - `磁盘列表` 标题
  - 数量标签
  - `自动连接` 统计
  - 面板内部滚动容器
- 已把主页布局补齐为桌面容器结构：
  - 顶部工具区单独成段
  - 主体列表区域独立留白
  - 列表内部滚动，页面不堆叠多余块
- 已保持当前阶段边界不变：
  - 不引入自定义主题系统
  - 不补概念稿视觉微调
  - 不接创建盘逻辑
  - 不接列表真状态

## 本次新增验收（`tauri-client` Step 2）

- `pnpm build` 已在 `windows/tauri-client/` 下通过。

## 本次确认边界（`tauri-client` Step 2）

- 当前主页仍然只是结构骨架，不包含真实磁盘卡片数据。
- 当前添加气泡只用于固定入口和布局锚点，不承接任何创建行为。
- 当前设置按钮仍然只是入口占位，不进入设置页流程。
- 当前仅使用 `Element Plus` 现成布局、按钮、标签、弹层、滚动容器和空态组件，没有建立自定义 `CSS` 体系。
- 当前下一步应转入宿主桥接最小命令面补齐，开始收 `disk` / `config` 的前后端接口边界。

## 本次新增完成（`tauri-client` Step 3）

- 已完成 `tauri-client` 当前轮 `Step 3` 的最小桥接骨架收口。
- 已在 `src-tauri` 建立 `config` / `disk` 命令模块：
  - `windows/tauri-client/src-tauri/src/commands/config.rs`
  - `windows/tauri-client/src-tauri/src/commands/disk.rs`
- 已在 Rust 宿主侧建立对应薄服务层：
  - `windows/tauri-client/src-tauri/src/backend/config_service.rs`
  - `windows/tauri-client/src-tauri/src/backend/disk_service.rs`
- 已把 Tauri 命令面扩展为当前最小可文档化集合：
  - `initialize_client`
  - `query_backend_defaults`
  - `query_session_config`
  - `query_managed_disks`
- 已明确当前桥接边界：
  - `query_backend_defaults` 只暴露 `BackendRust` 默认配置模板和 target 范围
  - `query_session_config` 只返回当前宿主持有的 session 配置快照
  - `query_managed_disks` 只返回 backend 当前已管理磁盘快照
- 已保持 `src-tauri` 为薄桥接：
  - 不在 command 层复制 `BackendRust` 内部逻辑
  - 只做 DTO 映射和最小服务分发
  - 当前不提前接入 `Media` 创建、建盘、删盘、配置持久化

## 本次新增验收（`tauri-client` Step 3）

- `cargo build` 已在 `windows/tauri-client/src-tauri/` 下通过。

## 本次确认边界（`tauri-client` Step 3）

- 当前 `disk` / `config` 命令仍然只读，不承担创建盘、删除盘、连接盘、断开盘行为。
- 当前 `query_backend_defaults` 暴露的是 backend 默认值模板，不等于客户端最终配置文件结构。
- 当前 `query_managed_disks` 读取的是 `BackendRust` 已管理盘集合，不包含未来由 `tauri` 宿主持有但尚未连接的 `Media` 配置态。
- 当前桥接层尚未建立“配置态盘列表 + 运行态连接状态”的组合快照，这应在下一步单独收口。

## 本次新增完成（`tauri-client` Step 4）

- 已完成 `tauri-client` 当前轮 `Step 4`：重建“配置态磁盘 / 运行态连接磁盘 / 主页展示 DTO”三层边界。
- 已在 `src-tauri` 建立宿主侧盘状态容器：
  - `windows/tauri-client/src-tauri/src/state/disk_store.rs`
  - 当前明确拆分为：
    - 配置态盘记录 `ConfigDiskRecord`
    - 运行态连接记录 `ConnectedDiskRecord`
    - 介质配置 `DiskMediaConfig`
- 已在 Rust 宿主侧建立主页列表组合快照服务：
  - `windows/tauri-client/src-tauri/src/backend/disk_service.rs`
  - 当前列表快照来源明确为：
    - 宿主持有的配置态盘列表
    - 宿主持有的连接关系
    - `BackendRust` 当前运行态已管理盘快照
- 已在 Tauri command 层新增主页列表最小只读接口：
  - `windows/tauri-client/src-tauri/src/commands/disk.rs`
  - 新增命令：
    - `query_home_disk_list`
- 已把主页列表返回结构收成面向 UI 的 DTO，而不是直接把 `BackendRust` 运行态结构暴露给前端。
- 已在前端建立磁盘领域模型与展示映射：
  - `windows/tauri-client/src/entities/disk/model.ts`
  - `windows/tauri-client/src/entities/disk/presenter.ts`
- 已在前端补最小磁盘列表读取接口：
  - `windows/tauri-client/src/shared/api/diskClient.ts`
- 已把主页和列表面板改为消费宿主快照：
  - `windows/tauri-client/src/pages/home/HomePage.vue`
  - `windows/tauri-client/src/widgets/DiskListPanel/DiskListPanel.vue`
  - 当前主页会在进入后主动读取宿主快照，并按“名称 / 介质类型 / 容量 / 文件路径 / 已连接状态”进行最小展示。
- 当前空列表仍保持空态，后续创建流程可直接落到这套真状态与展示映射之上。

## 本次新增验收（`tauri-client` Step 4）

- `pnpm build` 已在 `windows/tauri-client/` 下通过。
- `cargo build` 已在 `windows/tauri-client/src-tauri/` 下通过。

## 本次确认边界（`tauri-client` Step 4）

- 当前 `disk_store` 只是宿主真状态骨架，尚未接入真实创建、删除、连接、断开写路径。
- 当前主页列表接口仍然只读，不会修改宿主持有盘配置，也不会触发 `BackendRust` 建盘。
- 当前列表卡片只展示概念页本阶段必须信息，不扩展调试字段和系统可见路径块。
- 当前配置态与运行态关系已经拆开，但尚未持久化到家目录 `.yumedisk`。
- 当前下一步应转入内存盘创建流程，把“添加按钮 -> 内存盘对话框 -> 宿主创建配置态记录并刷新列表”这条主路径补齐。

## 本次新增完成（`tauri-client` Step 5）

- 已完成 `tauri-client` 当前轮 `Step 5`：重建内存盘创建最小闭环。
- 已在 `src-tauri` 宿主侧补内存介质实现：
  - `windows/tauri-client/src-tauri/src/backend/memory_media.rs`
  - 当前提供：
    - `DenseMemoryMedia`
    - `SparseMemoryMedia`
- 已在宿主盘状态容器中补未连接 `Media` 长期持有骨架：
  - `windows/tauri-client/src-tauri/src/state/disk_store.rs`
  - 当前创建盘后即生成 `diskId`、保存配置态记录，并把未连接 `Media` 实例保存在宿主内存中。
- 已在宿主服务层补内存盘创建逻辑：
  - `windows/tauri-client/src-tauri/src/backend/disk_service.rs`
  - 当前收口为：
    - 名称校验
    - 容量 MiB 校验
    - `auto` 创建策略解析
    - `denseMem` / `sparseMem` 真实介质实例创建
- 已沿用现有 Qt 客户端约定，把 `auto` 解析阈值收为：
  - `<= 1 GiB` 解析为 `denseMem`
  - `> 1 GiB` 解析为 `sparseMem`
- 已在 Tauri command 层新增内存盘创建命令：
  - `windows/tauri-client/src-tauri/src/commands/disk.rs`
  - 新命令：
    - `create_memory_disk`
- 已在前端补内存盘创建请求模型和 API：
  - `windows/tauri-client/src/entities/disk/model.ts`
  - `windows/tauri-client/src/shared/api/diskClient.ts`
- 已在前端补最小内存盘创建对话框：
  - `windows/tauri-client/src/features/createMemoryDisk/CreateMemoryDiskDialog.vue`
  - 当前字段严格收口为：
    - 名称
    - 容量（MiB）
    - 介质选择（自动 / 稠密 / 稀疏）
    - 启动自动连接
- 已把主页顶部添加按钮接入内存盘创建链：
  - `windows/tauri-client/src/widgets/AppHeader/AppHeader.vue`
  - `windows/tauri-client/src/pages/home/HomePage.vue`
  - 当前路径已打通为：
    - 添加按钮
    - 类型气泡
    - 选择“内存盘”
    - 打开创建对话框
    - 创建成功
    - 刷新主页列表

## 本次新增验收（`tauri-client` Step 5）

- `pnpm build` 已在 `windows/tauri-client/` 下通过。
- `cargo build` 已在 `windows/tauri-client/src-tauri/` 下通过。

## 本次确认边界（`tauri-client` Step 5）

- 当前只完成内存盘创建，不接文件盘创建。
- 当前创建成功只代表宿主配置态记录和未连接 `Media` 实例建立成功，不代表系统可见盘已经创建。
- 当前 `auto` 只存在于创建请求中，保存到列表前已解析为最终 `denseMem` 或 `sparseMem`。
- 当前尚未接入配置持久化，因此重启后新建内存盘列表不会恢复。
- 当前尚未接入连接 / 断开，因此创建后列表状态仍固定为未连接。
- 当前下一步应转入文件盘创建流程，把“添加按钮 -> 文件盘对话框 -> 选择现有 RAW 文件 -> 宿主创建配置态记录 -> 主页列表刷新”这条主路径补齐。

## 本次新增完成（`tauri-client` Step 6）

- 已完成 `tauri-client` 当前轮 `Step 6`：重建文件盘创建最小闭环。
- 已在 `src-tauri` 宿主侧补文件介质实现：
  - `windows/tauri-client/src-tauri/src/backend/raw_file_media.rs`
  - 当前提供 `RawFileMedia`，只承接已存在 `RAW` 文件。
- 已在宿主服务层补文件盘创建逻辑：
  - `windows/tauri-client/src-tauri/src/backend/disk_service.rs`
  - 当前收口为：
    - 名称校验
    - 文件路径非空校验
    - 文件必须已存在校验
    - 打开 `RAW` 文件并读取容量
    - 宿主创建配置态记录并立即持有未连接 `Media`
- 已把文件盘只支持“选择现有文件”的边界落到实现：
  - 当前不创建新文件
  - 当前不接 `RAW` 之外的文件格式
- 已在 Tauri command 层新增文件盘相关命令：
  - `windows/tauri-client/src-tauri/src/commands/disk.rs`
  - 新增命令：
    - `pick_raw_file_path`
    - `create_file_disk`
- 已在前端补文件盘创建请求模型和 API：
  - `windows/tauri-client/src/entities/disk/model.ts`
  - `windows/tauri-client/src/shared/api/diskClient.ts`
- 已在前端补最小文件盘创建对话框：
  - `windows/tauri-client/src/features/createFileDisk/CreateFileDiskDialog.vue`
  - 当前字段严格收口为：
    - 名称
    - 现有 `RAW` 文件路径
    - 启动自动连接
- 已把主页顶部添加按钮接入文件盘创建链：
  - `windows/tauri-client/src/widgets/AppHeader/AppHeader.vue`
  - `windows/tauri-client/src/pages/home/HomePage.vue`
  - 当前路径已打通为：
    - 添加按钮
    - 类型气泡
    - 选择“文件盘”
    - 打开文件盘创建对话框
    - 浏览选择现有 `RAW` 文件
    - 创建成功
    - 刷新主页列表
- 已把文件盘只读属性收为宿主推断：
  - 优先尝试读写打开
  - 失败后回退为只读打开
  - 列表中的 `readOnly` 状态与宿主真实打开结果保持一致

## 本次新增验收（`tauri-client` Step 6）

- `pnpm build` 已在 `windows/tauri-client/` 下通过。
- `cargo build` 已在 `windows/tauri-client/src-tauri/` 下通过。

## 本次确认边界（`tauri-client` Step 6）

- 当前文件盘只支持“选择现有 `RAW` 文件”，不支持“创建文件”分支。
- 当前文件选择由宿主侧打开原生文件选择框，不下沉为前端自管路径输入流程。
- 当前创建成功只代表宿主配置态记录和未连接 `Media` 实例建立成功，不代表系统可见盘已经创建。
- 当前尚未接入配置持久化，因此重启后新建文件盘列表不会恢复。
- 当前尚未接入连接 / 断开，因此创建后列表状态仍固定为未连接。
- 当前下一步应转入盘配置持久化，把宿主配置态盘列表与客户端最小配置保存到家目录 `.yumedisk`。

## 本次新增完成（`tauri-client` Step 7）

- 已完成 `tauri-client` 当前轮 `Step 7`：重建盘配置持久化最小闭环。
- 已在 `src-tauri` 状态层补客户端配置模型与家目录落盘能力：
  - `windows/tauri-client/src-tauri/src/state/client_config.rs`
  - 配置固定保存到家目录 `.yumedisk/client.json`
- 已把当前最小持久化内容收口为：
  - `sessionConfig`
    - `heartbeatIntervalMs`
    - `initialEventQueueCapacity`
  - `disks`
    - `diskId`
    - `diskName`
    - `autoConnect`
    - `media`
      - 内存盘：最终 `denseMem` / `sparseMem` 与 `capacityBytes`
      - 文件盘：`rawFile` 与 `filePath`
- 已明确配置语义只保存“希望恢复什么”，不保存运行态连接结果：
  - 不保存 connected / online / targetId
  - 不保存系统可见盘路径
  - 不保存宿主持有中的 `Media` 实例本体
- 已在宿主侧补配置恢复服务：
  - `windows/tauri-client/src-tauri/src/backend/persistence_service.rs`
  - 初始化时会：
    - 读取 `.yumedisk/client.json`
    - 恢复 `sessionConfig`
    - 恢复配置态盘列表
    - 立即在宿主侧重建并持有未连接 `Media` 实例
- 已把内存盘恢复语义收为“重建空介质”：
  - 按已保存最终类型 `denseMem` / `sparseMem` 重建
  - 不恢复上次进程中的内存数据
- 已把文件盘恢复语义收为“重新打开现有 `RAW` 文件”：
  - 若文件存在且可打开，则恢复成功
  - 若文件不存在或无法打开，则初始化失败并返回明确错误
- 已把创建链接入即时写回：
  - `windows/tauri-client/src-tauri/src/commands/disk.rs`
  - 当前在创建内存盘 / 文件盘成功后，立即写回 `.yumedisk/client.json`
  - 若写回失败，则回滚刚插入的配置态盘记录，不留下脏状态
- 已把初始化链接入配置恢复：
  - `windows/tauri-client/src-tauri/src/backend/session_service.rs`
  - 当前初始化路径已收为：
    - 先关闭现有 backend session
    - 加载 `.yumedisk/client.json`
    - 恢复 `sessionConfig`
    - 打开 session
    - 恢复宿主配置态盘列表

## 本次新增验收（`tauri-client` Step 7）

- `cargo build` 已在 `windows/tauri-client/src-tauri/` 下通过。
- `pnpm build` 已在 `windows/tauri-client/` 下通过。

## 本次确认边界（`tauri-client` Step 7）

- 当前只完成配置态盘列表与最小 session 配置持久化，不接主题、恢复策略、默认创建行为等其他配置项。
- 当前启动恢复只恢复宿主配置态盘列表，不执行自动连接。
- 当前若配置中的文件盘路径失效，初始化直接失败并停留在初始化页，这一轮先不补“跳过坏盘继续启动”策略。
- 当前内存盘恢复只重建空介质，不恢复上次进程中的内存数据。
- 当前下一步应转入手动连接 / 断开主路径，把“宿主持有 `Media` -> 手动连接时移交给 `BackendRust` -> 断开时再接回宿主”这条链补齐。

## 本次新增完成（`tauri-client` Step 8）

- 已完成 `tauri-client` 当前轮 `Step 8`：重建手动连接 / 断开主路径。
- 已在 `BackendRust` 补齐可回收介质的运行时接口：
  - `windows/BackendRust/src/runtime.rs`
  - 新增 `try_create_managed_disk(...) -> Result<u32, Box<dyn Media>>`
  - 新增 `remove_managed_disk_with_media(...) -> Option<Box<dyn Media>>`
- 已明确连接失败与断开回收时的介质所有权链：
  - 连接失败时，`BackendRust` 把原始 `Media` 归还宿主
  - 断开成功时，`BackendRust` 把原始 `Media` 交回宿主
- 已在宿主盘状态容器补齐连接态 / 持有态操作：
  - `windows/tauri-client/src-tauri/src/state/disk_store.rs`
  - 新增配置态查询、持有介质取回 / 放回、连接记录插入 / 查询 / 移除能力
- 已在宿主盘服务层补齐手动连接 / 断开最小主路径：
  - `windows/tauri-client/src-tauri/src/backend/disk_service.rs`
  - 当前连接路径已收为：
    - 从配置态找到目标盘
    - 从宿主取出当前持有 `Media`
    - 以最小 `DiskConfig` 调用 `BackendRust` 正式建盘
    - 成功后记录 `diskId -> targetId`
    - 失败后把同一个 `Media` 实例放回宿主
  - 当前断开路径已收为：
    - 根据 `diskId` 找到当前连接记录
    - 调用 `BackendRust` 删盘并回收 `Media`
    - 移除连接记录
    - 把原始 `Media` 实例重新交回宿主持有
- 已在 Tauri 命令面新增连接 / 断开接口：
  - `windows/tauri-client/src-tauri/src/commands/disk.rs`
  - `windows/tauri-client/src-tauri/src/lib.rs`
  - 新增命令：
    - `connect_disk`
    - `disconnect_disk`
- 已在前端补齐连接 / 断开 API 与磁盘领域请求模型：
  - `windows/tauri-client/src/entities/disk/model.ts`
  - `windows/tauri-client/src/shared/api/diskClient.ts`
- 已把主页列表接入最小连接 / 断开交互：
  - `windows/tauri-client/src/widgets/DiskListPanel/DiskListPanel.vue`
  - `windows/tauri-client/src/pages/home/HomePage.vue`
  - 当前列表中：
    - 未连接磁盘显示 `连接`
    - 已连接磁盘显示 `断开`
    - 点击后调用宿主命令并刷新列表快照
    - 动作期间按 `diskId` 显示最小 loading 状态

## 本次新增验收（`tauri-client` Step 8）

- `cargo build` 已在 `windows/BackendRust/` 下通过。
- `cargo build` 已在 `windows/tauri-client/src-tauri/` 下通过。
- `pnpm build` 已在 `windows/tauri-client/` 下通过。

## 本次确认边界（`tauri-client` Step 8）

- 当前只完成手动连接 / 断开主路径，不接自动连接恢复执行链。
- 当前连接时使用 `BackendRust::DiskConfig::default()` 作为最小模板，只按当前介质容量与只读属性覆盖必要字段，不提前展开高级配置页。
- 当前断开后只保证宿主重新持有原始 `Media` 实例，不做额外会话恢复、状态缓存恢复或跨进程保活。
- 当前内存盘“断开后重连数据不丢”只保证在当前 App 进程常驻期间成立；App 退出后仍然丢失内存数据。
- 当前主页只补最小连接 / 断开按钮，不扩展编辑、删除、统计或调试入口。
- 当前下一步应转入最小验收路径，把“启动 -> 初始化切主页 -> 创建内存盘 / 文件盘 -> 手动连接 / 断开 -> 重启恢复配置列表”这条链按当前实现做一次完整验收并收边界。

## 本次新增完成（`tauri-client` Step 9）

- 已完成本轮 4 项结构调整，继续保持 `Element Plus` 最小骨架，不引入额外样式体系。
- 已精简初始化页结构：
  - `windows/tauri-client/src/pages/init/InitPage.vue`
  - 删除初始化页中的步骤条，只保留加载态、提示文案、错误提示和重试按钮。
- 已把“坏文件盘不致命”收进宿主恢复边界：
  - `windows/tauri-client/src-tauri/src/backend/persistence_service.rs`
  - `windows/tauri-client/src-tauri/src/state/disk_store.rs`
  - `windows/tauri-client/src-tauri/src/backend/disk_service.rs`
  - 当前恢复时如果文件盘目标不存在或无法打开：
    - 不再导致整个客户端初始化失败
    - 改为把该磁盘配置恢复为 `valid=false`
    - 宿主不持有对应 `Media`
    - 列表中显示为 `无效`
    - 不允许执行连接
- 已补磁盘删除最小闭环：
  - `windows/tauri-client/src-tauri/src/commands/disk.rs`
  - `windows/tauri-client/src/shared/api/diskClient.ts`
  - `windows/tauri-client/src/pages/home/HomePage.vue`
  - `windows/tauri-client/src/widgets/DiskListPanel/DiskListPanel.vue`
  - 当前删除路径已收为：
    - 未连接盘可直接删除配置
    - 已连接盘会先删盘并回收 `Media`
    - 再删除配置并写回 `.yumedisk/client.json`
- 已把磁盘卡片最小展示状态补齐：
  - `windows/tauri-client/src/entities/disk/model.ts`
  - `windows/tauri-client/src/entities/disk/presenter.ts`
  - 当前文件盘无效时：
    - 卡片显示 `无效`
    - 详情文案显示无效原因
    - `连接` 按钮禁用
- 已把文件盘创建对话框结构对齐概念骨架：
  - `windows/tauri-client/src/features/createFileDisk/CreateFileDiskDialog.vue`
  - 新增两个标签页：
    - `选择现有文件`
    - `创建文件`
  - 当前只实现“选择现有文件”页
  - “创建文件”页仅保留占位空态，不承接创建逻辑

## 本次新增验收（`tauri-client` Step 9）

- `cargo build` 已在 `windows/tauri-client/src-tauri/` 下通过。
- `pnpm build` 已在 `windows/tauri-client/` 下通过。

## 本次确认边界（`tauri-client` Step 9）

- 当前只把坏文件盘恢复为“无效配置态”，不做启动时自动修复、自动删除或重新选择文件向导。
- 当前无效文件盘只禁止连接，仍保留在列表中供用户后续删除。
- 当前文件盘对话框虽然补了“创建文件”标签页，但仍然只是占位，不实现任何文件创建链。
- 当前删除只做最小闭环，不补确认弹窗、批量删除或更多交互保护。
- 当前下一步仍应回到 `todo` 既定方向，执行一次最小验收路径并归档验收结果。

## 本次新增完成（`tauri-client` Step 10）

- 已把磁盘状态模型从“`valid + connected + invalid_reason` 并列判断”重建为“统一三态 + 原因补充”。
- 已在 Rust 宿主状态层新增统一状态枚举：
  - `windows/tauri-client/src-tauri/src/state/disk_store.rs`
  - 新增 `DiskStatus`：
    - `Disconnected`
    - `Connected`
    - `Invalid`
- 已去掉 Rust 配置态记录中的独立 `valid` 字段：
  - `ConfigDiskRecord` 现在只保留：
    - 统一状态推导来源
    - 可选 `invalid_reason`
  - 无效不再作为并列布尔状态单独表达
- 已在 Rust 服务层统一状态推导：
  - `windows/tauri-client/src-tauri/src/backend/disk_service.rs`
  - 当前状态规则为：
    - `invalid_reason.is_some()` => `Invalid`
    - 否则若存在连接记录 => `Connected`
    - 否则 => `Disconnected`
- 已在 Tauri DTO 层同步改为统一状态导出：
  - `windows/tauri-client/src-tauri/src/commands/disk.rs`
  - 新增 `DiskStatusDto`
  - 移除前端快照中的独立 `valid` / `connected` 输出
- 已在前端领域模型中同步改为统一状态：
  - `windows/tauri-client/src/entities/disk/model.ts`
  - 新增 `DiskStatus = \"disconnected\" | \"connected\" | \"invalid\"`
  - 移除独立 `valid` / `connected` 字段
- 已在卡片展示中改为直接消费统一三态：
  - `windows/tauri-client/src/widgets/DiskListPanel/DiskListPanel.vue`
  - `windows/tauri-client/src/entities/disk/presenter.ts`
  - 当前行为为：
    - 底部文件盘详情始终保留原文件路径
    - 右上状态位只显示三态之一
    - `无效` 状态悬停显示 `invalidReason`
    - `连接` 按钮在 `invalid` 状态下禁用

## 本次新增验收（`tauri-client` Step 10）

- `cargo build` 已在 `windows/tauri-client/src-tauri/` 下通过。
- `pnpm build` 已在 `windows/tauri-client/` 下通过。

## 本次确认边界（`tauri-client` Step 10）

- 当前仍保留 `invalid_reason` 作为无效状态的补充原因，但它不再与 `valid` 组成并列状态系统。
- 当前统一三态只用于客户端配置态与主页展示，不额外扩展更多中间态。
- 当前无效盘仍然只禁止连接，不做自动修复链。
- 当前下一步仍应回到 `todo` 既定方向，执行一次最小验收路径并归档验收结果。

## 本次新增完成（`tauri-client` Step 11）

- 已完成 `tauri-client` 当前轮 `Step 11`：重建文件盘“创建文件”最小闭环。
- 已把文件盘对话框中的“创建文件”标签页从占位重建为正式表单：
  - `windows/tauri-client/src/features/createFileDisk/CreateFileDiskDialog.vue`
  - 当前字段收口为：
    - 名称
    - 文件路径
    - 容量（MiB）
    - 文件格式
    - 启动自动连接
- 已把文件格式选择收成当前阶段固定边界：
  - `RAW` 可选
  - `VMDK` / `VHD` / `VHDX` / `VDI` / `QCOW2` 仅作禁用占位
- 已在前端领域模型与 API 层补齐“创建文件盘”请求：
  - `windows/tauri-client/src/entities/disk/model.ts`
  - `windows/tauri-client/src/shared/api/diskClient.ts`
  - 新增 `CreateNewFileDiskRequest`
  - 新增 `createNewFileDisk(...)`
- 已在 Tauri command 层新增“创建文件盘”命令：
  - `windows/tauri-client/src-tauri/src/commands/disk.rs`
  - `windows/tauri-client/src-tauri/src/lib.rs`
  - 新增命令：
    - `create_new_file_disk`
- 已在宿主文件盘服务层补齐“先建文件，再走既有文件盘添加链”的实现：
  - `windows/tauri-client/src-tauri/src/backend/disk_service.rs`
  - 当前链路已收为：
    - 校验名称、文件路径、容量（MiB）和文件格式
    - 校验父目录存在且目标文件不存在
    - 仅对 `RAW` 使用快速 `set_len(...)` 创建指定大小文件
    - 不主动初始化文件内容
    - 创建成功后复用既有 `create_file_disk(...)` 逻辑
    - 若后续添加文件盘失败，则回滚刚创建的目标文件

## 本次新增验收（`tauri-client` Step 11）

- `cargo build` 已在 `windows/tauri-client/src-tauri/` 下通过。
- `pnpm build` 已在 `windows/tauri-client/` 下通过。

## 本次确认边界（`tauri-client` Step 11）

- 当前“创建文件”分支只支持 `RAW`，其余格式仅保留禁用占位，不接实际创建链。
- 当前创建文件只负责快速设定文件长度，不做零填充、不做格式头初始化、不做镜像结构写入。
- 当前创建成功后仍然遵循既有产品语义：
  - 只是在宿主侧创建并持有 `Media`
  - 不自动连接到系统
  - 后续仍需用户手动执行连接
- 当前未补“浏览保存路径”能力，文件路径仍由用户直接输入。
- 当前下一步应转入当前总目标的最小验收路径，把“初始化 -> 创建内存盘 / 选择现有文件盘 / 创建新 RAW 文件盘 -> 手动连接 / 断开 -> 删除 -> 无效盘恢复 -> 重启恢复列表”按当前实现跑通并归档。

## 本次新增完成（`tauri-client` Step 12）

- 已完成 `tauri-client` 当前轮 `Step 12`：补齐“创建文件”路径浏览功能。
- 已在宿主文件盘服务层新增保存路径选择能力：
  - `windows/tauri-client/src-tauri/src/backend/disk_service.rs`
  - 新增 `pick_new_raw_file_path()`
  - 当前通过原生保存对话框选择目标文件位置，并限制 `RAW` 扩展过滤。
- 已在 Tauri command 层新增对应命令：
  - `windows/tauri-client/src-tauri/src/commands/disk.rs`
  - `windows/tauri-client/src-tauri/src/lib.rs`
  - 新增命令：
    - `pick_new_raw_file_path`
- 已在前端 API 层接入对应调用：
  - `windows/tauri-client/src/shared/api/diskClient.ts`
- 已把“创建文件”标签页中的文件路径输入补成“输入 + 浏览”组合：
  - `windows/tauri-client/src/features/createFileDisk/CreateFileDiskDialog.vue`
  - 用户现在可直接输入路径，或点击“浏览”选择新 `RAW` 文件保存位置。

## 本次新增验收（`tauri-client` Step 12）

- `cargo build` 已在 `windows/tauri-client/src-tauri/` 下通过。
- `pnpm build` 已在 `windows/tauri-client/` 下通过。

## 本次确认边界（`tauri-client` Step 12）

- 当前“创建文件”路径浏览只服务于选择新 `RAW` 文件位置，不负责提前创建文件。
- 当前仍然只支持 `RAW`，其他格式继续保持禁用占位。
- 当前下一步仍应回到最小验收路径，把当前闭环整体跑通并归档。

## 本次新增完成（`tauri-client` Step 13）

- 已完成 `tauri-client` 当前轮 `Step 13`：重建磁盘卡片编辑最小闭环。
- 已在宿主盘状态层补齐配置态更新能力：
  - `windows/tauri-client/src-tauri/src/state/disk_store.rs`
  - 新增按 `diskId` 更新配置态的能力，只允许修改：
    - `diskName`
    - `autoConnect`
  - 新增回滚恢复配置态记录的能力，用于持久化失败回退。
- 已在宿主盘服务层补齐编辑请求边界：
  - `windows/tauri-client/src-tauri/src/backend/disk_service.rs`
  - 当前编辑链收口为：
    - 校验磁盘存在
    - 校验名称非空
    - 仅改配置态名称和自动连接标记
    - 不改 `Media`
    - 不触发重连或删建盘
- 已在 Tauri command 层新增磁盘编辑命令：
  - `windows/tauri-client/src-tauri/src/commands/disk.rs`
  - `windows/tauri-client/src-tauri/src/lib.rs`
  - 新增命令：
    - `update_disk`
- 已在前端领域模型与 API 层补齐编辑请求：
  - `windows/tauri-client/src/entities/disk/model.ts`
  - `windows/tauri-client/src/shared/api/diskClient.ts`
  - 新增 `UpdateDiskRequest`
  - 新增 `updateDisk(...)`
- 已在前端补最小编辑对话框：
  - `windows/tauri-client/src/features/editDisk/EditDiskDialog.vue`
  - 当前字段严格收口为：
    - 名称
    - 启动自动连接
- 已把磁盘卡片补齐“编辑”入口并打通刷新链：
  - `windows/tauri-client/src/widgets/DiskListPanel/DiskListPanel.vue`
  - `windows/tauri-client/src/pages/home/HomePage.vue`
  - 当前路径已收为：
    - 点击卡片“编辑”
    - 打开编辑对话框
    - 保存配置
    - 写回 `.yumedisk/client.json`
    - 刷新主页列表

## 本次新增验收（`tauri-client` Step 13）

- `cargo build` 已在 `windows/tauri-client/src-tauri/` 下通过。
- `pnpm build` 已在 `windows/tauri-client/` 下通过。

## 本次确认边界（`tauri-client` Step 13）

- 当前编辑只允许修改 `diskName` 和 `autoConnect`，不开放路径、容量、介质类型等其他字段。
- 当前编辑不触发连接态变更；已连接盘编辑后仍保持当前连接状态。
- 当前编辑失败时只回滚配置态，不额外补确认弹窗或脏表单保护。
- 当前下一步可以转入启动白屏治理，把主窗口收成“首屏页面 ready 后再显示”。

## 本次新增完成（`tauri-client` Step 14）

- 已完成 `tauri-client` 当前轮 `Step 14`：重建主窗口启动显示链，消除程序启动阶段白屏。
- 已把主窗口初始可见性收为隐藏启动：
  - `windows/tauri-client/src-tauri/tauri.conf.json`
  - 主窗口现在启动时 `visible=false`。
- 已把前端首屏显示链收为“挂载完成并经过两帧后显示窗口”：
  - `windows/tauri-client/src/main.ts`
  - 当前链路为：
    - 创建 Vue App
    - 挂载 `#app`
    - 连续等待两次 `requestAnimationFrame`
    - 调用 `getCurrentWindow().show()`
- 这样窗口弹出时，前端首屏骨架已经完成最小渲染，用户看到的是初始化页而不是 WebView 白屏。

## 本次新增验收（`tauri-client` Step 14）

- `cargo build` 已在 `windows/tauri-client/src-tauri/` 下通过。
- `pnpm build` 已在 `windows/tauri-client/` 下通过。

## 本次确认边界（`tauri-client` Step 14）

- 当前只做到“首屏页面 ready 后显示”，不等待 `session` 初始化完成后再显示。
- 当前不引入额外原生 splash，不补复杂加载动画或多阶段启动编排。
- 当前显示时机依赖前端挂载完成与两帧刷新，目标是消除白屏而不是隐藏初始化页。
- 当前下一步可回到最小验收路径，按现有功能整体跑通并归档。
