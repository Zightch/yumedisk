# 2026-05-16

## 已完成

### 1. 正式文档口径与当前宿主主线对齐

- 更新 `docs/README.md`
- 更新 `docs/architecture/项目概述.md`
- 更新 `docs/architecture/功能文档.md`
- 更新 `docs/architecture/技术文档.md`
- 更新 `docs/development/开发文档.md`
- 更新 `windows/AppKernel/AppKernel设计文档.md`
- 更新 `windows/AppKernel/AppKernel-SDK文档.md`
- 更新 `windows/BackendCore/BackendCore-SDK文档.md`

已收束边界：

- 当前正式桌面主线为 `tauri-client + BackendRust + AppKernel + YumeDiskKMDF + YumeDiskSCSI`
- `AppKernel` 作为唯一用户态数据面内核，正式宿主不再回到宿主直连驱动旁路
- 当前正式桌面主线不枚举系统设备路径，不以 `PhysicalDrive` 或可见盘路径作为挂载成功判据
- `cpp-cli + BackendCore`、`rust-cli` 作为并行辅助宿主保留
- 旧 `windows/TestApp/`、`windows/client/backendHost/` 不再作为当前事实引用

### 2. tauri-client 单例启动收口为唤醒已存在实例

- `tauri-client` 启动前加入 Windows 单例控制
- 首实例持有命名互斥体，生命周期与应用一致
- 次实例检测到已有实例后，不再继续启动
- 次实例改为向首实例发送唤醒事件并立即退出
- 首实例收到唤醒事件后，拉起并聚焦 `main` 主窗口

验收结果：

- `windows/tauri-client/src-tauri` 下 `cargo build` 通过
- 已提交并推送单例相关提交：
  - `95f7a83` `feat(tauri-client): wake existing instance on relaunch`
  - `01d3aa0` `fix(tauri-client): keep single-instance guard alive`
- 已提交并推送文档同步提交：
  - `ba99a46` `docs: align architecture docs with current host stack`
