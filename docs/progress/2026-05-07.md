# 2026-05-07

## 已完成

- 完成 `KMDF` 内核占用压缩方案定稿，并把当前阶段的唯一目标收束为“在不改协议的前提下，压低 `KMDF async slot transport` 的每-slot 固定开销”。
- 本轮相关提交已形成连续重建链路：
  - `4f1ad6a Document KMDF CPU reduction plan`
  - `7796eb1 Clarify KMDF transport rebuild principle`
  - `0ec7751 Add KMDF transport runtime skeleton`
  - `bad7315 Pool KMDF async slot requests`
  - `bc88f5c Use KMDF transport submit worker`
  - `ecf8f8a Remove KMDF slot admission wait lock`
  - `4b8229b Batch drain KMDF submit queue`
  - `c9bd7e6 Pool KMDF sync command buffers`
- `windows/YumeDiskKMDF/YumeDiskKMDF` 的数据面热路径已经从旧的 per-slot 短命对象模型重建为 session-owned transport runtime：
  - `CTRL_FILE_CONTEXT` 现在持有唯一的 `TransportRuntime`。
  - runtime 与 file session 同生命周期，在 session open 后启动，在 cleanup/watchdog 关闭链路中停止并释放。
  - 旧的 per-slot work item 提交路径已经退出当前实现。
- `POST_READ_SLOT / POST_WRITE_SLOT` 的异步 slot transport 已完成第一阶段重建：
  - async slot request 改为 session-owned pool object，不再每次独立 `alloc/free`。
  - pool object 长期持有 `IRP + IOCTL_SCSI_MINIPORT buffer`。
  - 提交前使用 `IoReuseIrp`，热路径不再做 per-slot `IoAllocateIrp/IoFreeIrp`。
  - slot 对象准备完成后进入 session-owned submit queue，由长期 submit worker 在锁外调用 `IoCallDriver`。
- `KMDF` 高频 session 准入已从 `WDFWAITLOCK` 热路径移除：
  - `POST_READ_SLOT / POST_WRITE_SLOT` 不再沿用普通控制命令的 `ControlSessionAcquire` 路径。
  - slot 路径现在使用原子 `State` 快照、`InFlightRequestCount + PendingSlotCount` 引用和二次校验来完成准入。
  - slot completion 统一释放这两类引用；cleanup/watchdog 仍只在低频生命周期路径上使用 `SessionLock`。
- submit queue 已按“取走即解锁”原则收束：
  - submit worker 现在一次性把共享 submit queue 批量搬到本地 list。
  - `IoCallDriver` 只在锁外执行，不再每个 slot 抢一次共享队列锁。
  - runtime shutdown 也走同一条批量摘队列路径，统一失败完成 queued-but-not-dispatched slot。
- `ControlProxyCommand` 的同步控制命令转发也已完成第一阶段池化：
  - 新增 session-owned sync command buffer pool。
  - 正常 `READ_ACK / WRITE_ACK_BATCH / CANCEL_SLOT` 转发现在复用 `SRB_IO_CONTROL` buffer 和 kernel event handle，不再每次临时 `ControlAlloc + ZwCreateEvent + ZwClose`。
  - `miniport probe/open` 和 `session close cleanup` 保留低频临时 fallback 路径，确保 runtime 标记为 closing 后仍能发送收尾命令。
- `docs/tmp/todo.md` 与 `docs/tmp/kmdf-kernel-cpu-reduction-draft.md` 已同步到当前真实实现状态：
  - 当前已完成到 Step 7。
  - 当前唯一剩余工作已经收束为 VM 运行验证，而不是继续扩协议或回退 `SCSI`/App 模型。
- `AppKernel` 的正式目标方案已从临时草案收束为独立正式文档 [AppKernel设计文档](../../windows/AppKernel/AppKernel设计文档.md)：
  - `AppKernel` 的职责固定为“纯 C 的用户态数据面内核”，只负责 `KMDF session`、per-disk slot runtime、ACK 和线程调度。
  - 业务宿主只保留驱动收敛、介质对象、LBA overlap 策略、CLI 和可见盘枚举。
  - 对外接口固定为 `session + disk + media_ops + session event queue`，不再为写路径堆多组专用回调。
  - `write` 真实落介质仍发生在 `WRITE_ACK_BATCH` 之前；ACK 成功或失败只通过可选事件队列向上层通知。
  - `state` 与 `stats` 边界正式拆开：前者表示当前生命周期和运行态，后者只表示累计计数。
- `AppKernel` 正式方案随后又按“暂存层 + 最终裁决”模型完成一次收敛：
  - 公共前缀统一从 `APPK` 改为 `AK`，类型和函数签名统一为 `AK_*` / `Ak*`。
  - 业务宿主的写接口不再是“直接写正式介质”，而是 `stage_write(op, data)`，其中 `op` 携带 `TargetId + DiskRuntimeId + EventId + Seq + TotalSeq + OffsetBytes` 等精确标识字段。
  - 写事件不再是 `range ACK` 结果，而是整笔系统写的最终事件：`AkEventWriteFinalCommitted` / `AkEventWriteFinalRejected`。
  - 业务宿主的 `read_bytes` 明确要求返回 overlay 后的可见读视图：先读 staged write，再回落到正式介质。
  - 写最终事件变成正确性链路的一部分，不再允许像旧草案那样做可选成功事件或静默丢弃。

## 验收

- 本轮每个结构步骤完成后都执行了 `msbuild windows\\YumeDiskKMDF\\YumeDiskKMDF.sln /t:Build /p:Configuration=Release /p:Platform=x64 /m`。
- `YumeDiskKMDF` 源码编译、链接、测试签名、打包均通过。
- 最终输出中的已知失败点仍然只有环境侧 `InfVerif.dll` 缺失诊断，不是本轮源码引入的新编译错误。
- 当前工作树在最近一次提交后保持干净。

## 已收束边界

- 当前不回退 `App per-disk bounded workers` 数据面模型。
- 当前不回退 `SCSI per-target queue`，也不把 `SCSI` 队列锁改成 wait lock。
- 当前不改协议边界，仍只保留：
  - `POST_READ_SLOT`
  - `POST_WRITE_SLOT`
  - `READ_ACK`
  - `WRITE_ACK_BATCH`
  - `CANCEL_SLOT`
- 当前不改取消模型：
  - 系统侧取消只追到 `SCSI`。
  - App 可以回晚到 ACK。
  - stale / cancelled / not found 仍由 `SCSI` 判定。
- 当前 `KMDF` 热路径已经删掉的旧结构，不应再恢复：
  - per-slot `IoAllocateWorkItem / IoQueueWorkItem / IoFreeWorkItem`
  - per-slot `IoAllocateIrp / IoFreeIrp`
  - `POST_*_SLOT` 每次进入 `SessionLock`
  - submit queue 每 slot 单次抢锁后立即 `IoCallDriver`
- 当前剩余验证缺口已经明确收缩到运行面，而不是设计面：
  - 需要在 VM 中按 `ct -> disk visible -> Q1/Q2/Q8/Q32 -> Ctrl+C while active -> rm all -> app exit` 验证吞吐、kernel CPU、cleanup/watchdog 收尾和 late ACK 语义是否仍稳定。
