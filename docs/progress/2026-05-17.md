# 2026-05-17

## 已完成

### 1. storer 自带网关单盘最小闭环已打通

- `server/internal/storer` 当前已完成自带 `embedded gateway` 的单盘最小闭环。
- `AuthStart / AuthFinish / SessionOpen / ReadAt / WriteAt / Ping / Close` 已贯通。
- `storer` 本地 `raw` 文件后端已可承接真实块读写。
- 当前 `SessionOpen` 已固定为单盘独占策略：
  - 多个 client 可以各自认证成功；
  - 但同一时刻只允许一个活跃打开会话；
  - 后续 client 在 `SessionOpen` 阶段拿到 `disk-busy`。

### 2. 会话语义已收口为 connection-scoped 认证资格 + session-scoped 数据面

- 认证阶段定位符当前是 `challenge_token`，而不是对 client 额外暴露独立 `auth_id`。
- 认证成功后，gateway 只在当前连接上记录该 `disk_id` 的认证资格。
- 真正的数据面定位符仍然只有 `session_id`。
- 已确认当前边界成立：
  - 认证成功不等于拿到会话；
  - `SessionOpen` 才是真实会话创建入口；
  - `SessionOpen` busy 失败后，只要连接未断开，允许在同一连接上再次重试 `open`。

### 3. 新增协议联调工具 `network-auth-open`

- 新增 `windows/rust-cli/src/bin/network-auth-open.rs`。
- 该工具只复用 `rust-cli` 现有 `network` 模块，不经过 `BackendRust / AppKernel / KMDF`。
- 当前已重建为常驻交互联调程序，命令为：
  - `auth`
  - `open`
  - `close`
  - `state`
  - `exit`

用途：

- 保持真实 `GatewayConnection`
- 分开观察认证资格和真实会话的边界
- 复现 `disk-busy`
- 验证“同一连接上重试 `SessionOpen`”的协议语义

### 4. 已完成真实联调验证

通过 `network-auth-open` 与真实 `storer` 常驻进程已验证下面这条链：

1. A 连接 `auth`
2. A `open` 成功
3. B 连接 `auth`
4. B 第一次 `open` 返回 `disk-busy`
5. A `close`
6. B 不重连、不重新认证，在同一连接上再次 `open`
7. B 第二次 `open` 成功，拿到新的 `session_id`

这证明当前“认证资格绑定连接、会话独立创建”的模型已经能稳定工作。

### 5. 正式文档已开始切到 `whole | storer` 双角色口径

- `docs/network-disk-go-server/README.md`
- `docs/network-disk-go-server/overview.md`

已同步的长期方向：

- `whole`：内嵌 `gateway` 的单体部署
- `storer`：只持有后端存储并主动注册到独立 `gateway`
- `gateway-and-storer` 当前只单独定义注册阶段与复用规则
- 注册成功后，`gateway <-> storer` 直接复用 `SessionOpen / ReadAt / WriteAt / Ping / Close` 业务语义

### 6. 已完成 A1 配置模型重建

- `server/internal/config` 已从单一扁平 `Config` 重建为两套明确入口：
  - `StorerConfig`
  - `GatewayConfig`
- `storer` 配置当前已收口为：
  - `role = whole | storer`
  - 顶层 `storage_file_path + claim_code`
  - `[whole]`
  - `[storer]`
- `gateway` 配置当前已独立为：
  - `[client]`
  - `[storer]`
- 配置解析已支持一层 section 和 inline comment，能够直接读取正式文档中的：
  - `role = "whole" # whole | storer`
- `cmd/storer` 已切到新的 `LoadOrInitStorer(...)`；当前仍只放行 `role = whole` 进入现有 embedded gateway 最小闭环。
- 已新增 `cmd/gateway` 配置入口骨架，负责独立读取或初始化 gateway 配置；运行期逻辑仍留待后续步骤实现。
- 本地 `server/config.toml` 已按新的 sectioned 配置形态手动对齐；该文件当前受 `.gitignore` 保护，不纳入版本管理。

验收：

- 在 `server/` 目录执行 `go test ./...` 通过。

当前收束边界：

- 本步只完成配置模型和启动入口收口，不实现 `storer -> gateway` 注册或独立 `gateway` 运行时。
- `role = storer` 与 `cmd/gateway` 当前都能完成配置读取/初始化，但会明确停在“runtime not implemented yet”，避免伪装成已打通功能。

### 7. 已完成 A2 本地存储宿主拆分

- `server/internal/storer/core.go` 已新增 `Core`，把原本混在 `storer/service.go` 里的本地真实盘宿主职责独立出来：
  - `claim_code -> auth material`
  - `storage/file` 打开与关闭
  - 本地 `session.Service` 创建
- `Core` 当前提供的稳定能力已经收口为：
  - `DiskID()`
  - `AuthVerifier()`
  - `StoragePath()`
  - `SessionService()`
  - `Close()`
- `server/internal/storer/service.go` 现已退化为 `whole` 运行时装配层：
  - 只负责校验 `role = whole`
  - 装配 `Core + gateway.Handler`
  - 持有 client TCP 监听与连接生命周期
- 这意味着当前 `whole` 路径已经不再把“本地盘宿主”直接塞在网络监听服务内部；后续 `A3` 可以在同一个 `Core` 上继续拼：
  - embedded gateway whole
  - external storer

验收：

- 新增 `server/internal/storer/core_test.go`
- 在 `server/` 目录执行 `go test ./...` 通过

当前收束边界：

- 本步只拆出“本地盘 + 本地 session 数据面”的最小核心，不引入新的对外协议入口。
- `whole` 仍然保持原有单盘最小闭环行为，方便后续在装配层继续演进，而不是重写行为。

### 8. 已完成 A3 运行形态装配层

- `server/internal/storer` 已从“默认只有一个总 Service”进一步收口为显式角色装配：
  - `RoleRuntime`
  - `WholeRuntime`
  - `StorerRuntime`
- `RoleRuntime` 当前负责：
  - 统一创建 `Core`
  - 按 `cfg.Role` 选择具体运行时
  - 对外暴露统一的 `Run / Close / Role / DiskID / StoragePath` 入口
- `WholeRuntime` 当前负责：
  - 在现有 `Core` 上装配 `gateway.Handler`
  - 监听 client TCP
  - 管理连接生命周期
- `StorerRuntime` 当前已作为独立角色骨架落位：
  - 复用同一个 `Core`
  - 不再伪装成 `whole`
  - 当前运行时明确返回 `not implemented yet`
- `cmd/storer` 已切到新的 role-aware 启动方式：
  - 不再硬编码“只有 `role = whole` 才允许启动”
  - 现在统一创建 `RoleRuntime`
  - 再根据实际角色打印：
    - `whole runtime ready`
    - `storer runtime ready`

验收：

- 新增 `server/internal/storer/role_runtime_test.go`
- 现有 `integration_test.go` 已切到 `NewRoleRuntime(...)`
- 在 `server/` 目录执行 `go test ./...` 通过

当前收束边界：

- 本步只完成运行形态装配，不实现 `storer -> gateway` 注册链路。
- `StorerRuntime` 当前只是明确角色边界和启动骨架，真正的数据面外联与注册仍留给后续 `B / D / C1` 步骤。

### 9. 已完成 B1 client-facing gateway 边界拆分

- `server/internal/gateway` 已从“单盘本地直绑 handler”收口为面向 client 的边界层：
  - `AuthVerifierSource`
  - `SessionDataPlane`
- `gateway.Handler` 现已不再接受：
  - `NewHandler(realDiskID, authVerifier, sessions)`
- 认证阶段当前改为：
  - `AuthStart` 继续只负责发 challenge
  - `AuthFinish` 通过注入的 `AuthVerifierSource` 查 `disk_id -> auth_verifier`
  - 不再在 `gateway` 内部硬编码“唯一真实盘”
- 会话与数据面阶段当前改为：
  - `SessionOpen / Ping / Close / ReadAt / WriteAt`
  - 全部通过注入的 `SessionDataPlane` 承接
  - `gateway` 自身不再依赖具体 `session.Service` 类型
- `whole` 形态下已新增本地单盘适配层：
  - `server/internal/storer/gateway_local_adapter.go`
  - 由 `Core` 提供本地 `auth_verifier` 查询和本地 session 数据面实现
  - 因此现有 embedded gateway 最小闭环行为保持不变
- `gateway` 单测已同步切到测试适配器，而不是继续使用旧的单盘直绑构造。

验收：

- 在 `server/` 目录执行 `go test ./...` 通过

当前收束边界：

- 本步只完成 client-facing gateway 边界重建，不实现 `disk_id -> storer route` 注册表。
- 当前 `whole` 仍然只通过本地单盘适配器工作；真正的多盘路由来源留给 `B2`。 

### 10. 已完成 B2 路由/注册层骨架

- `server/internal/route` 已新增 `Registry`，作为当前 `gateway` 侧唯一真实路由来源。
- 路由表当前已收口到单一结构 `route.Entry`，保存：
  - `disk_id`
  - `auth_verifier`
  - `route_target`
  - `connection_id`
  - `connected`
  - `disk_size_bytes`
  - `read_only`
  - `max_io_bytes`
  - `session_ttl_seconds`
- `gateway` 当前已不再直接持有认证缓存，而是统一通过 `RouteSource` 查路由：
  - `AuthFinish` 通过路由表查 `disk_id -> auth_verifier`
  - `SessionOpen` 在认证资格之外，再显式校验目标路由当前是否存在
- `whole` 形态当前已改为在启动时先把本地盘注册到 `route.Registry`：
  - 路由目标当前固定为 `embedded://whole`
  - `gateway` 再通过该注册表承接本地单盘认证来源
- 路由表当前已具备连接级失活语义：
  - `DisconnectConnection(connection_id)`
  - 失活后该连接名下盘路由立即对 `gateway` 不可见
- 已新增 `server/internal/route/registry_test.go`
- `gateway` 单测已补路由失活后的 `SessionOpen -> session-unavailable` 行为验证。

验收：

- 在 `server/` 目录执行 `go test ./...` 通过

当前收束边界：

- 本步只完成 `disk_id -> route` 的唯一内存来源，不实现真正的 `storer -> gateway` 注册协议收包。
- 当前 `route_target` 仍只是注册层元数据；真正的 `gateway_session_id -> storer_session_id` 映射留给 `B3`。

### 11. 已完成 B3 gateway session 映射层

- `server/internal/gateway` 已新增独立 `sessionRegistry`，专门维护：
  - `gateway_session_id`
  - `client_connection_id`
  - `route_connection_id`
  - `upstream_session_id`
- `SessionOpen` 当前已不再把底层 `session.Service` 返回的 `session_id` 直接回给 client：
  - `gateway` 会先保存 `upstream_session_id`
  - 再分配自己的 `gateway_session_id`
  - client 后续只看到 `gateway_session_id`
- `Ping / ReadAt / WriteAt / Close` 当前都已改为：
  - 先查 `gateway_session_id -> upstream_session_id`
  - 再对底层数据面使用真实 upstream session id
- 失效清理当前已收口为：
  - `Close(session_id)` 时同时删除 gateway 映射并关闭 upstream session
  - client 连接关闭时，同时删除该连接名下全部 gateway session 映射，并逐个关闭 upstream session
  - 若底层已返回 `session unavailable`，gateway 会同步删除本地映射
- 这意味着当前 `client-and-gateway` 边界已经不再暴露底层 session 命名空间，为后续真正的 `gateway <-> storer` 转发链保留了稳定外部接口。

验收：

- 新增 `server/internal/gateway/session_mapping_test.go`
- 在 `server/` 目录执行 `go test ./...` 通过

当前收束边界：

- 本步只完成 gateway 本地 session 映射，不实现独立 `storer-facing gateway` 监听与注册接入。
- 当前数据面仍然只接本地 `SessionDataPlane`；真正的 `storer -> gateway` 外联入口留给 `B4`。

### 12. 已完成 B4 storer-facing gateway 边界骨架

- `server/internal/gateway` 已新增独立 `Runtime`，可以同时启动：
  - `client.listen_addr`
  - `storer.listen_addr`
- `cmd/gateway` 当前已不再停在 `runtime not implemented yet`，而是会真实启动独立 gateway 进程，并按信号优雅退出。
- `gateway` 当前已明确拆成两条入口：
  - client-facing：
    - 继续复用现有 `Handler`
    - 继续承接 `AuthStart / AuthFinish / SessionOpen / ReadAt / WriteAt / Ping / Close`
  - storer-facing：
    - 新增 `StorerHandler`
    - 新增 `StorerRouteRegistry`
    - 单独绑定 `storer.listen_addr`
- `storer-facing` 边界当前已明确收口为独立协议面：
  - 不会误走 client-facing handler
  - 当前收到负载会明确返回 `storer-facing gateway protocol not implemented yet`
- 这一步保证了后续 `D1` 注册链路可以直接落到独立入口上，而不是再把注册逻辑塞回 client-facing handler。

验收：

- 新增 `server/internal/gateway/runtime_test.go`
- 新增 `server/internal/gateway/storer_handler_test.go`
- 在 `server/` 目录执行 `go test ./...` 通过

当前收束边界：

- 本步只完成 storer-facing listener 和 handler 边界，不实现实际注册报文与路由入表。
- 当前独立 `gateway` 的 client 数据面仍使用 `session unavailable` 占位数据面；真正的 `storer -> gateway` 注册和透传接线留给 `C1 / D1 / D2 / D3`。

### 13. 已完成 C1 + C2 storer / whole 结构收口

- `server/internal/storer` 当前已把 `role = storer` 从占位骨架收口为真实数据面宿主：
  - 启动后主动长连 `storer.gateway_addr`
  - 不对 client 监听
  - 不承接 `AuthStart / AuthFinish`
  - 只承接：
    - `SessionOpen`
    - `ReadAt`
    - `WriteAt`
    - `Ping`
    - `Close`
- 已新增 `dataPlaneHandler`，作为 storer 角色对 gateway 的唯一业务入口：
  - 只理解数据面和开会话命令
  - 明确拒绝 client 认证命令
  - `disk_id` 不匹配时返回 `session-unavailable`
- `StorerRuntime` 当前已改为真实连接循环：
  - 主动连接 `gateway_addr`
  - 在单条连接上运行 `dataPlaneHandler`
  - 连接断开后按 `reconnect_seconds` 重连
  - 连接退出时清理该连接名下本地 session
- `whole` 形态当前继续保持 embedded gateway 最小闭环能力不变：
  - 仍可独立监听 client 端口
  - 仍通过装配层复用同一套 `Core`
  - 没有重新回退到把所有逻辑塞回旧 `service.go`
- 这意味着当前 `storer` / `whole` 的职责边界已经清楚：
  - `storer`：真实数据面宿主
  - `whole`：在同一个本地数据面宿主上额外装配 embedded gateway

验收：

- 新增 `server/internal/storer/data_plane_handler.go`
- 新增 `server/internal/storer/data_plane_handler_test.go`
- `server/internal/storer/integration_test.go` 已补 `role = storer` 数据面联通验证
- 在 `server/` 目录执行 `go test ./...` 通过

当前收束边界：

- 本步只完成 storer / whole 结构收口，不实现 `storer -> gateway` 注册报文。
- 当前 `storer` 已具备“只做真实数据面”的稳定边界，但真正把自己挂到独立 `gateway` 仍留给 `D1`。

### 14. 已完成 D1 + D2 + D3 gateway-and-storer 主路径接线

- `server/internal/proto` 已新增 storer 注册报文：
  - `OpStorerRegister`
  - `BuildStorerRegisterRequestBody(...)`
  - `ParseStorerRegisterRequestBody(...)`
- `role = storer` 当前启动后会：
  1. 主动连接 `gateway.storer.listen_addr`
  2. 先提交注册请求：
     - `gateway_token`
     - `disk_id`
     - `auth_verifier`
     - `disk_size_bytes`
     - `read_only`
     - `max_io_bytes`
     - `session_ttl_seconds`
  3. 注册成功后进入后续数据面复用阶段
- `gateway` 当前已把 storer 长连收口为专用连接代理：
  - 接收注册请求并写入真实路由表
  - 保存 `connection_id -> storer connection`
  - 为后续数据面请求做 request/response 配对
- `client-facing gateway` 当前已不再使用占位 `session unavailable` 数据面，而是直接走真实 `gateway <-> storer` 转发链：
  - `SessionOpen(disk_id)`：
    - 查路由
    - 向目标 storer 发起复用语义的 `SessionOpen`
    - 取回 `storer_session_id`
    - 建立 `gateway_session_id -> route_connection + storer_session_id` 映射
  - `ReadAt / WriteAt / Ping / Close`：
    - 先查 gateway session 映射
    - 再用对应 `route_connection + storer_session_id` 向目标 storer 转发
    - 把响应按 client 侧语义返回
- 连接清理当前也已接通：
  - storer 长连断开时：
    - 立即撤销该连接名下盘路由
    - 同时清掉该连接名下全部 gateway session 映射
  - client 连接断开时：
    - 关闭本连接名下全部 upstream session
- `transport.Runtime` 当前已补并发安全关闭，避免 `Close()` 与 `Run()` 并发时把 `conn` 置空导致崩溃。

验收：

- 新增 `server/internal/gateway/integration_test.go`
- 独立 `gateway + storer + client` 最小闭环已在测试内打通：
  - auth
  - register
  - open
  - write
  - read
  - close
- 在 `server/` 目录执行 `go test ./...` 通过

当前收束边界：

- 当前已完成协议主路径接线，但还没有做启动方式、联调工具和正式文档的最终收口。
- 下一步转入 `E / F`，把入口、联调和文档口径统一到当前真实结构。

### 15. 已完成 E1 + E2 启动入口与联调验证

- `cmd/gateway` 与 `cmd/storer` 当前都已是可直接运行的真实入口：
  - `gateway` 独立读取同目录 `config.toml`
  - `storer` 独立读取同目录 `config.toml`
  - `storer` 内部按 `role = whole | storer` 选择运行形态
- 本轮已按最终部署口径做独立进程联调：
  - 单独启动 `gateway.exe`
  - 单独启动 `storer.exe`
  - `role = storer` 主动长连到 `gateway.storer.listen_addr`
  - 注册成功后复用该长连接承接后续数据面请求
- `windows/rust-cli/src/bin/network-auth-open.rs` 本轮未需改动，继续可直连独立 `gateway`
- 已完成真实场景验证：
  1. A 连接 `auth`
  2. A `open` 成功
  3. B 连接 `auth`
  4. B 第一次 `open` 返回 `disk-busy`
  5. A `close`
  6. B 保持原连接、不重新认证，再次 `open`
  7. B 第二次 `open` 成功
- 本轮额外确认的运行期事实：
  - 当前两个可执行文件都按“可执行文件所在目录的 `config.toml`”取配置
  - `storer` 后端盘文件需要是已存在的真实文件；本轮使用 `4096` 字节测试盘完成了联调
  - `gateway` 对 client 暴露的 `session_id` 与 storer 侧真实 `session_id` 已保持隔离，reopen 时 client 能拿到新的 gateway session id

验收：

- 在 `server/` 目录执行 `go test ./...` 通过
- 在 `windows/rust-cli/` 目录执行 `cargo build --bin network-auth-open --quiet` 通过
- 已完成独立 `gateway + storer + rust-cli` 真实联调：
  - auth
  - open
  - busy
  - close
  - reopen

当前收束边界：

- E 阶段已完成，当前没有发现需要回补到 `server` 或 `windows/rust-cli` 的联调缺口。
- 下一步转入 `F1 + F2`，把正式文档口径收束到当前真实结构与启动方式。

### 16. 已完成 F1 + F2 正式文档收口

- 已同步正式文档到当前真实结构与运行口径：
  - `docs/network-disk-go-server/README.md`
  - `docs/network-disk-go-server/overview.md`
  - `docs/network-disk-go-server/gateway-and-storer.md`
  - `docs/network-disk-go-server/auth-routing.md`
  - `docs/network-disk-go-server/data-plane.md`
- 同时补齐了两份会影响口径一致性的配套文档：
  - `docs/network-disk-go-server/transport.md`
  - `docs/network-disk-go-server/client-and-gateway.md`
- 本轮文档收口重点：
  - 删除“仍是草案/后续再定义”的过期表述
  - 收进当前真实启动方式：
    - `cmd/gateway`
    - `cmd/storer`
    - 可执行文件同目录 `config.toml`
  - 收进当前真实 `gateway <-> storer` 关系：
    - `storer` 主动长连 `gateway`
    - 先注册
    - 再复用长连接承接 `SessionOpen / ReadAt / WriteAt / Ping / Close`
  - 收进当前真实 session 边界：
    - `gateway_session_id -> (route_connection, storer_session_id)`
    - client 不直接看到 `storer_session_id`
  - 收进当前真实联调结论：
    - `network-auth-open`
    - auth
    - open
    - busy
    - close
    - reopen

验收：

- 在 `server/` 目录执行 `go test ./...` 通过

当前收束边界：

- A-F 当前已全部收完。
- 下一步如果继续推进，应转入新的阶段性目标，而不是继续改本轮结构文档。
