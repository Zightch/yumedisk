# 2026-05-19

## 已完成

### 1. server 侧继续按开发原则第 5 点收结构

- 今日 server 相关提交序列：
  - `a078838` `refactor(server): split gateway and storer link runtimes`
  - `e2b4bfc` `refactor(server): split gateway client session module`
  - `ca19527` `refactor(server): split client connection and gateway storer data plane`
  - `a7c2994` `refactor(server): split storer gateway link module`
  - `48aecee` `refactor(server): finish remaining structure cleanup`
- `gateway` 侧拆开了原来混在顶层的大块职责：
  - `client/session/` 承接 session 注册、打开、通知相关职责
  - `client/connection/` 承接连接状态职责
  - `client/auth/` 承接鉴权器和 grant registry
  - `gateway/storer/` 承接 storer link runtime、registry、rpc client、active links、disconnect notifier、data plane
- `storer` 侧继续把 gateway link 相关职责下沉到稳定前缀：
  - `internal/storer/gateway/link/`
  - `internal/storer/gateway/local_adapter.go`
  - `internal/storer/gateway/register_client.go`
- `core.go`、`role_runtime.go`、`whole_runtime.go` 继续回到上层编排职责，不再直接混放链接面、数据面和本地盘细节。
- `server/internal/storer/local_disk.go` 已独立落地，本地盘职责不再混在 `core.go` 中。

### 2. tauri-client 网盘结构重构与补口

- 今日 tauri-client 相关提交序列：
  - `5bd5bc8` `refactor(tauri-client): split network disk flows`
  - `bb6b22c` `refactor(tauri-client): split network client state`
  - `8e03d40` `test(tauri-client): cover network runtime edges`
  - `88c07e5` `refactor(tauri-client): tighten network event boundaries`
  - `7fd32af` `test(tauri-client): cover network high-risk flows`
  - `0753b85` `fix(tauri-client): restore network draft dialog layout`
  - `ea3a3f5` `refactor(tauri-client): add workflow network orchestration`
  - `296f85f` `fix(tauri-client): preserve mounted network runtime on rescan`
- Rust 侧重构结果：
  - 删除 `src-tauri/src/backend/network_service.rs`
  - 将 network 业务编排拆到 `src-tauri/src/network/`
  - 将 `NetworkClientState` 拆成目录模块：
    - `connection_pool.rs`
    - `opened_sessions.rs`
    - `drafts.rs`
    - `pending_events.rs`
  - 新增顶层 `src-tauri/src/workflow/`
    - `network_runtime.rs`
    - `network_draft.rs`
    - `runtime_disk.rs`
  - `backend/disk_service.rs` 退回 local adapter，只保留 local disk 相关宿主职责
- 前端侧重构结果：
  - 创建网络盘 feature 已拆成：
    - `CreateNetworkDiskDialog.vue`
    - `useNetworkDraftFlow.ts`
    - `NetworkDraftForm.vue`
    - `NetworkDraftList.vue`
    - `networkDraftError.ts`
  - 已补回创建网络盘对话框在重构后丢失的布局样式
- 本轮补掉的真实行为问题：
  - draft dispose 失败时，不再直接丢失前端对后端草稿的引用
  - network pending event 处理边界已收紧，不再在多个入口散着对 runtime 产生副作用
  - 网盘已挂载后再创建新网盘并触发重扫时，不再把已有 `Mounted` 状态错误写回 `Unmounted`
  - 当前重扫矩阵为：
    - 已挂载且 live session 仍可用：保持 `Mounted`
    - 已挂载但原 session 失效、重开成功：卸载旧挂载后落为 `Unmounted`
    - 已挂载但无法恢复：落为 `Invalid`

### 3. 文档同步

- `docs/network/client/tauri-client.md` 已同步到当前 network / workflow / state 代码落点。
- 本进度文档已按今日真实提交序列回填，不再沿用旧的 `02177e8` 验收口径。

## 验收

- `server/` 执行 `go test ./...` 通过。
- `windows/tauri-client/src-tauri` 执行 `cargo test` 通过，当前共 `23` 个测试通过。
- `windows/tauri-client` 执行 `pnpm exec vue-tsc --noEmit` 通过。

## 当前边界

- server 侧今天主要完成的是结构收束，不改协议与概念边界；后续若继续推进，应只在出现稳定目录前缀时再继续下沉。
- tauri-client 侧保持原有正式概念结构不变：
  - `NetworkClientState` 仍是 live session 真状态 owner
  - `DiskRuntime` 不持有 live session
  - `NetworkMedia` 仍只在 mount 时创建
  - 全局唯一键仍是 `(server_addr, remote_disk_id)`
- tauri-client 侧目前已完成结构重构主体、workflow 收口、事件边界收束、Rust 高风险路径测试补强、样式回补和重扫状态修正。
- 前端自动化测试与系统化手工回归清单仍未补。
