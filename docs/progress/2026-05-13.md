# 2026-05-13

## 本次新增完成（`tauri-client` Step 15）

- 已完成 `tauri-client` 当前轮 `Step 15`：回滚主窗口“首屏 ready 后显示”方案。
- 原因是该方案在当前 `Tauri + WebView` 启动链下导致主窗口长时间不显示，实测不可接受。
- 已把启动链回滚到此前稳定状态：
  - `windows/tauri-client/src-tauri/tauri.conf.json`
  - `windows/tauri-client/src/main.ts`
- 当前恢复为：
  - 主窗口默认可见启动
  - 前端正常挂载 `App`
  - 不再做隐藏窗口后的延迟 `show()` 控制

## 本次新增验收（`tauri-client` Step 15）

- `cargo build` 已在 `windows/tauri-client/src-tauri/` 下通过。
- `pnpm build` 已在 `windows/tauri-client/` 下通过。

## 本次确认边界（`tauri-client` Step 15）

- 当前先保证主窗口可稳定显示，白屏优化暂不继续推进。
- 当前不再尝试基于隐藏主窗口 + 前端回调 `show()` 的启动链。
- 当前下一步回到最小验收路径，继续验证现有功能闭环。

## 本次新增完成（`tauri-client` Step 16）

- 已完成 `tauri-client` 当前轮 `Step 16`：重建当前轮 `todo` 方向，把目标从“功能最小验收”切换为“概念页视觉效果落地方案”。
- 已明确新的 UI 推进原则：
  - `Element Plus` 继续作为基础架构
  - 在其基础上叠加概念页已确认的视觉效果
  - 不脱离现有页面结构、功能入口和数据边界
  - 不为美化引入概念页之外的新模块、新入口和新交互

## 本次确认边界（`tauri-client` Step 16）

- 当前只重建 UI 视觉层方案，不回退到纯骨架阶段。
- 当前 UI 美化必须建立在现有 `Element Plus` 结构之上，不重写基础组件体系。
- 当前视觉目标以 `docs/tauri-client-ui-design.md` 和概念页现有呈现为准。
- 当前下一步应先重建全局主题变量与基础视觉骨架，再逐步落页头、列表面板、磁盘卡片和弹层样式。

## 本次新增完成（`tauri-client` Step 17）

- 已完成 `tauri-client` 当前轮 `Step 17`：重建全局主题与视觉基础层。
- 已新增前端主题模型与默认主题装配：
  - `windows/tauri-client/src/shared/theme/theme.ts`
  - `windows/tauri-client/src/main.ts`
- 已新增全局样式入口与语义变量层：
  - `windows/tauri-client/src/shared/styles/index.css`
  - `windows/tauri-client/src/shared/styles/tokens.css`
- 已新增 `Element Plus` 变量覆盖层：
  - `windows/tauri-client/src/shared/styles/element.css`
- 已收口主题基础能力：
  - 亮色 / 暗色模式变量
  - 六套颜色家族变量
  - 页面、面板、边框、文本、强调色语义令牌
  - `Element Plus` 基础颜色、背景、边框、圆角、阴影变量对齐

## 本次新增验收（`tauri-client` Step 17）

- `pnpm build` 已在 `windows/tauri-client/` 下通过。
- 当前构建仍存在第三方依赖 `@vueuse/core` 的 Rollup 注释警告，以及打包体积提醒；均非本步引入，未影响构建通过。

## 本次确认边界（`tauri-client` Step 17）

- 当前只完成主题底座和 `Element Plus` 变量层，不改页面结构语义。
- 当前默认主题先固定为 `dark + higanRed`，后续再接设置页和运行时切换。
- 当前主题系统使用 `data-theme-mode` / `data-theme-color` 与 CSS 变量，不运行时拼接整份样式。
- 当前概念页中的具体卡片、页头、弹层视觉尚未迁移；下一步进入主窗口与初始化页视觉骨架重建。

## 本次新增完成（`tauri-client` Step 18）

- 已完成 `tauri-client` 当前轮 `Step 18`：重建主窗口与初始化页视觉骨架。
- 已新增主窗口与页面骨架样式：
  - `windows/tauri-client/src/shared/styles/layout.css`
- 已把应用根容器收为统一壳结构：
  - `windows/tauri-client/src/app/App.vue`
- 已把主页容器收为统一留白和主区布局：
  - `windows/tauri-client/src/pages/home/HomePage.vue`
- 已把初始化页迁移为概念稿一致的居中卡片骨架：
  - `windows/tauri-client/src/pages/init/InitPage.vue`
- 本步已落地的视觉骨架包括：
  - 根窗口背景与整页壳层
  - 统一外边距与主区高度收口
  - 初始化居中卡片、圆角、阴影和文本层级
  - 初始化加载图形容器与失败重试区域

## 本次新增验收（`tauri-client` Step 18）

- `pnpm build` 已在 `windows/tauri-client/` 下通过。
- 当前构建仍存在第三方依赖 `@vueuse/core` 的 Rollup 注释警告，以及打包体积提醒；均非本步引入，未影响构建通过。

## 本次确认边界（`tauri-client` Step 18）

- 当前只完成主窗口与初始化页视觉骨架，不进入主页顶部细节美化。
- 当前初始化页已移除旧的纯表单式骨架，改为正式壳层中的居中卡片结构。
- 当前主页与初始化页共用统一 `app-window` / `app-shell` 布局语义，为后续页头、列表面板和卡片样式迁移提供固定落点。
- 当前下一步进入主页顶部操作区视觉重建。

## 本次新增完成（`tauri-client` Step 19）

- 已完成 `tauri-client` 当前轮 `Step 19`：重建主页顶部操作区视觉。
- 已新增顶部操作区局部样式：
  - `windows/tauri-client/src/shared/styles/header.css`
- 已把主页顶部收为正式视觉结构：
  - `windows/tauri-client/src/widgets/AppHeader/AppHeader.vue`
- 本步已落地的顶部元素包括：
  - `YumeDisk` 大标题与设置按钮对位
  - 会话状态 pill
  - 添加按钮的圆形主操作样式
  - 三选项类型气泡的横向样式、无箭头样式和图标字母块
- 当前保持的交互语义：
  - 设置按钮仍为禁用占位
  - 添加入口仍然打开内存盘 / 文件盘 / 网络盘占位三选项
  - 内存盘、文件盘入口行为未变

## 本次新增验收（`tauri-client` Step 19）

- `pnpm build` 已在 `windows/tauri-client/` 下通过。
- 当前构建仍存在第三方依赖 `@vueuse/core` 的 Rollup 注释警告，以及打包体积提醒；均非本步引入，未影响构建通过。

## 本次确认边界（`tauri-client` Step 19）

- 当前只完成主页顶部操作区视觉，不进入磁盘列表面板和磁盘卡片视觉。
- 当前顶部操作区已经从临时 `Element Plus` 默认排布迁移到正式布局落点，但未引入设置页交互。
- 当前添加气泡仍由 `Element Plus` 弹层承接，只做视觉覆写，不重写弹层机制。
- 当前下一步进入磁盘列表面板视觉重建。

## 本次新增完成（`tauri-client` Step 20）

- 已完成 `tauri-client` 当前轮 `Step 20`：重建磁盘列表面板视觉。
- 已新增列表面板局部样式：
  - `windows/tauri-client/src/shared/styles/listPanel.css`
- 已把磁盘列表面板收为正式面板壳结构：
  - `windows/tauri-client/src/widgets/DiskListPanel/DiskListPanel.vue`
- 本步已落地的列表面板元素包括：
  - 面板背景、边框、圆角和内边距
  - 头部标题区与数量标签
  - 自动连接统计 pill
  - 列表滚动区与加载骨架容器
  - 空态与错误态在面板内的统一落点
- 当前保持的边界：
  - 单张磁盘卡片内部结构和视觉尚未重建
  - 列表项业务字段与操作语义未变

## 本次新增验收（`tauri-client` Step 20）

- `pnpm build` 已在 `windows/tauri-client/` 下通过。
- 当前构建仍存在第三方依赖 `@vueuse/core` 的 Rollup 注释警告，以及打包体积提醒；均非本步引入，未影响构建通过。

## 本次确认边界（`tauri-client` Step 20）

- 当前只完成列表面板本体视觉，不进入单张磁盘卡片视觉。
- 当前面板滚动、头部统计和加载/空态/错误态已经迁移到正式壳层，但卡片仍保留临时默认结构。
- 当前下一步进入磁盘卡片视觉重建。

## 本次新增完成（`tauri-client` Step 21）

- 已完成 `tauri-client` 当前轮 `Step 21`：重建磁盘卡片视觉。
- 已新增磁盘卡片独立组件：
  - `windows/tauri-client/src/widgets/DiskCard/DiskCard.vue`
- 已新增磁盘卡片局部样式：
  - `windows/tauri-client/src/shared/styles/diskCard.css`
- 已把列表中的单张磁盘项从临时默认 `Element Plus` 结构收为正式卡片结构：
  - `windows/tauri-client/src/widgets/DiskListPanel/DiskListPanel.vue`
- 已补充磁盘摘要展示拼接函数：
  - `windows/tauri-client/src/entities/disk/presenter.ts`
- 本步已落地的卡片元素包括：
  - `M` / `F` 图标块
  - 名称、摘要、文件路径
  - 已连接 / 未连接 / 无效状态标签
  - 已连接渐变背景与强调边框
  - hover / focus-within 显示的连接、编辑、删除操作区
  - 无效状态 tooltip
- 当前保持的交互语义：
  - 连接 / 断开 / 编辑 / 删除事件边界未变
  - 无效盘仍禁止连接
  - 文件盘仍展示路径，内存盘不展示额外描述行

## 本次新增验收（`tauri-client` Step 21）

- `pnpm build` 已在 `windows/tauri-client/` 下通过。
- 当前构建仍存在第三方依赖 `@vueuse/core` 的 Rollup 注释警告，以及打包体积提醒；均非本步引入，未影响构建通过。

## 本次确认边界（`tauri-client` Step 21）

- 当前已完成磁盘卡片视觉和操作显隐，不进入创建 / 编辑弹层视觉。
- 当前卡片结构已经独立为 `widget`，后续弹层与设置页不会继续把列表组件堆大。
- 当前下一步进入创建 / 编辑弹层视觉重建。

## 本次新增完成（`tauri-client` Step 22）

- 已完成 `tauri-client` 当前轮 `Step 22`：重建创建 / 编辑弹层视觉。
- 已新增对话框专用样式层：
  - `windows/tauri-client/src/shared/styles/dialogs.css`
- 已把样式入口接入全局样式装配：
  - `windows/tauri-client/src/shared/styles/index.css`
- 已重建以下弹层的正式视觉结构：
  - `windows/tauri-client/src/features/createMemoryDisk/CreateMemoryDiskDialog.vue`
  - `windows/tauri-client/src/features/createFileDisk/CreateFileDiskDialog.vue`
  - `windows/tauri-client/src/features/editDisk/EditDiskDialog.vue`
- 本步已落地的弹层视觉元素包括：
  - 居中 modal、overlay 模糊、圆角面板、弱边框和统一阴影
  - 自定义标题栏、关闭按钮、固定底部操作区
  - 表单输入、数字输入、开关、浏览按钮的统一视觉覆写
  - 文件盘双 tab 的正式页签样式
  - 内存盘介质选择和文件格式选择的 chip 样式
- 当前保持的交互语义：
  - 创建、浏览、编辑、提交命令边界未变
  - 文件盘“选择现有文件 / 创建文件”双 tab 语义未变
  - 文件格式仍然只开放 `RAW`，其余为禁用占位

## 本次新增验收（`tauri-client` Step 22）

- `pnpm build` 已在 `windows/tauri-client/` 下通过。
- 当前构建仍存在第三方依赖 `@vueuse/core` 的 Rollup 注释警告，以及打包体积提醒；均非本步引入，未影响构建通过。

## 本次确认边界（`tauri-client` Step 22）

- 当前只完成创建 / 编辑弹层视觉重建，不进入设置页视觉骨架。
- 当前仍以 `Element Plus` 对话框、表单和 tabs 为基础，只做视觉覆写，不重写弹层机制。
- 当前未改动磁盘创建、编辑、浏览和提交的功能语义。
- 当前下一步进入设置页视觉骨架重建。

## 本次新增完成（`tauri-client` Step 23）

- 已完成 `tauri-client` 当前轮 `Step 23`：重建设置页视觉骨架。
- 已新增设置页独立 `widget`：
  - `windows/tauri-client/src/widgets/SettingsPage/SettingsPage.vue`
- 已新增设置页局部样式：
  - `windows/tauri-client/src/shared/styles/settingsPage.css`
- 已把设置页样式入口接入全局样式装配：
  - `windows/tauri-client/src/shared/styles/index.css`
- 已把主页头部设置按钮从禁用占位切换为正式入口：
  - `windows/tauri-client/src/widgets/AppHeader/AppHeader.vue`
- 已把主页接入设置页覆盖层与主题切换：
  - `windows/tauri-client/src/pages/home/HomePage.vue`
- 本步已落地的设置页元素包括：
  - 返回头部
  - 主题模式切换块
  - 主题颜色切换块
  - 恢复策略列表块
  - 默认创建行为列表块
- 当前主题切换仍只作用于前端视觉层，未扩展新的宿主配置语义。

## 本次新增验收（`tauri-client` Step 23）

- `pnpm build` 已在 `windows/tauri-client/` 下通过。
- 当前构建仍存在第三方依赖 `@vueuse/core` 的 Rollup 注释警告，以及打包体积提醒；均非本步引入，未影响构建通过。

## 本次确认边界（`tauri-client` Step 23）

- 当前只完成设置页视觉骨架和前端主题切换，不扩展新的设置项语义。
- 当前恢复策略、默认创建行为仍为静态展示值，不改变现有功能闭环。
- 当前设置页仍以 `Element Plus` 的 `Card`、`RadioGroup`、`Scrollbar` 为基础，只做视觉覆写。
- 当前下一步进入整体视觉验收，对照概念页和 UI 设计文档检查一致性。 

## 本次新增完成（`tauri-client` Step 24）

- 已完成 `tauri-client` 当前轮 `Step 24`：执行整体视觉验收并收尾一致性修正。
- 已对照以下基线完成检查：
  - `tmp/tauri-client-concept`
  - `docs/tauri-client-ui-design.md`
  - 当前正式客户端初始化页、主页头部、列表面板、磁盘卡片、创建 / 编辑弹层、设置页
- 已完成本轮最小尾差修正：
  - 去掉概念页之外的根背景径向强调层
  - 把页头、列表面板、磁盘卡片、初始化页的响应式换行断点收回到概念稿同级别
  - 把设置按钮恢复为正式可点击游标语义
- 本步涉及文件：
  - `windows/tauri-client/src/shared/styles/tokens.css`
  - `windows/tauri-client/src/shared/styles/header.css`
  - `windows/tauri-client/src/shared/styles/listPanel.css`
  - `windows/tauri-client/src/shared/styles/diskCard.css`
  - `windows/tauri-client/src/shared/styles/layout.css`

## 本次新增验收（`tauri-client` Step 24）

- `pnpm build` 已在 `windows/tauri-client/` 下通过。
- 当前构建仍存在第三方依赖 `@vueuse/core` 的 Rollup 注释警告，以及打包体积提醒；均非本步引入，未影响构建通过。
- 本步只调整视觉样式层，未改动 `Tauri command`、前端功能语义和状态模型；当前可视为未引入新的功能回归面。

## 本次确认边界（`tauri-client` Step 24）

- 当前轮“正式 UI 视觉层重建”目标已完成，已完成概念页到真实客户端的主视觉迁移与整体验收。
- 当前不继续在本轮内追加新的功能、主题扩展或白屏优化动作。
- 当前 `docs/tmp/todo.md` 应清空到等待下一轮状态，由下一轮重新定义总目标与唯一下一步。

## 本次新增完成（`tauri-client` Step 25）

- 已完成 `tauri-client` 当前轮 `Step 25`：收尾一组用户追加的视觉修正。
- 已修正文件盘创建对话框页签未居中的问题，统一为对话框顶部居中排列。
- 已修正主题 / 创建对话框中的单选按钮描边发虚问题，统一改为更低亮度、更厚的 2px 描边。
- 已按要求移除设置页中的“恢复策略”和“默认创建行为”两个卡片，仅保留主题卡片。
- 已修正主页滚动语义：主窗口不再整页滚动，磁盘列表面板恢复为随窗口高度自适应并在列表内部滚动。
- 本步涉及文件：
  - `windows/tauri-client/src/shared/styles/tokens.css`
  - `windows/tauri-client/src/shared/styles/layout.css`
  - `windows/tauri-client/src/shared/styles/listPanel.css`
  - `windows/tauri-client/src/shared/styles/dialogs.css`
  - `windows/tauri-client/src/shared/styles/settingsPage.css`
  - `windows/tauri-client/src/widgets/SettingsPage/SettingsPage.vue`

## 本次新增验收（`tauri-client` Step 25）

- `pnpm build` 已在 `windows/tauri-client/` 下通过。
- 当前构建仍存在第三方依赖 `@vueuse/core` 的 Rollup 注释警告，以及打包体积提醒；均非本步引入，未影响构建通过。

## 本次确认边界（`tauri-client` Step 25）

- 当前只处理用户明确指出的四个视觉问题，不扩展新的页面语义和功能改造。
- 当前设置页先收口为主题页，恢复策略与默认创建行为后续如需恢复，应在下一轮重新定义。
- 当前下一步等待用户继续给出下一轮 UI 或功能目标。

## 本次新增完成（`tauri-client` Step 26）

- 已完成 `tauri-client` 当前轮 `Step 26`：继续收紧单选按钮边框和文件盘创建页签布局。
- 已把主题 / 创建对话框中的单选按钮描边继续降低亮度，同时保留 2px 宽度，减轻边缘发亮感。
- 已修正文件盘创建对话框页签容器的宽度语义，避免页签项被拉成 `| 选项A | 空白 | 选项B |` 的分散布局，恢复为紧凑并排显示。
- 本步涉及文件：
  - `windows/tauri-client/src/shared/styles/dialogs.css`
  - `windows/tauri-client/src/shared/styles/settingsPage.css`

## 本次新增验收（`tauri-client` Step 26）

- `pnpm build` 已在 `windows/tauri-client/` 下通过。
- 当前构建仍存在第三方依赖 `@vueuse/core` 的 Rollup 注释警告，以及打包体积提醒；均非本步引入，未影响构建通过。

## 本次确认边界（`tauri-client` Step 26）

- 当前只继续收两个视觉细节，不改动页面结构和功能语义。
- 如果页签仍存在 Element Plus 内部布局残留问题，下一轮应直接切换为更明确的局部自定义页签壳，而不是继续叠加覆盖样式。

## 本次新增完成（`tauri-client` Step 27）

- 已完成 `tauri-client` 当前轮 `Step 27`：重建设置页与对话框中 chip 单选按钮的边框实现。
- 已把以下两处 `ElRadioButton` chip 的边框呈现从默认 `Element Plus` segmented 风格覆写，收为本地统一实现：
  - `windows/tauri-client/src/shared/styles/settingsPage.css`
  - `windows/tauri-client/src/shared/styles/dialogs.css`
- 本步已处理的根因包括：
  - 去除默认 `outline` 残留
  - 去除默认激活态拼接 `box-shadow` 残留
  - 改为统一使用 `inset box-shadow` 绘制边框
  - 为 `focus-visible` 单独收一层轻量外环，避免暗色下边缘发虚

## 本次新增验收（`tauri-client` Step 27）

- `pnpm build` 已在 `windows/tauri-client/` 下通过。
- 当前构建仍存在第三方依赖 `@vueuse/core` 的 Rollup 注释警告，以及打包体积提醒；均非本步引入，未影响构建通过。

## 本次确认边界（`tauri-client` Step 27）

- 当前只处理 chip 单选按钮边框毛糙感，不改按钮布局、主题色和功能语义。
- 当前只收设置页与对话框内的 chip 样式，不扩散到其他 `Element Plus RadioButton` 使用点。

## 本次新增完成（`tauri-client` Step 28）

- 已完成 `tauri-client` 当前轮 `Step 28`：重建主题强调文本语义令牌，按亮暗模式与颜色家族收口强调文本对比度。
- 已在 `windows/tauri-client/src/shared/styles/tokens.css` 中引入独立的 `--accent-text` 令牌，把“强调填充色”与“强调文本色”从根上拆开。
- 已为以下指定组合单独收紧强调文本对比度：
  - 亮色 `coralOrange`：文本颜色调暗
  - 亮色 `mintGreen`：文本颜色调暗
  - 暗色 `twilightPurple`：文本颜色调亮
- 已把以下文本语义引用统一改为使用 `--accent-text`：
  - `windows/tauri-client/src/shared/styles/dialogs.css`
  - `windows/tauri-client/src/shared/styles/settingsPage.css`
  - `windows/tauri-client/src/shared/styles/diskCard.css`
  - `windows/tauri-client/src/shared/styles/layout.css`
  - `windows/tauri-client/src/shared/styles/element.css`
- 本步同时收录当前稳定的两项布局修正：
  - `windows/tauri-client/src/shared/styles/header.css`
  - `windows/tauri-client/src/shared/styles/layout.css`

## 本次新增验收（`tauri-client` Step 28）

- `pnpm build` 已在 `windows/tauri-client/` 下通过。
- 当前构建仍存在第三方依赖 `@vueuse/core` 的 Rollup 注释警告，以及打包体积提醒；均非本步引入，未影响构建通过。

## 本次确认边界（`tauri-client` Step 28）

- 当前从主题令牌层处理强调文本对比度，不继续在各组件局部硬编码特判颜色。
- 当前只对用户明确指定的三组亮暗 / 颜色家族组合做差异化强调文本调整，其余家族保持现有语义值。
