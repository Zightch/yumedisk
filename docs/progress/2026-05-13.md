# 2026-05-13

## 本次新增完成（`tauri-client` Step 15）

- 已完成 `tauri-client` 当前轮 `Step 15`：回滚主窗口“首屏 ready 后显示”方案。
- 原因是该方案在当前 `Tauri + WebView` 启动链下导致主窗口长时间不显示，实测不可接受。
- 已把启动链回滚到此前稳定状态：
  - `windows/tauri-client/src-tauri/tauri.conf.json`
  - `windows/tauri-client/src/main.ts`
- 当前恢复为：
  - 主窗口默认可见启动
  - 前端正常挂载 `App`
  - 不再做隐藏窗口后的延迟 `show()` 控制

## 本次新增验收（`tauri-client` Step 15）

- `cargo build` 已在 `windows/tauri-client/src-tauri/` 下通过。
- `pnpm build` 已在 `windows/tauri-client/` 下通过。

## 本次确认边界（`tauri-client` Step 15）

- 当前先保证主窗口可稳定显示，白屏优化暂不继续推进。
- 当前不再尝试基于隐藏主窗口 + 前端回调 `show()` 的启动链。
- 当前下一步回到最小验收路径，继续验证现有功能闭环。

## 本次新增完成（`tauri-client` Step 16）

- 已完成 `tauri-client` 当前轮 `Step 16`：重建当前轮 `todo` 方向，把目标从“功能最小验收”切换为“概念页视觉效果落地方案”。
- 已明确新的 UI 推进原则：
  - `Element Plus` 继续作为基础架构
  - 在其基础上叠加概念页已确认的视觉效果
  - 不脱离现有页面结构、功能入口和数据边界
  - 不为美化引入概念页之外的新模块、新入口和新交互

## 本次确认边界（`tauri-client` Step 16）

- 当前只重建 UI 视觉层方案，不回退到纯骨架阶段。
- 当前 UI 美化必须建立在现有 `Element Plus` 结构之上，不重写基础组件体系。
- 当前视觉目标以 `docs/tauri-client-ui-design.md` 和概念页现有呈现为准。
- 当前下一步应先重建全局主题变量与基础视觉骨架，再逐步落页头、列表面板、磁盘卡片和弹层样式。

## 本次新增完成（`tauri-client` Step 17）

- 已完成 `tauri-client` 当前轮 `Step 17`：重建全局主题与视觉基础层。
- 已新增前端主题模型与默认主题装配：
  - `windows/tauri-client/src/shared/theme/theme.ts`
  - `windows/tauri-client/src/main.ts`
- 已新增全局样式入口与语义变量层：
  - `windows/tauri-client/src/shared/styles/index.css`
  - `windows/tauri-client/src/shared/styles/tokens.css`
- 已新增 `Element Plus` 变量覆盖层：
  - `windows/tauri-client/src/shared/styles/element.css`
- 已收口主题基础能力：
  - 亮色 / 暗色模式变量
  - 六套颜色家族变量
  - 页面、面板、边框、文本、强调色语义令牌
  - `Element Plus` 基础颜色、背景、边框、圆角、阴影变量对齐

## 本次新增验收（`tauri-client` Step 17）

- `pnpm build` 已在 `windows/tauri-client/` 下通过。
- 当前构建仍存在第三方依赖 `@vueuse/core` 的 Rollup 注释警告，以及打包体积提醒；均非本步引入，未影响构建通过。

## 本次确认边界（`tauri-client` Step 17）

- 当前只完成主题底座和 `Element Plus` 变量层，不改页面结构语义。
- 当前默认主题先固定为 `dark + higanRed`，后续再接设置页和运行时切换。
- 当前主题系统使用 `data-theme-mode` / `data-theme-color` 与 CSS 变量，不运行时拼接整份样式。
- 当前概念页中的具体卡片、页头、弹层视觉尚未迁移；下一步进入主窗口与初始化页视觉骨架重建。

## 本次新增完成（`tauri-client` Step 18）

- 已完成 `tauri-client` 当前轮 `Step 18`：重建主窗口与初始化页视觉骨架。
- 已新增主窗口与页面骨架样式：
  - `windows/tauri-client/src/shared/styles/layout.css`
- 已把应用根容器收为统一壳结构：
  - `windows/tauri-client/src/app/App.vue`
- 已把主页容器收为统一留白和主区布局：
  - `windows/tauri-client/src/pages/home/HomePage.vue`
- 已把初始化页迁移为概念稿一致的居中卡片骨架：
  - `windows/tauri-client/src/pages/init/InitPage.vue`
- 本步已落地的视觉骨架包括：
  - 根窗口背景与整页壳层
  - 统一外边距与主区高度收口
  - 初始化居中卡片、圆角、阴影和文本层级
  - 初始化加载图形容器与失败重试区域

## 本次新增验收（`tauri-client` Step 18）

- `pnpm build` 已在 `windows/tauri-client/` 下通过。
- 当前构建仍存在第三方依赖 `@vueuse/core` 的 Rollup 注释警告，以及打包体积提醒；均非本步引入，未影响构建通过。

## 本次确认边界（`tauri-client` Step 18）

- 当前只完成主窗口与初始化页视觉骨架，不进入主页顶部细节美化。
- 当前初始化页已移除旧的纯表单式骨架，改为正式壳层中的居中卡片结构。
- 当前主页与初始化页共用统一 `app-window` / `app-shell` 布局语义，为后续页头、列表面板和卡片样式迁移提供固定落点。
- 当前下一步进入主页顶部操作区视觉重建。

## 本次新增完成（`tauri-client` Step 19）

- 已完成 `tauri-client` 当前轮 `Step 19`：重建主页顶部操作区视觉。
- 已新增顶部操作区局部样式：
  - `windows/tauri-client/src/shared/styles/header.css`
- 已把主页顶部收为正式视觉结构：
  - `windows/tauri-client/src/widgets/AppHeader/AppHeader.vue`
- 本步已落地的顶部元素包括：
  - `YumeDisk` 大标题与设置按钮对位
  - 会话状态 pill
  - 添加按钮的圆形主操作样式
  - 三选项类型气泡的横向样式、无箭头样式和图标字母块
- 当前保持的交互语义：
  - 设置按钮仍为禁用占位
  - 添加入口仍然打开内存盘 / 文件盘 / 网络盘占位三选项
  - 内存盘、文件盘入口行为未变

## 本次新增验收（`tauri-client` Step 19）

- `pnpm build` 已在 `windows/tauri-client/` 下通过。
- 当前构建仍存在第三方依赖 `@vueuse/core` 的 Rollup 注释警告，以及打包体积提醒；均非本步引入，未影响构建通过。

## 本次确认边界（`tauri-client` Step 19）

- 当前只完成主页顶部操作区视觉，不进入磁盘列表面板和磁盘卡片视觉。
- 当前顶部操作区已经从临时 `Element Plus` 默认排布迁移到正式布局落点，但未引入设置页交互。
- 当前添加气泡仍由 `Element Plus` 弹层承接，只做视觉覆写，不重写弹层机制。
- 当前下一步进入磁盘列表面板视觉重建。
