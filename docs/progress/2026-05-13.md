# 2026-05-13

## 本次新增完成（`tauri-client` Step 15）

- 已完成 `tauri-client` 当前轮 `Step 15`：回滚主窗口“首屏 ready 后显示”方案。
- 原因是该方案在当前 `Tauri + WebView` 启动链下导致主窗口长时间不显示，实测不可接受。
- 已把启动链回滚到此前稳定状态：
  - `windows/tauri-client/src-tauri/tauri.conf.json`
  - `windows/tauri-client/src/main.ts`
- 当前恢复为：
  - 主窗口默认可见启动
  - 前端正常挂载 `App`
  - 不再做隐藏窗口后的延迟 `show()` 控制

## 本次新增验收（`tauri-client` Step 15）

- `cargo build` 已在 `windows/tauri-client/src-tauri/` 下通过。
- `pnpm build` 已在 `windows/tauri-client/` 下通过。

## 本次确认边界（`tauri-client` Step 15）

- 当前先保证主窗口可稳定显示，白屏优化暂不继续推进。
- 当前不再尝试基于隐藏主窗口 + 前端回调 `show()` 的启动链。
- 当前下一步回到最小验收路径，继续验证现有功能闭环。

## 本次新增完成（`tauri-client` Step 16）

- 已完成 `tauri-client` 当前轮 `Step 16`：重建当前轮 `todo` 方向，把目标从“功能最小验收”切换为“概念页视觉效果落地方案”。
- 已明确新的 UI 推进原则：
  - `Element Plus` 继续作为基础架构
  - 在其基础上叠加概念页已确认的视觉效果
  - 不脱离现有页面结构、功能入口和数据边界
  - 不为美化引入概念页之外的新模块、新入口和新交互

## 本次确认边界（`tauri-client` Step 16）

- 当前只重建 UI 视觉层方案，不回退到纯骨架阶段。
- 当前 UI 美化必须建立在现有 `Element Plus` 结构之上，不重写基础组件体系。
- 当前视觉目标以 `docs/tauri-client-ui-design.md` 和概念页现有呈现为准。
- 当前下一步应先重建全局主题变量与基础视觉骨架，再逐步落页头、列表面板、磁盘卡片和弹层样式。
