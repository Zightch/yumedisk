[package]
name = "rust-cli"
version = "0.1.0"
edition = "2026"

[dependencies]
backend_rust = { path = "../BackendRust" }
network_core = { path = "../network-core" }

[dev-dependencies]
network_core = { path = "../network-core", features = ["test-support"] }
